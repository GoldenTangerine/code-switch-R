# Code Switch v2.6.80

## 修复
- **首页供应商编辑弹窗宽度自适应**：供应商编辑弹窗支持按视口自适应放宽，修复内容区域过窄导致的横向滚动条问题。
- **弹窗内容防横向溢出**：为供应商编辑表单滚动容器补充 `min-width: 0` 与 `overflow-x: hidden` 约束，避免长字段或复杂控件撑破布局。

## 技术改进
- `BaseModal` 新增 `panelWidth` 可配置能力，支持按业务场景单独定制弹窗宽度，避免全局样式联动风险。

---

# Code Switch v2.6.79

## 新功能
- **日志存储改为弹窗入口**：日志页将“日志存储”从常驻面板改为头部按钮触发弹窗，支持在弹窗中查看数据库占用、明细/统计行数与空间，并保留清理能力。

## 修复
- **首页供应商 Token 口径统一**：首页供应商卡片 `Token` 统一按 `input + output + cache_read` 计算，和日志页口径保持一致；同时补充数值容错，避免异常数据导致显示 `NaN`。
- **供应商编辑弹窗底栏可见性**：修复首页“编辑供应商”弹窗在长内容场景下必须滚动到底部才能看到 `取消/保存` 的问题，底部操作栏改为固定可见且不遮挡最后一行内容。
- **日志筛选操作布局调整**：日志页查询按钮调整到“查询日期类型”后方（供应商下方），并与筛选表单提交行为保持一致。

## 技术改进
- `BaseModal` 新增 `bodyScrollable` 可配置能力（默认保持原行为），支持“内容区滚动 + 固定底部操作栏”布局模式。
- 清理日志页无引用样式，减少样式维护噪音并降低后续误改风险。

---

# Code Switch v2.6.78

## 新功能
- **日志模型分布面板**：在日志页请求卡片与折线图之间新增「模型占比」模块，包含模型占比饼图和模型列表（模型、请求、Token、金额），支持按当前筛选条件联动展示。

## 修复
- **模型统计 Token 口径统一**：后端模型统计与前端展示统一按 `输入 + 输出 + 缓存读取` 计算 `total_tokens`，不再混入 `reasoning`，并保持请求数、金额统计一致。
- **Token 明细缺项修复**：修复日志页 Token 明细弹窗仅重复展示输入的问题，补齐缓存读取项，确保明细与卡片总量可对齐。
- **费用公式可核对性增强**：计费公式中的 token 数值改为「单位缩写 + 原始值」双展示，避免仅显示 `k/M` 导致核算歧义。

## 体验优化
- **Token 展示单位自适应**：折线图坐标轴、tooltip、饼图 tooltip 及模型列表中的 token 统一支持 `k/M/B` 自动换算显示。
- **折线图可读性优化**：图例置顶显示名称、移除折线圆点、为各折线增加向 `x` 轴渐变填充，并将 tooltip 图例色块改为实心；暗色主题下图例与刻度文本颜色同步优化。

## 技术改进
- 新增 `ModelStatsRangeV2` 接口（含 `provider_id` 优先、`provider` 回退策略），前端新增 `fetchModelStatsV2` 与类型定义对接模型统计数据。
- 新增模型统计回归测试，覆盖 `provider_id` 优先匹配、`provider` 回退、token 口径与金额符号保持等关键场景。

---

# Code Switch v2.6.77

## 修复
- **日志 Token 流量口径调整**：日志页顶部「Token 流量」卡片改为 `输入 + 输出 + 缓存读取` 三项汇总，不再将 `reasoning` 计入该卡片，与当前统计口径保持一致。

## 技术改进
- 前端 `statsCards` 汇总公式调整为 `input_tokens + output_tokens + cache_read_tokens`，其余卡片与明细展示逻辑保持不变。

---

# Code Switch v2.6.76

## 修复
- **老库升级启动失败**：修复历史数据库残留 `request_log_stats_hourly_ai` / `request_log_stats_daily_ai` 触发器、但统计表缺失时，启动阶段执行 `request_log` 补列迁移触发 `no such table` 并导致应用退出的问题。升级流程现改为先清理旧统计插入触发器，再执行补列与统计存储重建，保证旧库可平滑升级。

## 技术改进
- 新增 `dropRequestLogStatsInsertTriggersWithDB` 兼容函数，统一处理历史版本残留统计触发器。
- 新增回归测试 `TestEnsureRequestLogTableWithDB_RepairsDanglingStatsTrigger`，覆盖“悬挂触发器 + 缺失统计表”的历史脏库场景，防止后续回归。

---

# Code Switch v2.6.75

## 新功能
- **供应商名称可编辑**：解除首页供应商编辑时对名称的限制，支持在不改变实体身份的前提下直接修改供应商名称。
- **导入支持同名供应商**：导入流程改为按 `API URL + API Key` 识别供应商身份，不再因为同名而阻止导入。

## 修复
- **身份键不一致导致的联动偏差**：统一健康检查、连通性检测、轮询与黑名单联动中的供应商引用规则，避免同一供应商在不同模块中被拆分成不同标识。
- **日志筛选供应商分裂**：修复历史日志中 `provider_id` 为空时，筛选项可能拆成 `name` 和 `id` 两个入口的问题；在“同名唯一映射到单一 id”场景下自动归并。

## 技术改进
- 新增统一身份引用工具，标准化数值 ID / 字符串 ID 的回退策略，降低后续模块接入的身份漂移风险。
- 重构日志供应商引用聚合逻辑，按最新请求时间选择展示名，并补齐同名多实体场景的安全分流。
- 新增导入去重与日志引用归并的回归测试，覆盖“同名可导入”“同名唯一映射归并”“同名多 id 不误合并”等关键边界。

---

# Code Switch v2.6.74

## 修复
- **日志页宽屏适配修复**：修复日志页面在超宽窗口下中间内容被固定宽度限制、无法随窗口拓宽的问题，页面主体现可随容器宽度自适应扩展。

## 体验优化
- **大屏留白更自然**：日志页头部与内容区横向内边距改为 `clamp` 响应式策略，在大屏和小屏下都保持更均衡的阅读间距。

---

# Code Switch v2.6.73

## 修复
- **预览弹窗底栏定位修正**：修复「Claude 官方价格预览」底部操作区未稳定贴底的问题，改为“内容区滚动 + 底栏固定区”结构，确保按钮始终在弹窗最底部可见。
- **同步按钮文本溢出修正**：修复“同步到内置价格表”按钮在窄宽度下文案溢出按钮边界的问题，优化按钮最小宽度与布局策略。

## 技术改进
- **弹窗滚动能力增强**：`InlineModal` 新增可配置 `bodyScrollable` 能力，支持在特定弹窗中由业务组件接管滚动布局。

---

# Code Switch v2.6.72

## 修复
- **预览弹窗操作区可见性**：修复「Claude 官方价格预览」在长列表滚动时底部按钮区被滚出可视区的问题，底部操作区改为常驻显示。
- **同步按钮文案溢出**：修复「同步到内置价格表」按钮在窄宽度下文字超出的问题，优化按钮布局与自适应换行策略。
- **同步来源时间可追溯**：模型价格列表中“网络同步”标签新增悬停提示，显示该模型最近网络同步时间（`yyyy-MM-DD HH:mm:ss`）。

## 技术改进
- **价格来源时间透传**：`ListModelPricing` 返回新增 `source_updated_at` 字段，前端类型与文案同步补齐。
- **交互细节优化**：预览弹窗底部操作区样式增强（sticky + 分隔线 + 按钮最小高度），提升可操作性与一致性。

---

# Code Switch v2.6.71

## 新功能
- **Claude 官方价格预览弹窗**：在「设置 > 模型价格」中点击 `同步 > Claude` 后，先展示官方价格预览弹窗，再由用户确认是否写入内置价格表。
- **预览信息增强**：预览列表完整展示官方模型名、Input、Output、Cache Hit、Cache 5m、Cache 1h，并显示与内置模型 key 的映射关系（含已映射/未映射状态）。

## 修复
- **官方价格页表头兼容性**：修复仅匹配旧表头导致的“未找到 Claude Model pricing 表格”问题，新增对 `Base Input Tokens`、`Output Tokens` 等表头变体兼容。
- **预览异步竞态**：修复弹窗关闭重开时旧请求回写 UI 的竞态问题，避免出现过期预览内容覆盖当前状态。
- **预览与同步数据漂移**：同步逻辑改为优先消费预览阶段缓存（一次性），避免确认同步时二次抓取导致结果不一致。

## 技术改进
- **共享样式抽取**：抽取模型列表共享样式到 `provider-model-list-shared.css`，统一首页模型列表与 Claude 价格预览弹窗视觉结构。
- **回归测试补齐**：新增表头别名解析、预览行构建、预览缓存 TTL 与一次性消费等测试用例，降低后续回归风险。

---

# Code Switch v2.6.70

## 修复
- **CI 打包 TypeScript 阻断**：修复「设置 > 模型价格」同步流程中 `task` 变量在赋值前被闭包引用导致的 `TS2454` 报错，恢复 `vue-tsc` 与自动打包流程。

---

# Code Switch v2.6.69

## 新功能
- **模型价格网络同步入口**：在「设置 > 模型价格」弹窗新增 `同步` 按钮与供应商选择小弹窗，支持一键同步 Claude 官方价格页。
- **价格来源标签增强**：模型价格列表新增来源标签，明确区分 `内置价格`、`网络同步`、`手动修改`。

## 修复
- **官方价格表列顺序变动风险**：修复抓取 Claude 官方价格表时依赖固定列下标的隐患，改为按表头索引解析，避免列重排导致价格错位写入。
- **同步状态并发误判**：修复同步任务在弹窗关闭后被误置空闲的问题，改为基于真实异步任务状态管理，避免重复触发同步请求。

## 技术改进
- **映射规则可维护性**：将 Claude 官方展示名到内置模型 key 的映射改为常量映射表，替代分支式硬编码，便于后续模型增量维护。
- **同步结果可观测性**：同步接口新增 `unchanged_models` 与 `unrecognized_models` 返回字段，前端增加同步摘要与未识别模型提示。
- **回归测试补齐**：新增表头重排解析、官方模型映射完整性、未识别模型回传等测试用例，降低价格同步回退风险。

---

# Code Switch v2.6.64

## 修复
- **流式缓存拆分计费偏差**：修复 Claude 流式日志在 `cache_create` 明细分片到达顺序不一致时可能出现 `5m + 1h > total` 的问题，避免缓存写入 token 被放大导致金额偏高。
- **缓存明细归一化口径**：统一 `cache_create_tokens / ephemeral_5m_tokens / ephemeral_1h_tokens` 的归一化策略，确保拆分明细不会反向抬高总量。

## 体验优化
- **日志缓存标签降噪**：日志页 `Cache write` 的 `5m/1h` 徽章改为仅在数值大于 `0` 时显示，减少无效 `0` 标签干扰阅读。

## 技术改进
- **Provider API 计费路径优化**：仅在存在 `1h` 缓存 token 时才计算 `ephemeral_1h` 单价，减少不必要的定价计算开销。
- **回归测试补齐**：新增流式分片顺序一致性测试与缓存拆分归一化/补齐测试，覆盖 `total/5m/1h` 的关键边界场景，降低计费回退风险。

---

# Code Switch v2.6.63

## 修复
- **接口计费口径对齐**：修复 `provider_api` 场景下 `cache_create` / `cache_read` 误按 input 单价计算的问题，现已按缓存倍率计费，日志金额与供应商后台口径一致。
- **接口价格日志可追溯**：补全「日志 > 使用请求明细」中接口价格的分步计算展示，支持按本次请求的 token 与分项成本还原计算过程。

## 技术改进
- 供应商价格解析增加缓存倍率字段兼容（`cache_create_*` / `cache_read_*`）。
- 新增计费回归测试，覆盖缓存倍率回退与接口缓存倍率映射。

## 下版本代办
- 按 Provider 实际分组应用 `group_ratio`（当前仅使用 `default` 组倍率）。

---

# Code Switch v2.6.57

## 体验优化
- **WebDAV 入口收敛**：将 WebDAV 同步能力收敛到「应用设置」中的单行入口，设置页更清爽；点击“配置与同步”后在弹窗内集中完成配置、测试与同步操作。
- **管理弹窗更稳**：WebDAV 管理弹窗宽度改为响应式（窄窗口不再横向溢出），加载配置期间入口按钮自动禁用，避免误触后看到不可操作状态。

## 修复
- **暗色模式按钮对比度**：修复“新版本已准备就绪”通知在 dark 模式下按钮背景与文字可读性异常的问题，统一了主/次按钮在不同状态下的视觉表现。
- **通知按钮样式串改**：修复全局 `button` 兜底样式对更新通知按钮与 spinner 的覆盖问题，避免尺寸、间距被全局规则意外改写。

## 技术改进
- 新增 `components.general.webdav.manage` 多语言文案键（中文/英文），避免复用无关模块文案导致后续维护耦合。
- 更新通知 dark 卡片背景增加 fallback，兼容不支持 `color-mix` 的环境。

---

# Code Switch v2.6.54

## 修复
- **托盘已用额度不再固定**：恢复「已用金额 = 实际消耗 + 调整值」口径，修复 Claude 在校准后已用值不再增长的问题。
- **设置保存稳定性**：`persistAppSettings` 增加防抖 + 排队重放 + 页面卸载前 flush，修复快速修改或切页时最后一次改动可能丢失的问题。

## 体验优化
- **托盘轮询降噪**：托盘窗口不可见时暂停分钟级刷新，恢复可见后立即补刷，降低无效数据拉取。
- **窗口高度同步更稳**：托盘高度缓存增加生命周期重置，减少重复 IPC 调整并避免重挂载后的高度不同步。

## 技术改进
- **防抖间隔可配置**：新增 `VITE_SETTINGS_PERSIST_DEBOUNCE_MS`（默认 `150`，范围 `0-2000`），并补充空值兜底逻辑。
- **开发文档对齐构建链路**：README 中 Node.js 要求更新为 `20.19+`（或 `22.12+`）。

---

# Code Switch v2.6.53

## 新功能
- **日志明细显示计价来源**：在「日志 > 使用请求明细」模型列新增“计价来源”标记，明确区分“接口模型价格 / 内置价格表 / 无可用价格”。
- **供应商模型价格自动刷新**：应用启动时自动刷新已配置供应商的模型价格缓存；点击供应商卡片时会再次刷新当前供应商的模型价格。

## 修复
- **0 价模型误回退**：修复接口价格为 `0` 时被误判为“无价格”并回退内置计费的问题，接口可用时始终优先使用接口价格。
- **金额 tooltip 口径不一致**：修复 `provider_api` 来源仍按内置价格公式拆解的误导展示，改为显示接口计价说明与日志记录金额。

## 技术改进
- 供应商价格刷新链路增加“点击节流 + in-flight 去重 + 启动限并发”，减少重复请求与启动瞬时压力。
- 请求日志计费链路完善为“接口缓存命中优先，未命中/异常回退内置价格表”，并将来源写入 `request_log.price_source` 便于审计与排查。

---

# Code Switch v2.6.52

## 体验优化
- **定价弹窗统一风格**：设置页「模型价格」弹窗对齐首页「模型列表」弹窗的展示风格（搜索 + pill 筛选 + 卡片列表），更一致也更好读。

---

# Code Switch v2.6.51

## 修复
- **CI 打包 TypeScript 报错**：主页 `GeminiProvider` 类型改为从 Wails bindings 推导，避免 `envConfig` 类型漂移导致 `vue-tsc` 失败。

--- 

# Code Switch v2.6.50

## 新功能
- **供应商模型列表**：首页每个供应商卡片新增「模型列表」按钮，一键查看该供应商的模型列表与定价信息。
- **厂商筛选 + 搜索**：按模型名/owner 识别厂商并分组展示，支持搜索过滤，定位模型更快。

## 体验优化
- **鉴权自动兜底**：拉取模型与定价时支持 Bearer / x-api-key 自动 fallback，减少“Key 没错但就是拉不下来”的玄学问题。
- **无定价提示**：当站点不提供定价接口时，弹窗明确提示仅展示模型名称，避免一屏 `—/M` 把人看懵。

---

# Code Switch v2.6.44

## 体验优化
- **日志页顶部栏吸顶**：顶部「返回首页 / 刷新」操作栏固定，滚动仅滚动下方内容区，避免操作按钮被滚走。
- **查询区布局收敛**：收紧「查询」与表单之间的间距，并将「查询」按钮移动到「日志存储」刷新按钮后方，按钮间距更统一。

---

# Code Switch v2.6.38

## 修复
- **测试污染用户配置**：修复 `providerrelay_models_test` 在运行时写入真实 `~/.code-switch` 目录的问题，避免测试把首页 Claude/Codex 供应商配置覆盖掉。

## 技术改进
- 新增测试隔离工具：统一将 `HOME` / `USERPROFILE` 指向 `t.TempDir()`，并清理 `HOMEDRIVE` / `HOMEPATH`，确保跨平台路径解析稳定。
- 新增测试文件落盘断言：明确校验配置仅写入临时目录，防止后续改动回退到真实用户目录。

---

# Code Switch v2.6.36

## 修复
- **日志金额气泡竞态**：修复金额列悬浮时的异步竞态问题，避免鼠标移出后气泡仍延迟出现
- **模型价格匹配口径**：补齐 `anthropic.` 前缀剥离与 `gpt-5-codex -> gpt-5` 别名匹配，减少“有价却显示无价”的误判
- **费用拆分一致性**：日志读取时补齐 `input/output/reasoning/cache` 的费用拆分字段，前端公式说明与后端计费保持一致

## 体验优化
- **金额气泡可读性**：气泡支持交互停留与滚动查看，长公式不再被截断，边界避让更稳定
- **样式细节**：优化金额 hover/focus 态与暗色主题对比度，提升整体视觉一致性

## 技术改进
- 金额气泡定位逻辑增加 `requestId + anchor` 防抖防串号机制
- 前端日志类型新增 `reasoning_cost` 字段，避免类型漏字段导致说明计算偏差

---

# Code Switch v2.6.35

## 新功能
- **模型价格模糊匹配**：当日志中的模型名未命中精确价格表时，自动匹配最相近模型并计算价格
- **日志模型列增强**：在日志列表中展示“原模型 + 计价匹配模型”，便于追踪价格来源

## 修复
- **Claude 流式 token 统计稳定性**：修复按行 SSE 回调和非标准响应场景下 token 解析遗漏问题，避免日志持续显示 `0`

## 技术改进
- 价格匹配升级为“精确命中 + 别名 + 相似度评分”的多阶段策略，降低新模型版本号变更导致的漏计费
- 日志读取阶段支持在 `total_cost=0` 但可命中价格时动态补算，兼容历史数据
- 新增 `model-pricing` 模糊匹配单测和日志解析回归测试，减少回退风险

---

# Code Switch v2.6.34

## 修复
- **Claude 日志 token 归零**：修复模型价格配置改造后，部分 Claude 请求在日志中 `input/output` 显示为 `0` 的问题
- **SSE 分片兼容**：修复 `data:{...}`（无空格）和跨 chunk 分片场景下的 usage 解析丢失
- **非流式 usage 兼容**：补齐非流式 JSON 响应的 usage 解析，避免只依赖 SSE 行格式导致漏记

## 技术改进
- Claude usage 解析新增多字段兼容：`input_tokens/output_tokens`、`prompt_tokens/completion_tokens` 及缓存字段别名
- 新增回归测试，覆盖 SSE 分片、非流式 JSON 分片与字段优先级，防止 token 统计回退

---

# Code Switch v2.6.33

## 体验优化
- **模型价格弹窗二段式**：弹窗第一页仅展示模型列表；点击列表中的模型才会弹出编辑弹窗，保存后自动关闭编辑弹窗并回到列表
- **弹窗宽度更合理**：列表弹窗更宽方便看表，编辑弹窗更窄专注改参数

## 技术改进
- **嵌套弹窗滚动锁定修复**：滚动锁改为引用计数，避免二层弹窗关闭时页面滚动被提前解锁

---

# Code Switch v2.6.31

## 修复
- **WebDAV 同步卡进度/大文件卡顿**：同步改为逐文件上传（不再本地打包 zip），进度更平滑，也更容易定位卡住的具体文件
- **同步内容过滤**：排除 `.DS_Store` 等无关文件，避免把 macOS 垃圾同步上去
- **错误可见性**：同步弹窗新增实时日志区，上传失败会在进度下方持续输出错误信息

## 技术改进
- WebDAV 备份采用 `manifest.json` + 目录结构（兼容旧 zip 备份的恢复）

---

# Code Switch v2.6.30

## 新功能
- **同步进度弹窗**：同步到 WebDAV 时展示同步内容清单与实时进度（含上传字节与远程 URL）

## 修复
- **WebDAV 上传兼容性**：PUT 请求补齐 `Content-Length`，兼容部分不支持 chunked 上传的 WebDAV / 网盘网关
- **设置项遮挡**：修复 WebDAV「同步操作 / 同步内容」等长文案在设置页被裁切的问题

## 技术改进
- WebDAV 同步流程通过 Wails Event 推送阶段与进度，前端可实时订阅展示
- 新增本地同步内容预览接口（用于弹窗展示）

---

# Code Switch v2.6.29

## 新功能
- **WebDAV 同步备份/恢复**：在「通用设置」新增 WebDAV 配置，支持一键上传本地配置到 WebDAV、以及从 WebDAV 下载并覆盖本地（下载前自动创建本地备份）
- **可配置超时**：支持设置 WebDAV 请求超时（秒），避免网盘/自建盘网络抖动导致同步失败

## 修复
- **大文件下载内存占用**：下载改为流式落盘，避免压缩包较大时内存飙升
- **界面语言一致性**：成功提示统一走 i18n，避免英文界面弹中文提示

## 技术改进
- SQLite 备份优先使用 `VACUUM INTO` 做一致性快照；失败时自动执行 WAL checkpoint 并重试，尽量避免导出“缺 WAL 的半残包”
- WebDAV redirect 兼容 `303 See Other` 等常见返回

---

# Code Switch v2.6.26

## 新功能
- **日志统计汇总独立存储**：请求量 / Token / 缓存读取 / 金额总数按本地小时/日聚合入库，查询更快
- **日志存储占用与清理**：新增存储占用面板，支持清除“请求明细”和“一键清除统计汇总”

## 修复
- **日志日期类型联动**：修复切换年/月/日/时间段后，卡片与折线图仍查询当天的问题
- **拉黑配置保存提示**：保存成功/失败 toast 提示更明确

## 技术改进
- 新增 `request_log_stats_hourly` / `request_log_stats_daily` 聚合表及 trigger，降低实时扫表压力
- 增加日志存储统计：展示 DB 使用/可回收空间、各表记录数与占用估算

---

# Code Switch v2.6.25

## 新功能
- **日志查询条件置顶**：平台、供应商、查询按钮移到页面最上方，查询更顺手
- **日志查询日期类型**：新增日期类型（全部 / 年 / 月 / 日 / 时间段），按类型展示不同日期选择器
- **拉黑时长更细粒度**：设置中新增 30 秒、1 分钟、3 分钟拉黑时长（原最小 5 分钟）

## 修复
- **按真实本地日历日筛选日志**：修复时区/日界线导致的“差一天/差几个小时”问题

## 技术改进
- 为 `request_log` 增加按时间/平台/供应商的索引，提升日志查询性能
- 拉黑时长固定模式改用秒级存储（兼容旧分钟配置）

---

# Code Switch v2.6.14

## 新功能
- **自适应热力图**：新增供应商使用热力图功能，直观展示各供应商的请求分布情况，支持按时间范围筛选
- **图标搜索**：新增供应商图标搜索功能，方便快速查找和选择合适的图标

## 修复
- 修复控制台日志递归爆炸问题

---

# Code Switch v2.0.0

## 新功能
- **自定义 CLI 工具支持（Others Tab）**：新增"托管自定义 CLI"功能，支持为任意 AI CLI 工具（如 Droid、RooCode 等）配置代理托管。用户可以自定义配置文件路径、格式（JSON/TOML/ENV）和代理注入字段，实现统一的供应商管理。
- **多配置文件编辑器**：支持为每个自定义 CLI 工具管理多个配置文件，提供实时编辑、格式校验和一键保存功能。

## 修复
- **自定义 CLI 代理路由**：修复自定义 CLI 工具的代理路由 `/custom/:toolId/v1/messages`，确保请求正确转发到供应商。
- **代理注入 URL 格式**：修复代理注入时 URL 路径拼接问题，确保生成正确的 `http://127.0.0.1:18100/custom/{toolId}` 格式。
- **Windows 路径扩展**：修复 Windows 系统下 `~\` 路径前缀的展开问题，正确识别用户主目录。
- **前端 toast 提示**：修复创建/更新 CLI 工具后的成功提示显示。
- **Claude 代理开关状态检测**：修复刷新后 Claude 代理开关显示为关闭的问题。根本原因是 Claude CLI 可能覆盖 `ANTHROPIC_AUTH_TOKEN`，现改为仅检查 `ANTHROPIC_BASE_URL` 是否指向本地代理。

## 技术改进
- 新增 `CustomCliService` 服务，提供完整的 CRUD 和代理状态管理
- 前端新增 `CustomCliConfigEditor` 组件，支持多文件编辑和格式校验
- 中英文国际化支持完善

---

# Code Switch v1.5.4

## 更新亮点
- 🔧 **彻底修复 Claude 代理开关状态问题**：根本原因是后端 `ProxyStatus` 使用 `map[string]string` 解析 `env`，当配置文件中存在非字符串值时解析失败，导致状态始终返回 `false`。现改用 `map[string]any` 宽容解析。
- 🔄 **CI 自动同步版本号**：构建时自动从 Git Tag 提取版本号，更新到所有平台配置文件。
- 📝 **Release 自动提取更新说明**：从 `RELEASE_NOTES.md` 自动提取当前版本的更新内容。

# Code Switch v1.5.3

## 更新亮点
- 🔧 **代理开关状态持久化修复**：修复了刷新页面后代理开关显示为关闭状态的问题，实际上代理仍在运行。问题根源是 Wails RPC 返回的字段名与前端读取的字段名不一致。
- ✏️ **CLI 配置预览可编辑**：配置文件预览区域新增解锁编辑功能，用户可以直接修改配置内容，支持 JSON/TOML/ENV 格式自动解析和校验。
- 🎨 **Tab 按钮左对齐**：Claude Code / Codex / Gemini 三个选择按钮现在与供应商卡片左边缘对齐，视觉更统一。
- 📖 **README 重写**：完全重写 README 文档，面向小白用户，3 步快速开始，包含常见问题解答。
