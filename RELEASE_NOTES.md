# Code Switch v2.11.14

## 修复
- **修复“已拉黑”标签点击无响应**：移除导致弹窗节点引用异常的单项循环，点击后可正常显示完整拉黑详情。
- **修复供应商卡片客户端初始化异常**：调整额度状态监听器的声明顺序，避免真实 DOM 挂载时提前访问尚未初始化的数据。

## 优化
- **收敛拉黑详情交互**：详情改为仅点击打开，再次点击、点击外部、按 Esc、状态变化或执行操作后关闭，并保留焦点恢复与弹窗互斥。
- **完善操作反馈与回归覆盖**：“已拉黑”标签保持手形光标，新增真实 DOM 点击、Teleport 展示、外部关闭和键盘关闭测试。

## 验证
- 已通过前端 71 个测试文件共 565 项单元测试、TypeScript 类型检查与 `git diff --check`。

## 发布
- **本次发版版本号推进到 `v2.11.14`**：同步应用常量、全平台构建元数据、依赖锁文件与发布说明，并推送 `v2.11.14` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.13

## 修复
- **修复拉黑详情弹层闪退**：点击或悬停“已拉黑”标签后，详情可稳定展示；标签边框、弹层及过渡区域共同参与悬停判断，移入弹层不会误关闭。
- **保持弹层状态互斥**：拉黑详情与额度错误详情不会同时显示，点击外部、Esc、状态变化和操作完成后均可正常关闭。

## 优化
- **适配视口空间**：弹层根据上下可用空间自动调整位置和高度，窄窗口及长原因内容保持可读，操作按钮持续可见。
- **补齐交互与主题覆盖**：完善焦点恢复、定时器清理、亮暗主题文字颜色及边界行为回归测试。

## 验证
- 已通过前端 71 个测试文件共 564 项单元测试、TypeScript 类型检查与 `git diff --check`。

## 发布
- **本次发版版本号推进到 `v2.11.13`**：同步应用常量、全平台构建元数据与发布说明，并推送 `v2.11.13` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.12

## 性能优化
- **大幅加速请求负载脱敏**：ASCII 请求使用无分配快速扫描，Unicode 内容继续保留原有匹配语义；1 MiB 基准中 raw 捕获由 410.463 ms 降至 2.051 ms，脱敏捕获由 1.334 s 降至 334.701 ms。
- **合并日志仪表盘统计读取**：新增兼容聚合接口，前端刷新不再重复获取同一范围日志；100k Proxy 冷缓存基准由 3.435 s 降至 1.248 s，分配量由约 2.617 GB 降至约 783.718 MB。
- **降低供应商快照刷新开销**：SQLite 平台直接计算有序存储行指纹，并消除保存后的额外查询；100 个 Provider 稳定刷新中位数由 2.077 ms 降至 0.447 ms。
- **减少后台无效调用**：主窗口隐藏时暂停首页周期轮询，恢复时立即刷新；设置页保持 AppSettings 先加载，其余六项独立数据并行读取。

## 稳定性
- **修复 SQLite 连接池忙等待不一致**：每个新连接都应用 30 秒 `busy_timeout`，固定竞争测试中的 `SQLITE_BUSY/LOCKED` 由中位数 592/596 降至 0/596。
- **修复事务组返回后的任务竞态**：`ExecTxGroup` 在正常完成时等待全部结果通道关闭，调用方可安全复用任务切片。
- **统一代理尝试状态**：收敛成功、失败、停止条件及最终错误出口，保持轮询、降级、黑名单、会话绑定和上游错误透传语义不变。
- **保持快照外部变更一致性**：保存端直接绑定本次事务指纹，raw 格式变化只同步指纹，不丢失运行时字段或触发无关业务副作用。

## 交互优化
- **优化拉黑详情悬浮体验**：支持延迟打开、移入弹层取消关闭、点击固定显示及长原因区域独立滚动；点击外部、Esc 和状态切换行为保持不变。
- **优化隐藏与恢复体验**：窗口重新显示时先刷新一次再恢复原轮询周期，重复可见性信号不会重复启动定时器。

## 工程质量
- **补齐关键性能与行为基线**：覆盖代理调度、请求负载、数据库双队列、SQLite 写竞争、日志聚合、Provider 快照及前端生命周期。
- **形成可持续优化计划**：记录 15 项问题与 14 个可独立验证、回退的批次，首轮规划批次均已完成。

## 验证
- 已通过 Go services 全量测试、根包测试、关键竞态测试、前后 5 轮性能基准、前端 71 个测试文件共 552 项单元测试、TypeScript 类型检查与 `git diff --check`。

## 发布
- **本次发版版本号推进到 `v2.11.12`**：同步应用常量、全平台构建元数据、更新日志与发布说明，并推送 `v2.11.12` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.11

## 优化
- **区分启用与未启用供应商卡片**：未启用供应商在亮色和暗色主题下使用中度灰度背景、边框、阴影、图标及非告警徽标，便于快速识别当前可参与调度的供应商。
- **优化暗色停用层级**：压暗未启用卡片背景，避免停用供应商比启用供应商更突出；悬停继续保持灰度并提供中性边框反馈。

## 修复
- **保留额度自动停用告警**：额度耗尽自动停用的供应商继续显示橙色边框、左侧强调线及临时启用操作，不再被普通停用样式覆盖。
- **补齐状态样式回归覆盖**：增加启用、手动停用、额度自动停用及样式优先级测试，防止组合状态再次丢失告警强调。

## 验证
- 已通过前端 69 个测试文件共 536 项单元测试、TypeScript 类型检查、`git diff --check`，并完成 Chrome 亮色与暗色截图复验。

## 发布
- **本次发版版本号推进到 `v2.11.11`**：同步应用常量、全平台构建元数据、更新日志与发布说明，并推送 `v2.11.11` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.10

## 优化
- **取消供应商事件自动切换平台 Tab**：供应商切换或拉黑事件到达时，页面停留在当前平台 Tab，不再被强制切换到事件对应的平台。
- **取消高亮卡片自动居中滚动**：移除供应商事件高亮时卡片自动滚动居中的逻辑，浏览列表位置不再被打断，并清理相关的 DOM 引用链路。

## 行为调整
- **非当前 Tab 仅静默更新状态**：其他平台的事件只同步最近使用供应商与黑名单状态，不触发隐藏卡片的脉冲高亮。
- **当前 Tab 保留 3 秒脉冲高亮**：当前平台的供应商事件继续以卡片脉冲效果提示，其余事件同步行为保持不变。
- **补齐回归覆盖**：增加切换与拉黑事件在当前、非当前 Tab 下的定位行为测试。

## 验证
- 功能完成阶段已通过前端 69 个测试文件共 534 项单元测试及 TypeScript 类型检查；本次发布按要求不重复运行测试、构建或 lint。

## 发布
- **本次发版版本号推进到 `v2.11.10`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.10` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.9

## 修复
- **修复托盘额度查询失败展示错误详情**：供应商额度查询失败时不再显示接口返回的错误信息、金额、进度和重置详情，仅显示与首页供应商一致的静态错误图标。
- **修复有效金额掩盖查询错误**：只要额度结果携带 `invalidMessage`，即统一按查询失败处理，避免错误响应仍被展示为可用金额。

## 优化
- **统一托盘与首页错误状态样式**：错误图标同步首页的尺寸、颜色及明暗主题效果，并补充无障碍状态播报。
- **补齐错误状态回归覆盖**：增加错误详情隐藏、金额与错误并存、图标结构、无障碍属性及明暗主题样式测试。

## 验证
- 功能完成阶段已通过前端 69 个测试文件共 530 项单元测试及 TypeScript 类型检查；本次发布按要求不重复运行测试、构建或 lint。

## 发布
- **本次发版版本号推进到 `v2.11.9`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.9` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.8

## 修复
- **修复 Claude Code 子进程供应商漂移**：优先使用 Claude Code 官方会话 Header 识别主 Agent、一级子 Agent 和嵌套子 Agent，`code-switch-r-subagent` 子进程继承主进程最近成功使用的供应商。
- **兼容新旧会话标识混用**：旧版主请求的 `metadata.user_id` 与新版子请求的 `x-claude-code-session-id` 自动归一到同一根会话，避免子进程切换到新供应商。

## 优化
- **完善 Claude Sticky Session 继承**：子 Agent 优先继承直接父节点绑定，父节点尚未确认时回退到根会话绑定；官方 Header 缺失时保留原有请求体识别逻辑。
- **限制会话别名缓存**：根会话别名按容量和空闲时间清理，不记录原始会话 ID、Agent ID 或父 Agent ID。
- **补齐回归覆盖**：增加官方 Header 身份树、混合 metadata/Header、普通模型映射和嵌套 Sticky 继承测试。

## 验证
- 功能完成阶段已通过 Go 服务测试、竞态检查与 `git diff --check`；本次发布按要求不重复运行测试、构建或 lint。

## 发布
- **本次发版版本号推进到 `v2.11.8`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.8` Tag 触发 GitHub Actions 全平台自动打包。

# Code Switch v2.11.7

## 新增
- **新增供应商强制优先**：首页供应商支持设置唯一的强制优先状态；在供应商可用、模型兼容且会话与并发容量未满时，新会话优先尝试该供应商。
- **统一强制优先路由覆盖**：Claude、Codex、Gemini、自定义 CLI、模型列表和托盘预览同步使用强制优先规则；已有会话继续保持原供应商绑定。

## 修复
- **修复强制优先保存竞态**：快速重复点击只触发一次保存，保存占用时不再错误提示成功，失败后恢复本地状态。
- **修复 Gemini 切换非原子问题**：新增后端原子接口，切换失败时整体回滚；前端失败后重新读取服务端真实状态，避免供应商状态漂移。
- **修复供应商复制状态继承**：复制供应商不会继承源供应商的强制优先状态，并保持同平台唯一性。

## 优化
- **优化首页状态识别**：强制优先按钮使用高对比蓝色激活态并显示“强制”徽标，保存期间统一禁用，便于快速识别当前状态。
- **完善无障碍交互**：图钉按钮补充动态标签、忙碌状态、指针反馈和亮暗主题键盘焦点样式。
- **补齐回归覆盖**：增加后端唯一性、原子切换、失败恢复，以及前端并发保存和可访问性测试。

## 验证
- **功能完成阶段已通过**：Go 服务测试、前端 68 个测试文件共 527 项单元测试、TypeScript 类型检查和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.7`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.7` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.6

## 修复
- **修复新会话未优先使用首页首位供应商**：开启会话粘滞后，首页排序第一的当前可用供应商若尚无会话，新会话会跨 Level 优先尝试，符合用户排序优先级。
- **修复并发冷启动重复抢占首位供应商**：在会话锁内复核零会话条件并创建临时绑定；并发新会话只有首个请求占用首位，其余请求继续按原 Level、容量和负载规则调度。

## 优化
- **保持既有会话与降级行为稳定**：已有临时或确认绑定继续使用原供应商；首位不可用、拉黑、配置无效、模型不兼容或尝试失败时，保留原有过滤、重试和降级逻辑。
- **统一全平台路由与预览结果**：Claude、Codex、Gemini、自定义 CLI 以及托盘默认供应商预览统一应用首位优先规则。
- **补齐并发与真实入口覆盖**：增加 Provider、Gemini 真实调度入口、并发屏障和竞态回归用例，验证首位与后备供应商分配结果。

## 验证
- **功能完成阶段已通过**：目标测试、Provider/Gemini 并发用例连续运行、竞态检测、Go 服务全量测试、`go vet` 和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.6`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.6` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.5

## 修复
- **修复 Cursor API 代理下的 Codex 会话乱切供应商**：优先识别 Cursor 会话标识；无显式标识的 Responses 请求使用稳定的 `prompt_cache_key` 归一同一会话，避免请求在多个供应商间漂移。
- **修复 Codex 父子会话关系异常**：统一线程、会话与对话标识的 Hash 语义，消除仅父级字段形成的自引用，并保留请求体与请求头合并后的父级、根级继承关系。
- **修复供应商改写请求体影响会话识别**：会话身份始终从入口原始请求体提取，不受供应商覆盖或协议转换后的请求体影响。

## 优化
- **完善会话识别来源展示**：请求日志记录 Cursor 会话、Codex 显式会话或缓存键来源，首页连接详情的活跃与历史记录同步显示紧凑标签。
- **完善深浅主题与数据库兼容**：来源标签支持亮色、暗色主题及窄窗口换行；旧数据库自动补充来源字段，且不保存原始会话 ID 或缓存键。
- **补齐会话边界回归覆盖**：覆盖顶层 Cursor 标识、父根字段、通用 `id` 优先级、body/header 关系合并和入口原始请求体选择。

## 验证
- **功能完成阶段已通过**：Go 服务测试、前端 68 个测试文件共 519 项单元测试、TypeScript 类型检查和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.5`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.5` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.4

## 修复
- **修复日志详情复制失败**：请求与响应原文、格式化内容统一走可靠的剪贴板降级链路，避免 WebView 拒绝当前用户代理权限时直接提示复制失败。
- **修复浏览器预览误调 Wails Runtime**：识别 `Arch: browser` 环境并跳过原生剪贴板调用，保留浏览器 API 与 textarea 降级能力。

## 优化
- **统一剪贴板处理**：模型调试、OAuth、控制台、供应商日志和额度错误复制复用公共工具，保留原生错误与降级错误，临时节点异常时也能清理。
- **优化文本选择**：日志详情、供应商错误、模型调试、控制台错误和 OAuth 可见文本支持鼠标选择及系统快捷键复制，并收窄选择样式范围。
- **清理无效文案与补齐回归覆盖**：移除不再使用的剪贴板不可用文案，增加日志载荷详情和剪贴板失败场景测试。

## 验证
- **功能完成阶段已通过**：前端 68 个测试文件共 518 项单元测试、TypeScript 类型检查、双语 JSON 解析和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.4`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.4` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.3

## 修复
- **修复无会话时托盘供应商缺失**：没有活跃请求时，改为显示下一个新会话预计调用的供应商，避免已开启平台不显示任何供应商。
- **修复默认供应商与实际路由不一致**：预览同步考虑 Level、用户顺序、黑名单、轮询游标、会话容量与负载、并发限制，且不会推进真实轮询状态。
- **修复托盘查询状态误判**：运行态接口失败或批量结果缺少平台时显示“查询失败”，不再误报为“暂无可用供应商”。

## 优化
- **精简托盘实时查询**：批量接口只返回供应商并发计数和默认候选，不再向托盘序列化完整请求明细。
- **优化连接详情布局**：会话编号、会话层级和“切换”入口收紧到模型行，长模型名与接口自动省略，窄窗口自适应换行。
- **保持托盘刷新边界**：仅查询首页供应商开关已显示的平台；托盘失焦、隐藏或卸载时清除相关刷新定时器。

## 验证
- **功能完成阶段已通过**：Go 服务测试、前端 67 个测试文件共 515 项单元测试、TypeScript 类型检查和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.3`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.3` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.2

## 新增
- **托盘展示实时调用供应商**：开启路由、轮询或会话粘滞后，同一平台正在调用的多个供应商会同时展示活跃请求数、今日统计和额度信息。
- **连接详情增加显式切换入口**：活跃连接行提供“切换”按钮，可直接查看当前会话的候选供应商与可用状态。

## 修复
- **修复 Codex TUI 会话识别**：解析 `x-codex-turn-metadata`、父线程请求头和 `client_metadata`，补齐根会话、子会话及供应商切换所需的会话编号。
- **修复托盘固定显示首个供应商**：优先使用实时并发状态，无活动请求时回退最近调用供应商，避免路由结果与托盘展示不一致。
- **修复托盘刷新竞态与资源占用**：平台开关变化时丢弃旧快照；托盘失焦、隐藏或关闭后清除定时刷新并停止后续额度查询。
- **修复会话候选状态覆盖**：关闭弹窗、关闭 Sticky 或切换连接后，使旧候选请求失效，避免过期结果覆盖当前状态。

## 优化
- **遵循首页供应商开关**：托盘仅展示设置中已开启的首页供应商 Tab；开启“其他”时同步支持 Custom CLI 工具。
- **降低托盘刷新调用量**：新增供应商并发状态批量查询，每秒 Wails 调用固定为两次，不再随平台数量增长。
- **完善多供应商布局**：增加轻量滚动条、供应商分区、长名称换行、键盘焦点及深浅色不可用状态。

## 验证
- **功能完成阶段已通过**：Go 服务测试、前端 67 个测试文件共 507 项单元测试、TypeScript 类型检查和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.2`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.2` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.1

## 修复
- **修复余额查询失败展示历史数据**：查询失败时清除历史余额缓存，供应商卡片第一层不再显示过期余额。
- **修复额度错误悬浮窗定位**：卡片统计状态或额度行布局变化后自动重新计算悬浮窗位置，避免固定弹窗停留在旧位置。

## 优化
- **优化额度失败信息展示**：卡片仅显示错误图标和刷新按钮，完整错误详情在错误图标悬浮窗中显示。
- **完善错误详情交互**：支持 100ms 延迟悬浮打开、点击锁定、触控点击、Esc/外部关闭，双击可复制完整错误信息并反馈复制结果。
- **补充额度查询回归覆盖**：增加错误悬浮交互、复制 payload 及失败后恢复成功的测试覆盖。

## 验证
- **功能完成阶段已通过**：前端 65 个测试文件、497 项单元测试、TypeScript 类型检查和 `git diff --check` 均已通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.1`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.1` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.11.0

## 新增
- **新增 Sticky Session 会话粘滞**：按平台开启会话粘滞，同一主会话及其子会话优先复用当前供应商，新会话在最高优先级内按供应商容量与负载均衡分配。
- **新增人工切换供应商**：连接详情可查看当前会话的可用供应商并强制切换，切换会传播到整个子会话树。
- **新增会话容量与 TTL 配置**：供应商支持最大会话数和空闲释放时间配置，降低供应商速率限制下的集中请求风险。

## 修复
- **修复父子会话身份匹配**：统一 `session_id`、`parent_session_id`、多层父子关系及 Claude Subagent 身份 Hash，避免会话继承与整树迁移失效。
- **修复并发迁移与旧请求回滚**：后备供应商可在同会话存在活跃请求时接管；Revision 保护避免旧请求失败覆盖人工切换或自动迁移结果。
- **修复工具调用会话关联**：真实转发链注册响应中的工具调用 ID，使无稳定会话标识的后续请求仍能保持供应商粘滞。
- **修复迁移后的负载统计**：旧供应商活跃请求按实际供应商单独计数，容量、TTL 和候选供应商状态保持一致。

## 优化
- **完善连接详情交互**：当前供应商不可再次选择，Sticky 关闭时提示先开启，切换成功后自动关闭候选弹窗；中英文文案同步更新。
- **清理会话关系缓存**：绑定过期或超过容量时同步淘汰关系记录，避免长期运行内存增长。

## 验证
- **功能完成阶段已通过**：Go 服务测试、前端 493 项单元测试、TypeScript 类型检查、双语 JSON 解析和 `git diff --check` 均通过；本次发布按要求不重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.11.0`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.11.0` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.10.5

## 修复
- **修复通用模板零余额被误判为不限额**：General、NewApi、自定义等非 Sub2API 查询中，余额为 `0` 或负数时继续按额度耗尽处理，避免供应商余额用尽后仍显示不限额。
- **收紧 Sub2API 不限额判定**：仅当 `subscription` 的日、周、月 limit 字段完整且均明确为 `0` 时显示不限额；字段缺失、非法或存在非零限制时不再误判。
- **保留 Sub2API 真实余额**：脚本误报不限额但原始订阅限制不满足条件时保留实际余额，避免正余额被清零并错误触发自动停用。

## 优化
- **完善首页与托盘额度展示**：长套餐名称在窄宽度下自动省略并保留完整悬浮提示，进度条补充辅助技术可读的进度语义。
- **增加前后端防御性校验**：后端统一收口不限额语义，前端仅对 Sub2API 展示不限额状态，并补充对应回归覆盖。

## 验证
- **功能修复阶段验证通过**：6 个 Go 回归入口、4 个前端测试文件共 57 项测试、TypeScript 类型检查和 `git diff --check` 均已通过；按发布要求未重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.10.5`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.10.5` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.10.4

## 新增
- **新增 Sub2API 订阅额度查询**：供应商额度查询配置在 NewApi 后新增 Sub2API 模版，支持独立配置 Base URL 与 API Key，默认请求 `/v1/usage`。
- **同步首页与托盘额度展示**：按接口响应展示日、周、月限制进度和刷新倒计时，限制为 `0` 的周期按不限额处理。

## 修复
- **修复查询模版脚本串用**：切换查询模版时加载目标模版默认预设，避免 Sub2API 继续请求 General 的 `/user/balance`。
- **修复无限订阅被误判耗尽**：完整透传无限状态，不再将 `remaining: -1` 归零后自动停用供应商；首页显示“不限额”，托盘显示 `∞`。
- **修复周期标题与异常倒计时**：首页和托盘统一使用日、周、月标题；周窗口为空时不再生成 1970 年刷新时间。

## 优化
- **统一额度数据转换**：复用标准周期标题、无限状态和托盘转换逻辑，保持明暗主题及现有紧凑卡片布局。
- **补齐回归覆盖**：覆盖默认脚本执行、模版切换、无限订阅、自动停用、周期标题和托盘展示。

## 验证
- **功能完成时验证通过**：64 个前端测试文件、491 项单元测试、Go 服务测试、TypeScript 类型检查、双语 JSON 与 `git diff --check` 均已通过；按发布要求未重复运行测试。

## 发布
- **本次发版版本号推进到 `v2.10.4`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.10.4` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.10.3

## 修复
- **收纳供应商额度查询错误**：首页供应商卡片查询剩余额度失败时只显示错误图标，点击后再展示完整错误信息，避免长错误文本破坏卡片布局。
- **修复错误悬浮窗边界问题**：悬浮窗脱离卡片裁剪区域，并根据视口空间自动调整方向、位置和最大高度。
- **修复弹窗交互冲突**：查看或选择错误文本时暂停卡片拖拽；切换平台或供应商后自动关闭旧弹窗，避免状态残留。

## 优化
- **完善悬浮窗体验**：支持点击外部、再次点击和 `Esc` 关闭，补充焦点恢复、长文本换行、明暗主题及无障碍状态。
- **保持卡片信息紧凑**：错误状态继续显示更新时间和刷新入口，多条查询错误在同一悬浮窗内汇总展示。

## 验证
- **补充回归覆盖**：覆盖有无统计数据时的紧凑展示、视口右边界、底部翻转与小视口约束；发布前已通过 64 个前端测试文件、486 项单元测试和 TypeScript 类型检查。

## 发布
- **本次发版版本号推进到 `v2.10.3`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.10.3` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.10.2

## 新增
- **首页供应商 Tab 排序**：设置页的已显示 Tab 支持拖拽和键盘排序，重新显示的 Tab 自动追加到末尾。

## 修复
- **修复供应商 Tab 显隐设置失效**：补齐 Grok Build、Claude Desktop、OpenClaw、Hermes 和 Pi 的后端允许列表，首页现按设置正确显示、隐藏并保持自定义顺序。
- **保证至少显示一个 Tab**：最后一个可见项不可隐藏，活动 Tab 被隐藏时自动回退到第一个可见项。
- **修复设置保存同步竞态**：返回首页前等待防抖和排队保存完成，且仅允许最新设置请求更新页面状态。

## 优化
- **完善设置交互**：区分已显示与未显示 Tab，增加拖拽目标、键盘焦点和减少动画状态。
- **优化窄窗口布局**：首页 Tab 与操作区在中等及窄窗口下自动分行，避免多 Tab 被裁切。

## 验证
- **补充回归覆盖**：覆盖显隐下限、持久化顺序、拖拽、键盘移动与活动项回退；前端 482 项单元测试、TypeScript 类型检查和 Go 服务测试已通过。

## 发布
- **本次发版版本号推进到 `v2.10.2`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.10.2` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.29

## 新增
- **Claude Subagent 会话供应商跟随**：开启 Claude 模型路由并识别到稳定会话后，Subagent 默认优先使用当前会话主请求最近成功的供应商，避免主会话与 Subagent 在供应商聚合场景下分流。
- **路由结果可见性**：活动连接和请求日志记录会话首选供应商及实际选择结果，详情面板显示“已跟随”或“已降级”。

## 修复
- **修复会话供应商状态竞态**：并发主请求按最新活动优先，失败时回滚到仍有效的请求；较旧请求成功不会覆盖更新的会话首选。
- **修复首选供应商降级顺序**：首选供应商跨 Level 优先尝试，失败后保留原有 Level 和轮询起点继续降级。
- **修复并发槽时序**：供应商未获取到并发槽时不发布临时会话首选，避免满载请求污染后续 Subagent 路由。

## 优化
- **限制会话缓存规模**：非活动会话最多保留 4096 条，按最近使用顺序淘汰，避免长期运行时无界增长。
- **保持兼容行为**：未识别模型、缺少稳定会话标识或未开启模型路由时继续使用原有路由；普通主请求的会话亲和开关保持关闭。

## 测试
- **补充路由回归覆盖**：覆盖跨 Level 跟随、失败回滚、并发竞态、缓存淘汰、满载供应商及前端日志/连接详情展示。
- **验证结果**：Go 服务测试、前端 458 项单元测试、TypeScript 类型检查和 `git diff --check` 均通过。

## 发布
- **本次发版版本号推进到 `v2.9.29`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.29` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.28

## 修复
- **修复 macOS 27 托盘左键事件被丢弃**：合并 Wails 官方 PR `#5919` 的事件类型恢复逻辑；当 `NSApp.currentEvent` 不再返回鼠标按下事件时，通过 `pressedMouseButtons` 恢复左右键，使 `SystemTray.OnClick` 能正常打开托盘弹窗。

## 兼容
- **保持 macOS 26 及以下行为**：原有 `LeftMouseDown` / `RightMouseDown` 事件直接透传，右键优先规则及现有原生菜单路径保持不变。

## 测试
- **增加原生事件回归覆盖**：GitHub macOS Job 验证 `MouseMoved + 左键按下`、右键、双键与旧版事件类型映射，防止仅修复菜单路径而遗漏自定义点击回调。

## 发布
- **本次发版版本号推进到 `v2.9.28`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.28` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.27

## 修复
- **修复 macOS artifact 临时上传失败**：GitHub Runner 遇到 DNS `ENOTFOUND` 时自动等待并重试 macOS 产物上传，避免已完成构建因单次网络波动中断 Release。

## 构建
- **增加 macOS 上传容错**：artifact 最多尝试上传 3 次，重试时启用同名产物覆盖，兼容首次请求已部分创建 artifact 的情况。

## 发布
- **本次发版版本号推进到 `v2.9.27`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.27` Tag 重新触发 GitHub 全平台自动打包。

# Code Switch v2.9.26

## 修复
- **修复 Linux Release 漏传 DEB/RPM**：按 Wails beta.6 实际生成的 `CodeSwitch.deb` 与 `CodeSwitch.rpm` 收集、校验并发布安装包，避免工作流成功但 Release 仅包含 AppImage。

## 构建
- **强化 Linux 产物校验**：DEB/RPM 生成后必须确认文件存在，并为两个安装包生成 SHA256；发布阶段移除静默忽略逻辑，缺少任一产物时直接失败。

## 发布
- **本次发版版本号推进到 `v2.9.26`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.26` Tag 重新触发 GitHub 全平台自动打包。

# Code Switch v2.9.25

## 修复
- **修复 macOS 27 托盘弹窗无法打开**：托盘窗口显示后增加短暂失焦保护，避免状态栏点击尚未结束时弹窗立即被隐藏，保护期结束后继续保持原有失焦关闭行为。
- **修复 macOS 27 托盘原生输入兼容**：保留 Wails `v3.0.0-beta.6`，在 macOS 构建时移植 Wails 官方 PR `#5760` 的托盘补丁，使用 `NSGestureRecognizer` 和 `performClick:` 替代 macOS 27 不再支持的旧输入路径。

## 兼容
- **保持旧版 macOS 行为**：macOS 26 及以下继续使用 Wails 原有 `NSEvent` 路径，不改变现有托盘交互。
- **保持 Linux GTK3 支持**：Linux 继续使用独立 GTK3/WebKit2GTK 4.1 构建与打包配置，不受 macOS 补丁影响。

## 构建
- **完善 macOS 自动打包**：Darwin 构建自动复制固定的 Wails beta.6、应用最小托盘补丁并通过临时 `modfile` 编译；GitHub Release 工作流增加补丁接入校验。

## 测试
- **补充托盘失焦回归测试**：覆盖打开保护期内忽略失焦、保护期结束后恢复失焦关闭；patched Wails 与应用根包均通过 macOS 27 编译检查。

## 发布
- **本次发版版本号推进到 `v2.9.25`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.25` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.24

## 修复
- **修复 Linux 自动打包安装 Wails 失败**：Linux Release job 安装 Wails CLI 时显式使用 `gtk3` build tag，避免 Wails beta.6 默认检查 GTK4 与 WebKitGTK 6.0，继续兼容现有 GTK3/WebKit2GTK 4.1 构建环境。

## 文档
- **补充 Linux GTK3 安装命令**：中英文开发指南与 Linux 构建设计统一使用 `go install -tags gtk3`，避免本地开发环境复现相同错误。

## 发布
- **本次发版版本号推进到 `v2.9.24`**：同步应用常量、全平台构建元数据和发布说明，并推送 `v2.9.24` Tag 重新触发 GitHub 全平台自动打包。

# Code Switch v2.9.23

## 修复
- **修复 macOS 27 托盘弹窗无法打开**：升级 Wails 后恢复状态栏托盘窗口的置顶与显示行为，保持现有点击打开、失焦关闭和主窗口生命周期逻辑不变。

## 兼容
- **保留 Linux GTK3 支持**：开发与生产构建继续使用 GTK3，DEB、RPM 与 AUR 改用独立 nFPM 配置并声明 GTK3/WebKit2GTK 4.1 运行时依赖。
- **同步 Wails 运行环境**：升级至 Wails `v3.0.0-beta.6`、Go 1.25，并将前端 runtime 锁定为 `3.0.0-beta.5`，同步重新生成前端绑定。

## 构建
- **完善 GitHub 自动打包**：Release 工作流同步 Go/Wails 版本，并在 Linux 打包前校验 GTK3 构建标签、独立 nFPM 配置和运行时依赖，继续生成 macOS、Windows、AppImage、DEB 与 RPM 产物。

## 测试
- **完成兼容性回归验证**：通过 60 个前端测试文件、456 项测试、TypeScript 类型检查、Go 编译检查、模型定价资源测试及 Linux GTK3 打包 dry-run。

## 发布
- **本次发版版本号推进到 `v2.9.23`**：同步应用常量、Wails 构建配置、全平台构建元数据和发布说明，并推送 `v2.9.23` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.22

## 新增
- **供应商今日性能趋势**：供应商数据概览新增首字延迟与生成速度双轴折线图，按本地自然日每 15 分钟展示均值，支持当前分桶、平滑曲线、同色渐变填充和无圆点显示。
- **独立性能样本口径**：首字延迟与生成速度分别统计成功流式请求中的有效样本，缺失区间保持为空并跨空连接，悬浮详情同时展示指标值与样本数。

## 修复
- **修复数据刷新后图表空白**：刷新供应商概览前主动销毁旧 ECharts 实例，避免额度周期刷新或供应商切换后复用已脱离 DOM 的实例。

## 优化
- **降低性能趋势查询开销**：SQLite 直接完成 15 分钟聚合，不再读取完整日志或执行无关定价解析；查询限制最多 100 个分桶，兼容夏令时自然日并防止异常范围占用大量内存。
- **完善异常与无障碍体验**：性能查询失败仅影响趋势面板，不阻断原有概览；为屏幕阅读器提供各有效时间点的指标值和样本数。
- **调整数据概览布局**：性能趋势独占统计卡片下方一行，原有 7 日金额趋势、7 日请求与 Tokens 面板整体下移，并保持亮暗主题适配。

## 测试
- **补充性能趋势回归覆盖**：覆盖 15 分钟分桶、当前不完整分桶、独立样本、旧日志名称回退、超长范围拒绝、前端映射与服务参数；已通过 60 个前端测试文件、455 项测试、Go 服务测试和 TypeScript 类型检查。

## 发布
- **本次发版版本号推进到 `v2.9.22`**：同步应用常量、Wails 构建配置、全平台构建元数据和发布说明，并推送 `v2.9.22` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.21

## 修复
- **修复托盘花费金额截断**：移除占宽较大的 `US$` 前缀，统一使用紧凑的 `$数字` 格式，并保留本地化千位分隔符和两位小数。

## 优化
- **优化托盘指标数字空间**：减少指标单元格横向内边距，为 Tokens 和花费分配更多列宽，并适当缩小花费字号，尽量完整显示较长数字。

## 测试
- **补充金额格式回归覆盖**：覆盖零金额、常规金额和五位数金额；通过 60 个测试文件、452 项测试和 TypeScript 类型检查。

## 发布
- **本次发版版本号推进到 `v2.9.21`**：同步应用常量、Wails 构建配置、全平台构建元数据和发布说明，并推送 `v2.9.21` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.20

## 修复
- **修复托盘内容遮挡**：macOS 托盘窗口按实际内容动态撑高，移除固定 `640px` 高度限制，供应商指标与额度内容不再被旧窗口高度裁剪。
- **修复超屏内容不可访问**：内容超过当前屏幕可用高度时保留触控板和滚轮滚动能力，同时隐藏滚动条，兼顾完整访问与简洁外观。
- **修复首次打开与跨屏切换闪烁**：托盘显示前先按目标屏幕收紧旧高度，激活后立即测量初始内容，不再等待网络刷新完成后才调整窗口。

## 优化
- **完善多屏窗口定位**：窗口尺寸变化后重新定位到菜单栏图标下方，重新打开时按当前显示器工作区重新计算高度。
- **减少隐藏状态无效调用**：托盘失焦后停止刷新生命周期，在途刷新结束时不再提交窗口尺寸更新。

## 测试
- **补充窗口高度边界回归覆盖**：覆盖内容高度、屏幕上限、最小高度、小屏幕和屏幕信息不可用回退场景；通过 60 个测试文件、451 项测试、Go 测试和 TypeScript 类型检查。

## 发布
- **本次发版版本号推进到 `v2.9.20`**：同步应用常量、Wails 构建配置、全平台构建元数据和发布说明，并推送 `v2.9.20` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.19

## 新增
- **macOS 托盘供应商指标**：在 Claude Code 和 Codex 当前供应商名称下展示成功率、请求数、Tokens、花费、首字延迟和生成速度。
- **统一指标口径**：成功率、请求数、Tokens 和花费使用今日统计，首字延迟与生成速度使用跨天最近 5 条成功流式请求。

## 修复
- **修复指标刷新失败后清零**：同一供应商查询短暂失败时保留上一次有效数据，切换供应商时使用占位值，避免闪烁、误导和串数据。

## 优化
- **降低托盘后台资源占用**：托盘隐藏或失焦后立即销毁刷新定时器和待执行任务，隐藏期间不查询数据，重新打开时立即刷新。
- **完善托盘主题与窄宽布局**：优化 360px 托盘中的四列统计与两列性能卡片，提高亮色主题小字号文本对比度，并补齐中英文文案。
- **复用供应商统计格式**：首页与托盘共用 Tokens、成功率和生成速度格式化逻辑，避免展示口径偏移。

## 测试
- **补充托盘回归覆盖**：覆盖供应商 ID 匹配、旧数据名称回退、无数据占位、刷新失败保留、供应商切换及打开/隐藏定时器生命周期；通过 60 个测试文件、451 项测试和 TypeScript 类型检查。

## 发布
- **本次发版版本号推进到 `v2.9.19`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.19` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.18

## 修复
- **并发上限 `0` 正确表示满载**：并发限制开启时拒绝该供应商的新连接并自动尝试下一供应商；已有连接继续自然完成，留空仍表示无限制。
- **修复复制供应商丢失并发上限**：Claude、Codex、自定义和 Gemini 供应商副本完整保留未配置、满载及有限并发状态，避免 `0` 被转换为无限制。

## 优化
- **强化实时并发状态辨识度**：首页供应商存在活跃连接时显示青色轻微辉光闪烁，达到有效上限时切换为琥珀色满载状态，空闲标签降低视觉权重。
- **完善主题与动态效果适配**：亮色、暗色主题保持一致状态层级，减少动态效果模式下停用闪烁并保留静态状态提示。
- **统一并发上限数据语义**：前后端、Wails 绑定及状态接口明确区分无限制、满载和有限上限，移除临时类型补丁。

## 测试
- **补充并发回归覆盖**：覆盖 `nil/0/1-999` 归一化、序列化、限流开关、实时标签状态及普通供应商与 Gemini 复制流程。

## 发布
- **本次发版版本号推进到 `v2.9.18`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.18` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.17

## 新增
- **供应商额度后台自动恢复**：应用进程存活时自动复查因额度耗尽停用或临时启用的供应商，额度恢复后自动启用，不再依赖主页及对应供应商标签页处于活动状态。
- **后台恢复设置**：新增独立恢复间隔和系统通知开关；默认每 60 秒检查一次，支持 10-3600 秒配置，恢复通知默认关闭。

## 修复
- **修复关闭页面后额度无法恢复**：后台协调器随应用启动并在完全退出时停止，托盘运行期间持续工作；查询失败保持原状态并在下一周期重试。

## 优化
- **降低后台恢复资源占用**：所有供应商串行查询并共用单协程、单计时器；没有待恢复供应商时不保留运行中的计时器。
- **合并额度恢复通知**：同一轮多个供应商恢复时只发送一条系统通知，前台或手动查询恢复不触发后台通知。

## 测试
- **补充调度器回归覆盖**：覆盖默认值与配置边界、首次完整间隔、间隔热更新、串行恢复、临时启用恢复、合并通知及等待状态快速停止，并通过竞态检测。

## 发布
- **本次发版版本号推进到 `v2.9.17`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.17` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.16

## 新增
- **供应商额度耗尽自动停用**：通用设置新增全局开关，默认关闭；开启后，已配置远端额度查询的供应商在额度耗尽时自动停用，额度恢复后自动启用。
- **自动停用状态交互**：供应商卡片以“额度用完”状态替代普通关闭开关，支持临时启用和恢复自动管理，并与手动关闭明确区分。

## 修复
- **修复官方零余额未触发停用**：兼容官方余额接口以 `Active=false` 表示零余额的返回方式，同时继续忽略认证失败等无效额度项。
- **保护手动关闭状态**：用户手动关闭的供应商不会因额度刷新自动开启；关闭总开关、移除额度配置或应用启动时会校正残留自动状态。
- **阻止自动停用供应商直接应用**：卡片直接应用和编辑弹窗“保存并应用”统一拦截，避免绕过停用状态。
- **修复 OpenCode 副本状态继承**：复制供应商时清除自动停用和暂停状态，防止额度刷新意外启用副本。

## 优化
- **降低额度状态刷新开销**：状态变化事件只更新目标平台的目标供应商卡片，不再重载全部平台配置。
- **完善跨平台状态同步**：Claude、Codex、Gemini、OpenCode 和自定义 CLI 共用一致的自动停用状态流转；OpenCode 同步维护 live 配置。
- **增强异常触发检查**：Claude、Codex 和 Gemini 上游请求失败时异步补查额度，同一供应商 30 秒内合并触发。

## 测试
- **补充完整状态流转覆盖**：覆盖零余额停用、额度恢复、手动关闭保护、临时启用、恢复自动、关闭总开关、启动校正、直接应用拦截和 OpenCode live 配置同步。

## 发布
- **本次发版版本号推进到 `v2.9.16`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.16` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.15

## 新增
- **macOS 主窗口 WebView 延迟释放**：关闭主窗口后按设置等待 `0-300` 秒再销毁 WebView，默认 30 秒；宽限期内重新打开会取消释放，超时后再次打开自动重建首页。
- **主窗口释放延迟设置**：通用设置新增 macOS 专属选项，支持 `0` 秒立即释放，Windows 和 Linux 继续保持原有隐藏窗口行为。

## 优化
- **降低后台驻留内存**：主窗口关闭并超过宽限期后释放对应 WebView，后台 relay、日志、健康监控、更新检查和托盘能力继续运行。
- **增强设置保存可靠性**：释放延迟使用专用即时保存接口，内存快照优先更新，并通过请求版本阻止快速输入时旧请求覆盖新值。
- **收紧窗口生命周期并发边界**：窗口创建与销毁状态分别串行化，不再持生命周期锁进入 Wails 原生窗口创建；代次校验避免旧定时器误销毁重开的窗口。

## 测试
- **补充窗口生命周期回归覆盖**：覆盖宽限期重开、旧定时器失效、关闭许可单次消费、应用退出取消和设置请求反序场景。

## 发布
- **本次发版版本号推进到 `v2.9.15`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.15` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.14

## 优化
- **精简供应商卡片**：移除首页卡片上的请求失败计数和巡检失败计数，减少常驻信息干扰。
- **补充成功率悬浮详情**：鼠标悬停成功率时显示请求失败、巡检失败及对应阈值，暂无统计时明确显示暂无数据。
- **优化拉黑状态交互**：活跃拉黑状态在花费指标后显示剩余倒计时，点击可查看触发来源、原因并执行解除拉黑或清零等级。
- **增强阈值加载可靠性**：阈值请求支持并发复用和 60 秒缓存，加载失败不伪造默认值，非法响应保留上次有效值并继续重试。
- **改善窄屏显示**：移动端拉黑详情浮层按指标区域定位，避免超出视口；中英文触发来源文案保持简洁一致。

## 测试
- **补充前端回归覆盖**：新增供应商卡片和黑名单状态组合测试，覆盖悬浮提示、阈值异常、拉黑详情操作及移动端状态展示。

## 发布
- **本次发版版本号推进到 `v2.9.14`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.14` Tag 触发 GitHub 全平台自动打包。

# Code Switch v2.9.13

## 新增
- **供应商日志红点独立开关**：Claude、Codex 和 Gemini 的供应商日志弹窗新增“日志红点”开关，每个供应商可独立控制首页日志图标是否显示未读红点。

## 优化
- **保持旧配置默认行为**：历史供应商配置和新建供应商默认开启红点，无需迁移即可继续显示未读失败提醒。
- **明确关闭后的日志交互**：关闭红点后仍保留日志采集和未读记录，日志图标单击或双击均只打开弹窗，不再触发快捷标记已读。
- **增强即时保存可靠性**：开关修改立即写入当前供应商配置，保存期间锁定操作，失败时自动恢复原状态。
- **完善跨平台持久化与测试**：打通 Claude、Codex、Gemini 配置映射，并补充默认值、保存、回滚和红点显示规则测试。

## 发布
- **本次发版版本号推进到 `v2.9.13`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.13` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.12

## 修复
- **修复完成响应被误记为失败**：收到 `response.completed` 或成功的 `response.done` 后，即使客户端随后关闭流，也保持供应商请求成功，不再被 `context canceled` 覆盖为 `499/502`。
- **修复客户端取消污染供应商成功率**：完成事件前的客户端取消、本地并发限制和代理内部错误从成功率分母排除，避免非供应商故障拉低成功率。
- **修复三态结果判定不一致**：非法、未知或带多余空格的结果值统一规范化并回退 HTTP 状态，确保汇总、失败列表、可用性与聚合统计一致。

## 优化
- **引入请求结果三态口径**：请求日志新增 `success`、`failure`、`excluded` 结果及原因，成功率统一按 `success / (success + failure)` 计算。
- **保留真实供应商失败**：上游非 2xx、网络错误、协议失败、缺少终止事件和 `unexpected EOF` 继续计入供应商失败。
- **完善统计展示**：日志页新增已排除请求数，首页成功率提示展示成功、失败和排除数量，无有效样本时显示 `—`。

## 发布
- **本次发版版本号推进到 `v2.9.12`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.12` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.11

## 修复
- **修复黑名单阈值误触发**：真实请求与后台健康巡检改为独立连续计数，手动健康检测不再影响拉黑计数。
- **修复并发计数丢失**：黑名单状态变更串行化，避免并发失败丢计数或成功清零覆盖最新状态。
- **修复拉黑详情被清空**：活跃拉黑期间的后续成功请求不再清除触发来源、计数和脱敏错误原因。

## 优化
- **增强 502 与断流诊断**：请求日志新增错误来源，区分供应商响应、上游网络、上游断流、代理内部与客户端取消。
- **完善供应商错误详情**：即使未采集完整返回体，502 仍保留最多 2KB 的脱敏错误摘要，供应商卡片展示拉黑来源与原因。
- **独立配置双阈值**：真实请求阈值仅统计网络错误、断流、`429` 和 `5xx`；后台巡检阈值可单独配置为 2-9 次。
- **降低后台写入开销**：健康检查成功仅在存在失败计数时写库，避免周期性无效刷新。

## 发布
- **本次发版版本号推进到 `v2.9.11`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.11` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.10

## 修复
- **修复客户端断开导致供应商误拉黑**：客户端取消和下游写入中断统一记录为 `499/client_abort`，立即停止转发且不累计供应商失败。
- **修复 Gemini 降级后丢失失败日志**：每次供应商尝试独立落库，后备供应商成功后仍可查看前一供应商的错误原因。
- **修复 Claude 兼容参数回退**：`previous_response_id`、`prompt_cache_key` 或可选参数被上游拒绝时，仅执行一次协议参数修正，不计入供应商失败。

## 优化
- **降低弱网环境误拉黑概率**：单次路由对每个供应商只尝试一次，失败后直接切换下一个供应商；新安装默认拉黑阈值由 3 调整为 5。
- **增强断流诊断与日志可读性**：请求日志新增脱敏的 `error_message`，项目日志与供应商日志直接展示错误原因和流状态。
- **完善敏感信息保护**：错误诊断补充 JSON、Bearer/Basic 认证、API Key 和纯文本密码脱敏，并限制最大存储长度。
- **完善回归测试**：覆盖断流分类、客户端取消、供应商切换、Gemini 逐尝试日志、Claude 协议回退和敏感信息脱敏。

## 发布
- **本次发版版本号推进到 `v2.9.10`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.10` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.09

## 修复
- **修复模型下拉被容器遮挡**：模型候选弹层提升到页面顶层，并根据视口空间自动向上或向下展开，避免在 Subagent 等靠近底部的设置中被裁剪。
- **修复大列表虚拟候选无法显示**：移除当前 Headless UI 版本中未生效的虚拟接口，改用稳定的自有虚拟窗口，候选与空状态恢复正常显示。
- **修复延迟搜索误选旧模型**：搜索结果随输入即时更新，键盘导航和鼠标选择始终基于当前查询结果。
- **禁用模型输入自动替换**：关闭系统拼写修正、自动替换和常见第三方写作建议，避免模型名称被意外修改。

## 优化
- **提升大量模型下拉性能**：仅渲染可视候选和少量缓冲项，复用预计算搜索索引，万级候选无需一次创建全部 DOM 节点。
- **稳定下拉尺寸与可读性**：候选充足时至少完整显示 8 行，空间不足时保留视口边距；长模型名使用单行省略并支持悬停查看完整内容。
- **完善交互回归测试**：覆盖下拉高度、视口翻转、虚拟窗口、键盘索引、搜索缓存和输入替换拦截。

## 发布
- **本次发版版本号推进到 `v2.9.09`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.09` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.08

## 新增
- **Claude Subagent 固定模型设置**：首页编辑 Claude 供应商时，可在模型映射列表末尾配置 Subagent 目标模型、思考强度和 1M 上下文声明。
- **默认兜底模型**：新增独立默认兜底设置，在精确映射、具体通配符和 Subagent 专用映射均未命中时使用，同样支持思考强度和 1M 声明。

## 修复
- **阻止内部 Subagent 别名错误透传**：`code-switch-r-subagent` 仅允许命中专用映射或默认兜底，不再被普通通配符或未命中透传发送到上游。
- **修复 Claude 代理状态迁移与中断恢复**：v2 到 v3 迁移保留原始 API Key，Subagent 模型注入和恢复支持中断后自动收敛，代理关闭时保留用户手动修改。
- **修复模型映射并发回滚**：规则保存失败时仅回滚当前开关，固定模型设置在保存期间锁定，避免覆盖其他表单修改。
- **修复并发保存反序协调**：连续保存 Claude 供应商时重新读取最新配置并串行协调，确保 `CLAUDE_CODE_SUBAGENT_MODEL` 与最终供应商配置一致。

## 优化
- **明确模型映射优先级**：保持精确映射和更具体通配符优先，Subagent 专用映射缺失时再使用默认兜底。
- **缩短供应商保存锁持有时间**：Claude 配置文件协调移出供应商主锁，降低文件读写对其他供应商操作的阻塞。
- **完善跨层回归测试**：覆盖前端固定映射状态、代理生命周期、状态迁移、内部别名拦截和并发保存收敛。

## 发布
- **本次发版版本号推进到 `v2.9.08`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.08` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.07

## 新增
- **Claude 供应商级认证字段**：供应商高级选项新增 `ANTHROPIC_AUTH_TOKEN`、`ANTHROPIC_API_KEY` 和自定义 Header，默认使用 `ANTHROPIC_AUTH_TOKEN`。
- **认证选择贯通完整链路**：供应商认证字段统一控制 Claude 直连配置、托管代理转发、可用性探测、模型与价格获取及 CLI 配置预览。

## 修复
- **修复 Claude 认证字段互斥与协议头行为**：切换标准认证字段时自动移除另一字段，原生 Anthropic 请求独立补充 `anthropic-version`，避免健康探测误判和自动拉黑。
- **修复认证字段进入 CLI 可编辑配置**：两个标准认证字段保持系统只读，不再随其他 CLI 配置写入供应商数据。
- **修复保存并应用状态不同步**：自定义 Header 配置实时禁用直连，切回标准认证字段后恢复直接应用。
- **修复模型候选点击选择**：候选项按下时保留明确选择意图，避免失焦先清理状态导致点击候选未生效。

## 优化
- **统一认证 Header 构造**：健康探测、代理转发和模型价格请求复用一致的认证解析逻辑，自定义 Header 仅允许托管路由并校验名称。
- **增强配置导入兼容**：cc-switch 与深链导入同时识别两种 Claude 标准认证字段，并在两者并存时优先 `ANTHROPIC_AUTH_TOKEN`。
- **明确本地代理设置语义**：全局“Claude 代理认证”更名为“本地代理认证字段”，与供应商级认证选择区分。

## 发布
- **本次发版版本号推进到 `v2.9.07`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.07` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.06

## 修复
- **修复搜索下拉自动覆盖输入值**：输入 `k3` 等自定义模型时，即使匹配到 `kimi-k3` 等候选，Tab、失焦或直接按 Enter 也会保留输入值；仅鼠标点击候选或键盘明确导航后按 Enter 才会写入候选。
- **修复候选导航状态残留**：失焦、完成选择或输入内容变化后完整清理选择意图，避免后续 Enter 无响应或错误选中候选。
- **修复输入法 Enter 误触发操作**：输入法合成阶段按 Enter 不再切换焦点、提交模型映射或应用 CLI 配置。

## 优化
- **统一全局搜索下拉交互**：模型映射、思考强度、CLI 模型和模型定价模板使用一致的显式选择规则，并支持 Home、End、PageUp、PageDown 候选导航。
- **完善连续交互回归测试**：覆盖直接 Enter、方向键选择、Tab、失焦、鼠标选择、输入法合成和完整键盘导航。

## 发布
- **本次发版版本号推进到 `v2.9.06`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.06` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.05

## 新增
- **Claude 模型映射支持声明 1M**：首页新增或编辑 Claude 供应商时，可为原生 Anthropic 格式的每条模型映射勾选“声明支持 1M”，已声明规则显示 `1M` 状态徽标。
- **命中映射后自动启用 1M beta**：精确或通配符映射命中后，向原生 Anthropic 请求合并 `context-1m-2025-08-07`，保留并去重已有 `anthropic-beta`；OpenAI Chat/Responses 格式不注入。

## 修复
- **修复隐藏 1M 草稿误写入**：切换到 OpenAI 格式后，新映射不会继承已隐藏的未保存勾选状态；编辑已有映射时继续保留原声明。

## 优化
- **完善 1M 配置生命周期**：支持保存回显、规则重命名、删除、供应商复制、配置归一化和快照深拷贝，失效映射不会残留声明状态。
- **补充跨层回归测试**：覆盖前端状态维护、供应商保存读取、精确与通配符命中、请求头合并去重及协议隔离。

## 发布
- **本次发版版本号推进到 `v2.9.05`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.05` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.04

## 修复
- **优化供应商状态切换排序**：已有供应商关闭后移动到未启用组首位，重新启用后移动到启用组首位；连续关闭时最近关闭的供应商优先显示，便于快速找回。
- **修复编辑失败后的排序回滚**：编辑弹窗切换启用状态后若保存失败，完整恢复供应商列表、排序字段和弹窗卡片引用，不再因置顶规则改变原顺序。

## 优化
- **保持新增与手动排序规则稳定**：新增已启用供应商继续放在启用组末尾，新增未启用供应商继续放在未启用组首位，手动拖拽及持久化行为不变。
- **完善排序回归测试**：覆盖启用与关闭置顶、连续关闭、保存失败完整回滚及弹窗引用恢复。

## 发布
- **本次发版版本号推进到 `v2.9.04`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.04` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.03

## 新增
- **日志页支持多来源用量统计**：新增“代理 / 会话 / 全部”切换，统一作用于请求明细、汇总、趋势、Provider、模型统计和存储热力图，并记忆上次选择。
- **本地会话用量自动同步**：支持 Claude、Codex 和 Gemini CLI 会话日志，启动时同步一次，之后每分钟检查变化；Windows 可手动扫描 WSL 发行版。
- **跨来源持久化去重**：“全部”模式对代理与会话记录进行一对一匹配，避免相同请求重复计算；清理日志时可选择重新导入会话历史。

## 修复
- **修复会话快照统计不完整**：Claude 最终快照可跨同步周期更新，Gemini 零 Token 占位消息补全后可正常导入。
- **修复 Gemini 去重口径不一致**：输出 Token 与思考 Token 分开存储，与代理日志保持相同匹配口径。
- **修复来源切换数据不一致**：无效日期草稿下仍使用最后一次有效范围刷新，费用详情、热力图和当天日志始终使用已应用来源。

## 优化
- **避免重复解析和计价**：未变化文件通过指纹直接跳过，Gemini 仅对新增或发生变化的消息重新计价，Codex 文件归档后继承原同步游标。
- **保持既有统计稳定**：预算、托盘、配额、健康统计及历史聚合继续使用代理日志口径，不受会话数据影响。
- **补充跨层回归测试**：覆盖会话更新、归档游标、来源去重、筛选回退、热力图来源和 Wails 参数传递。

## 发布
- **本次发版版本号推进到 `v2.9.03`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.03` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.02

## 新增
- **日志页新增多维用量统计**：保留原有汇总卡片、模型占比和趋势图，新增请求明细、Provider 统计、模型统计三个数据视图，并支持按核心指标排序。
- **新增全局计价模型筛选**：明细、汇总、趋势、Provider 与模型统计统一按有效计价模型筛选，请求明细同时展示计价模型和路由模型链路。
- **自动刷新档位可配置**：支持关闭、5 秒、10 秒、30 秒和 60 秒，默认 30 秒并持久化；日志数据视图会记住上次选择。

## 修复
- **统一计价模型筛选口径**：数据库筛选、统计分组和请求主展示共同使用存储有效计价模型，避免历史日志动态补价后出现模型可见但筛选无结果。
- **修复自动刷新请求叠加**：刷新任务改为单任务执行，统计加载期间不会继续叠加整页查询。
- **修复统计加载状态提前结束**：加载状态覆盖整批仪表盘和分页请求，避免 Provider 或模型统计尚未完成时显示旧结果。
- **修复筛选草稿被刷新覆盖**：切换 Provider 后即时级联模型选项，后台刷新不会覆盖用户正在编辑的筛选范围。

## 优化
- **降低日志聚合开销**：Provider 统计仅在对应数据视图激活时加载，减少请求明细和模型统计场景下的无用扫描。
- **提高小额成本可读性**：模型单次均价固定显示六位小数，保留低成本请求的有效精度。
- **补充跨层回归测试**：覆盖动态补价、空白模型、筛选联动、长任务刷新、异步加载、设置持久化与 Provider 按需加载。

## 发布
- **本次发版版本号推进到 `v2.9.02`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.02` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.01

## 新增
- **Claude 代理认证字段可选**：通用设置支持在 `ANTHROPIC_AUTH_TOKEN` 与 `ANTHROPIC_API_KEY` 间切换，默认保持 `AUTH_TOKEN`，保存后立即应用到当前代理接管配置。

## 修复
- **避免 Claude 双认证字段警告**：代理接管、CLI 配置预览和 WSL 自动配置只写入一个认证字段，减少 `Both ANTHROPIC_AUTH_TOKEN and ANTHROPIC_API_KEY set` 警告。
- **修复客户端认证信息透传**：Claude 请求转发前清理客户端 `Authorization`、`x-api-key`、`x-goog-api-key` 及供应商自定义认证头，再注入当前供应商真实凭证。
- **修复代理关闭恢复不完整**：代理状态升级为 v2，完整保存并恢复启用前的 Token、API Key 和 Base URL，同时兼容旧状态迁移。
- **修复无状态启动协调与并发覆盖**：缺少有效代理状态时不再创建错误恢复基线；代理启用、关闭、认证切换和直连应用统一串行化。
- **保持 Claude 直连配置兼容**：认证字段单选仅作用于代理模式，直连预览、保存和高级编辑继续保留原有 `ANTHROPIC_API_KEY` 行为。

## 优化
- **统一 Claude 认证配置链路**：本机代理、CLI 高级编辑、启动修复与 WSL 自动配置共用同一认证字段设置，外部 `--settings` 文件保持不变。
- **补充认证链路回归测试**：覆盖单字段接管、原样恢复、v1 状态迁移、无状态关闭、直连兼容、上游认证头替换及竞态检测。

## 发布
- **本次发版版本号推进到 `v2.9.01`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.01` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.9.00

## 变更
- **调整模型映射位置**：首页编辑供应商时，模型映射移动到预算额度正上方，模型白名单位置及原有显隐、交互和保存逻辑保持不变。

## 优化
- **清理临时测试程序**：删除根目录 9 个历史 `test_*.exe` 构建产物，并增加忽略规则，避免临时测试程序再次进入版本控制。

## 发布
- **本次发版版本号推进到 `v2.9.00`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.9.00` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.8.99

## 新增
- **模型映射支持独立启停**：首页编辑供应商时，每条模型映射可单独开启或关闭，并显示当前启用数量；已保存规则切换后立即持久化。
- **供应商模型支持搜索下拉**：模型映射目标支持自由输入、下拉选择和模糊筛选，候选覆盖内置及同步模型。

## 修复
- **修复关闭规则仍参与路由**：关闭的映射不再参与精确匹配、通配符匹配、Claude 路由、模型聚合和配置校验；关闭高优先级规则后继续匹配其他开启规则。
- **修复全部映射关闭后透传失效**：Claude 路由正确执行全局未命中策略及透传模式筛选，不再提前移除可用供应商。
- **修复开关即时保存竞态**：保存期间锁定映射开关及弹窗操作，失败自动回滚；过期请求不会覆盖其他供应商的表单状态。
- **修复下拉键盘选择冲突**：Enter 可正确区分高亮候选选择与自定义模型提交。
- **修复连接详情主题反转**：亮色主题使用浅色悬浮窗，暗色主题使用深色悬浮窗。

## 优化
- **完善映射状态维护**：重命名规则时迁移关闭状态，删除规则时清理关联状态，历史配置默认全部开启。
- **补充跨层回归测试**：覆盖映射匹配回退、Claude 动态透传、即时保存成功与失败、并发锁、过期请求隔离和模型候选筛选。

## 发布
- **本次发版版本号推进到 `v2.8.99`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.99` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.8.98

## 新增
- **连接详情展示模型参数**：首页连接详情在模型名称外新增思考强度和最大输出，便于快速确认请求的实际上游参数。
- **区分请求值与实际值**：Claude 模型路由开启时，请求模型和实际模型分别显示对应参数，并可查看原请求、请求体覆盖或模型映射来源。

## 修复
- **修复最大输出别名冲突**：请求体同时存在 `max_tokens`、`max_output_tokens` 或 `max_completion_tokens` 时，按最终上游协议选择真正生效的字段。
- **修复兼容重试参数状态滞后**：上游拒绝可选参数并触发重试时，活跃连接同步刷新最终思考强度与最大输出。

## 优化
- **完善参数展示与交互**：参数标签支持亮色、暗色和窄窗口布局，鼠标悬停或键盘聚焦可查看完整值、来源与模型路由详情。
- **统一连接详情数据流**：活跃与历史连接共用模型行生成逻辑，保持单模型、双模型和无障碍文案一致。
- **补充跨协议回归测试**：覆盖 Anthropic、OpenAI Chat Completions、OpenAI Responses、Gemini、兼容重试与前端模型展示。

## 发布
- **本次发版版本号推进到 `v2.8.98`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.98` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.8.97

## 新增
- **模型映射支持强制思考强度**：首页编辑供应商时，可为每条模型映射单独设置 `low`、`medium`、`high`、`xhigh`、`max` 或自定义强度；留空时继续透传请求值。
- **请求日志记录强度来源**：日志详情区分请求原值、供应商请求体覆盖和模型映射强制值，便于确认最终上游参数。

## 修复
- **修复不同 API 格式的思考字段转换**：Anthropic、OpenAI Chat Completions 和 OpenAI Responses 分别写入 `output_config.effort`、`reasoning_effort` 和 `reasoning.effort`，OpenAI 格式下自动将 `max` 转换为 `xhigh`。
- **修复未开启思考时误注入强度**：只有请求已开启思考或显式携带强度字段时才应用模型映射强度，不会仅因配置强度而强制开启思考。
- **修复兼容重试与能力记忆重复发送字段**：上游拒绝可选思考字段后，重试和后续请求会同步清理字段并刷新最终日志，避免重复触发兼容性错误。
- **修复请求体覆盖来源误判**：按最终生效控制字段识别来源，预算覆盖会正确记录，低优先级字段不会覆盖实际来源。
- **修复自定义强度边界**：`minimal` 等有效自定义值可正常覆盖和记录，超长标签不会挤压映射列表操作区。

## 优化
- **统一请求思考元数据**：请求计划集中记录最终强度、来源、目标字段和模型映射实际应用状态，保证转发、重试和日志一致。
- **完善映射编辑状态**：重命名、删除映射或清空强度时同步维护关联配置，避免残留无效强度。
- **补充跨层回归测试**：覆盖三种 API 格式、能力记忆、兼容重试、预算覆盖、Gemini 日志来源和前端映射状态操作。

## 发布
- **本次发版版本号推进到 `v2.8.97`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.97` Tag 触发 GitHub 全平台自动打包。

---

# Code Switch v2.8.96

## 新增
- **连接详情展示模型路由快照**：Claude 模型路由开启时，活跃连接与历史连接展示请求模型、实际模型、映射规则、映射结果和请求体覆盖过程。
- **Codex 流诊断 Tooltip**：日志页 HTTP 状态支持查看最后事件、终止事件、压缩请求与返回状态、传输字节和上游 HTTP 协议。

## 修复
- **修复额度预设删除无响应**：删除与放弃修改改用应用内确认弹窗，不再依赖桌面 WebView 可能不显示的浏览器原生 `confirm`。
- **修复 Claude Responses 子代理续链串扰**：主代理与不同子代理使用隔离的续链上下文；缺少稳定身份时回放完整上下文，避免错误共享 `previous_response_id`。
- **修复 Codex 压缩流零字节断开无法降级**：上游返回 `200` 但未输出任何流内容时，在提交响应头前切换下一可用供应商。
- **修复不完整流误判成功**：缺少 `response.completed` 时记录协议错误；已经输出内容后禁止拼接重试，且不会伪造完成事件。

## 优化
- **完善请求级诊断快照**：请求日志新增模型路由和流生命周期字段，旧数据库自动迁移，历史记录不使用当前配置反推。
- **优化确认与 Tooltip 交互**：删除操作按捕获的预设 ID 执行并阻止重复提交；路由与流诊断支持键盘聚焦、ESC 关闭及视口自适应定位。
- **补充跨层回归测试**：覆盖模型路由落库读回、子代理隔离、预设确认、压缩流诊断、零字节故障转移与竞态检查。

## 发布
- **本次发版版本号推进到 `v2.8.96`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.96` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.95

## 变更
- **代理转发链路低延迟优化**：供应商、应用设置和黑名单路由状态改用内存快照，请求计划按实际尝试供应商懒生成，减少磁盘读取和大请求体重复处理。
- **请求详情新增转发性能分解**：展示代理准备、DNS、TCP、TLS、上游首字节、转发等待和连接复用状态，便于定位延迟来源。

## 修复
- **修复流式首行转发阻塞**：移除固定 1024 字节预读限制，首个完整 SSE 行到达后立即转发并 Flush，同时保留单行 JSON 回退和响应 Hook。
- **修复客户端断开后上游请求继续运行**：将下游请求 Context 传递到上游，客户端取消不再计入供应商失败或黑名单。
- **修复快照可变字段共享竞态**：应用设置和供应商配置在存取快照时深拷贝 `map`、切片和指针字段，避免未保存修改污染运行态或触发并发崩溃。
- **修复黑名单异常扫描发布残缺状态**：仅在完整扫描成功后更新运行快照，读取异常时继续使用上一份有效状态。
- **修复兼容性重试性能统计失真**：每次尝试独立计时并累计网络阶段耗时，首写时间不再包含客户端写阻塞。

## 优化
- **复用上游 HTTP 连接**：共享 Transport 连接池，启用 HTTP/2 并提升单主机空闲连接容量，降低重复 DNS、TCP 和 TLS 建连成本。
- **降低请求日志收尾等待**：日志批量提交窗口由 100ms 缩短至 10ms，请求体仅在实际启用日志载荷时延迟物化。
- **降低后台数据库轮询压力**：黑名单本地写入与设置保存后即时刷新，完整校准降为每分钟一次；后台 watcher 由应用生命周期统一启动和停止。
- **完善沙箱回归测试**：覆盖快照隔离、黑名单异常回退、连接复用、流式首行、取消传播、兼容重试计时和竞态检查。

## 发布
- **本次发版版本号推进到 `v2.8.95`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.95` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.94

## 变更
- **Claude 显式模型映射改为最高优先级**：开启按模型路由后，只要请求模型命中 `modelMapping` 左侧规则，即允许供应商参与路由，不再要求映射目标存在于 `supportedModels` 或供应商模型接口列表。
- **支持模型库外请求动态匹配**：`claude-opus-*` 可直接匹配尚未进入本地模型库的 `claude-opus-4.8` 等请求模型，并按映射结果转发。

## 修复
- **修复映射目标未被供应商模型接口返回时请求被提前拒绝**：显式配置的映射目标和请求体模型覆盖不再被本地真实模型白名单拦截，实际不支持时保留上游原始错误和现有失败切换流程。
- **修复聚合索引限制真实请求路由**：模型聚合列表继续只展示模型库可展开的具体模型，但不再限制可动态匹配的实际请求模型。

## 优化
- **保持路由边界清晰**：未命中映射的模型继续按 `supportedModels` 和原样放过规则严格校验，Codex 与自定义 CLI 保持原有行为。
- **完善缺失元数据回退**：映射目标没有远端元数据时，优先使用请求模型的本地信息，再回退到稳定默认值。
- **补充端到端回归测试**：覆盖配置保存、通配符动态选择、请求体模型改写、上游实际转发和工具间行为隔离。

## 发布
- **本次发版版本号推进到 `v2.8.94`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.94` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.93

## 新增
- **Claude 多供应商模型路由**：可按 Claude Code 请求模型、供应商映射和实际模型列表选择兼容供应商，计划与执行请求可分别使用不同模型。
- **Claude 模型聚合列表**：可选聚合全部已启用供应商的 `/v1/models`，返回去重、稳定排序和分页后的 Anthropic 格式模型列表。
- **模型放过规则**：供应商支持精确、前缀和关键字通配规则，映射未命中时仅放过已验证支持的模型。

## 变更
- **新增独立功能开关**：按模型路由和模型聚合默认关闭；旧配置存在 `supportedModels` 或 `modelMapping` 时自动开启路由以保持兼容。
- **模型元数据支持合并策略**：可在激进与保守策略间切换 Token 上限和能力合并方式。
- **供应商模型缓存持久化**：启动和供应商启用后异步刷新，缓存有效期 24 小时，失败后按 30 分钟间隔重试。

## 修复
- **修复供应商连续保存导致路由回退**：保存返回前同步重建路由，旧配置的迟到刷新结果不再覆盖新配置。
- **修复连接变更后误用旧模型缓存**：配置指纹包含 API Key 摘要，新连接刷新失败时不会复用旧连接模型列表。
- **修复重叠通配符映射不确定**：路由索引和真实转发统一使用确定性映射优先级。
- **修复空放过规则升级不兼容**：空规则默认覆盖全部已由远端列表、白名单或映射证明支持的模型，未知模型仍直接拒绝。

## 优化
- **批量刷新减少重复工作**：每批供应商刷新只持久化一次缓存并重建一次路由。
- **模型库变更即时生效**：内置、手动新增、Claude 同步和云端同步模型变化后立即更新路由索引。
- **手动刷新入口保持可用**：开启模型路由后无需同时开启模型聚合即可刷新供应商模型。
- **补充并发与回归测试**：覆盖缓存代际、连接失败、保存即时生效、通配符优先级和放过规则边界。

## 发布
- **本次发版版本号推进到 `v2.8.93`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.93` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.92

## 变更
- **首字耗时支持自适应单位**：日志性能列和首页供应商卡片统一显示毫秒、秒、分钟或小时，长耗时更易阅读。

## 修复
- **修复秒级半值舍入偏差**：基于整数毫秒计算秒级精度，避免 `.005`、`.05` 等半值因浮点误差向下舍入。

## 优化
- **统一首字耗时格式化逻辑**：日志与首页复用同一工具，无效值、零值和非流式日志继续显示 `—`。
- **补充格式化回归测试**：覆盖毫秒、秒、分钟、小时、单位进位、半值舍入及首页统计展示流程。

## 发布
- **本次发版版本号推进到 `v2.8.92`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.92` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.91

## 变更
- **补全 Claude API 格式兼容**：完善 OpenAI Chat Completions、Responses 与 SSE 的推理内容、图片、工具结果、Web Search 和缓存 usage 转换。
- **调整供应商性能统计口径**：首页跨天选取最近 5 条成功流式请求计算首字与速度，日志速度统一使用输出 Token 除以总耗时。

## 修复
- **修复可选参数兼容重试误删字段**：仅删除上游明确拒绝的安全白名单参数，并与 `prompt_cache_key`、`previous_response_id` 共用一次重试预算。
- **修复强制 Web Search 工具选择**：Responses 使用原生 Web Search 工具选择，Chat 路径不再发送不存在的函数选择。
- **修复浏览器复制假成功**：浏览器开发环境跳过不可用的 Wails Clipboard 调用，可靠降级到标准 Clipboard API。
- **修复流式首字统计偏差**：按协议识别首个有效输出，排除前置、usage-only 和终态事件。

## 优化
- **优化供应商能力记忆**：按规范化 API URL、最终端点和格式记忆不支持字段，并增加 30 分钟有效期与容量限制。
- **保留 Web Search 引用**：非流式 Responses 转换会保留并去重 URL 引用结果。
- **补充回归测试**：覆盖 Claude 格式转换、兼容重试、首字统计、供应商性能聚合和剪贴板降级。

## 发布
- **本次发版版本号推进到 `v2.8.91`**：同步应用常量、Wails 构建配置、平台构建元数据和发布说明，并推送 `v2.8.91` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.90

## 修复
- **修复复制供应商后改名保存失败**：复制供应商后修改名称并保存时，旧统计表或历史数据异常不再阻断供应商配置保存。
- **修复额度查询配置弹窗底部操作不可见**：首页编辑供应商的“额度查询配置”弹窗内容区改为独立滚动，取消、测试查询、保存按钮固定显示在弹窗底部。

## 优化
- **优化供应商改名关联数据同步**：核心保存流程优先落盘，日志、黑名单、健康检查和额度周期状态按可用数据同步，异常记录日志后跳过。
- **优化供应商保存失败提示**：前端保存失败时显示后端返回的具体错误详情，便于定位配置问题。

## 发布
- **本次发版版本号推进到 `v2.8.90`**：同步更新应用常量、Wails 构建配置、平台构建元数据和发布说明，并准备推送 `v2.8.90` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.89

## 变更
- **日志生成可用性改为按错误占比分色**：可用性页面的“日志生成”模式不再因时间桶内出现单个失败请求就直接标红，而是按错误占比显示绿色、黄色和红色。
- **高延迟与错误占比分开展示**：日志生成模式的颜色只表达错误占比，高延迟请求保留在详情提示中展示，避免与错误状态混淆。

## 修复
- **修复日志可用性少量错误导致平均延迟失真**：黄色时间桶继续参与平均延迟计算，避免平均延迟被错误排除为 `0`。

## 优化
- **补充日志聚合详情**：时间桶提示中新增错误率、失败请求数和慢请求数，便于判断供应商可用性波动来源。
- **补充错误占比分色回归测试**：覆盖绿色、黄色、红色阈值和高延迟独立展示逻辑。

## 发布
- **本次发版版本号推进到 `v2.8.89`**：更新发布说明，并准备推送 `v2.8.89` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.88

## 新增
- **配置查询支持多预设管理**：首页编辑供应商的“配置查询”中，`自定义`、`通用模版`、`NewApi` 三类脚本预设支持新增、命名、保存、删除、重命名与设为默认。

## 变更
- **载入预设改为默认预设优先**：点击“载入预设”会直接载入当前模版的默认预设；未设置默认预设时回退内置默认脚本。
- **旧版单条预设自动迁移**：历史 `provider_quota_query_preset_codes` 会自动迁移为命名预设“自定义预设”，保留既有用户脚本。

## 修复
- **修复默认预设删除后的空默认状态**：删除默认预设后会自动选择剩余第一条预设作为默认。
- **修复预设编辑脏状态误报**：保存后同步修剪后的名称与脚本内容，避免保存完成后仍提示未保存。

## 优化
- **补充预设编辑保护**：新增、切换、关闭前会提示未保存修改，删除预设前会二次确认。
- **打开编辑弹窗时刷新最新设置**：避免多个入口或设置页保存后使用旧预设数据。

## 发布
- **本次发版版本号推进到 `v2.8.88`**：同步更新应用常量、Wails 构建配置、平台构建元数据和发布说明，并准备推送 `v2.8.88` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.87

## 新增
- **配置查询支持编辑脚本预设**：首页编辑供应商的“配置查询”中，`自定义`、`通用模版`、`NewApi` 三类查询脚本可编辑全局预设，并支持一键还原默认。

## 修复
- **修复编辑预设格式化按钮无法点击**：调整代码编辑器底部按钮层级，避免按钮被编辑区域覆盖。
- **修复模型定价同步菜单按钮布局**：同步菜单按钮改为居中排列，避免全局按钮样式导致位置偏移。

## 优化
- **补充预设脚本保存校验**：保存查询脚本预设前校验脚本语法、`request`、`request.url`、`request.method` 和 `extractor`，不发起真实请求。

## 发布
- **本次发版版本号推进到 `v2.8.87`**：同步更新应用常量、Wails 构建配置、平台构建元数据和发布说明，并准备推送 `v2.8.87` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.86

## 修复
- **修复 Codex OAuth 验证页无法打开**：认证页不再依赖 WebView 的普通外链跳转，改用 Wails `Browser.OpenURL` 调起系统浏览器，避免点击“打开验证页”没有反应。

## 优化
- **补充 Codex OAuth 手动复制回填**：设备码登录卡片新增“复制链接”和“粘贴完成”，可复制验证页 URL，也可从剪贴板读取认证后的 URL 后立即触发登录完成检查。

## 发布
- **本次发版版本号推进到 `v2.8.86`**：同步更新应用常量、Wails 构建配置、平台构建元数据和发布说明，并准备推送 `v2.8.86` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.85

## 新增
- **独立认证菜单**：侧边栏新增“认证”入口，放置在“技能”下方，统一管理 ChatGPT(Codex OAuth) 官方认证。

## 变更
- **Codex OAuth 迁移到认证页**：通用设置不再承载官方认证登录卡片，保留官方登录与统一历史开关继续留在通用设置中。

## 修复
- **修复登录流程切页中断**：设备码授权轮询不会因离开认证页被清理，避免返回页面后登录状态丢失。
- **修复认证页重复加载**：首次进入认证页不再重复请求账号与状态数据。

## 发布
- **本次发版版本号推进到 `v2.8.85`**：同步更新应用常量、Wails 构建配置和发布说明，并准备推送 `v2.8.85` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.84

## 新增
- **ChatGPT(Codex OAuth) 认证**：设置页新增 Codex 官方 OAuth 登录入口，支持设备码登录、多账号列表、默认账号切换、移除与退出。
- **Codex OAuth 托管转发**：Codex 代理可使用 ChatGPT OAuth access token 调用官方 Codex 后端，并注入官方会话所需请求头。

## 变更
- **移除旧官方登录卡片**：不再注入不可删除、不可关闭的“Codex 官方登录”内置供应商卡片，历史残留卡片会被过滤。
- **默认账号影响路由**：切换默认 ChatGPT 账号时，仅启用对应 OAuth provider，避免继续误走旧账号。

## 修复
- **修复官方登录抢占路由**：OAuth provider 不再因登录新账号自动抢占第三方 Codex provider。
- **修复退出后重新登录问题**：退出登录会清理设备码轮询和后端 pending 状态，避免授权回调重新创建账号。
- **修复账号移除后卡片状态滞后**：移除账号或退出登录后会刷新供应商列表。

## 优化
- **托管认证表单精简**：Codex OAuth provider 隐藏 API Key，并锁定官方后端地址，避免误编辑导致不可用。
- **补充 Codex 模型路由**：新增 `/models` 路由用于 Codex 官方 OAuth 模型列表转发。
- **补充回归测试**：覆盖 OAuth provider 创建、默认账号选择和旧 OAuth provider 禁用。

## 发布
- **本次发版版本号推进到 `v2.8.84`**：同步更新应用常量、Wails 构建配置和发布说明，并准备推送 `v2.8.84` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.83

## 新增
- **Codex 官方登录保留**：使用第三方 Codex API 时可保留官方登录能力，继续使用官方插件、手机远程操作和统一会话历史。
- **Codex 统一会话历史**：官方订阅可使用 `code-switch-r` 供应商标识运行，官方与第三方会话显示在同一历史列表。
- **官方会话迁移与恢复**：开启统一历史时可迁入现有官方会话，迁移前自动备份；关闭后可按备份恢复。
- **Codex 官方登录卡片**：新增内置“Codex 官方登录”供应商卡片，支持一键切回官方登录。

## 修复
- **避免覆盖官方认证**：保留官方登录时不再向 `auth.json` 写入第三方 API Key，并会安全清理旧的 `OPENAI_API_KEY`。
- **修复官方/第三方配置串扰**：统一历史下不会把第三方配置误重写为官方配置，切回官方登录时会清理第三方 `openai` 覆盖。
- **修复内置卡片持久化问题**：Codex provider 文件缺失时继续走默认预设初始化；内置官方卡片仅运行时注入，不写回磁盘。
- **修复设置与实际配置不一致**：Codex 配置重写失败时会回滚应用设置，避免开关状态与真实配置偏离。

## 优化
- **仅迁移官方历史**：统一历史迁移只处理现有官方会话，避免破坏第三方旧会话的原 provider 分桶。
- **过滤内置官方卡片**：代理转发、健康检查、连通性测试等运行时路径会跳过内置官方卡片，不影响自定义 official 分类供应商。
- **补充回归测试**：覆盖官方登录、保留认证、统一历史迁移/恢复、内置卡片注入与设置回滚等关键边界。

## 发布
- **本次发版版本号推进到 `v2.8.83`**：同步更新应用常量、Wails 构建配置和发布说明，并准备推送 `v2.8.83` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.82

## 优化
- **连接详情改为 Tab 切换**：首页供应商并发标签打开的连接详情弹窗中，当前活跃连接与最近连接历史分离展示，通过“活跃 / 历史”切换查看。
- **历史记录按需加载**：弹窗默认停留在活跃连接，仅切换到历史时加载最近连接历史，减少无效日志请求。
- **历史数量展示更准确**：历史记录未加载前显示占位，加载中显示进度状态，避免误显示为 `0`。

## 修复
- **修复连接详情可访问性标记不完整**：移除未完整实现的 Tab ARIA 角色，改为普通分段按钮状态，避免辅助技术误判交互结构。

## 发布
- **本次发版版本号推进到 `v2.8.82`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.82` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.81

## 新增
- **首页并发连接详情**：点击供应商卡片的并发标签可查看当前活跃连接与最近连接历史，包含 `User-Agent`、模型、端点、流式状态和耗时信息。
- **日志红点快捷处理**：首页供应商日志按钮支持双击标记未读失败日志为已读，单击仍打开日志弹窗。

## 优化
- **并发状态快照更一致**：并发数量与连接详情改为同锁读取，避免高并发刷新时出现数量和详情短暂不一致。
- **日志按钮交互更清晰**：有未读失败时 tooltip 提示“单击查看日志，双击标记已读”，并增加重复双击防重入保护。
- **首页代理开关显示优化**：代理托管与并发限制开关视觉样式保持一致，状态入口更清晰。

## 修复
- **修复无红点双击日志按钮无响应**：无未读失败时双击日志按钮会正常打开日志弹窗。
- **修复标记已读后红点可能残留**：快捷标记完成后无论实际标记数量是否为 0，都会刷新供应商统计。

## 发布
- **本次发版版本号推进到 `v2.8.81`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.81` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.80

## 变更
- **并发限制改为显式开关**：供应商配置中的并发上限不再自动启用限流，需在首页对应平台手动开启“并发限制”后才按上限拦截。
- **并发占用持续可见**：未开启并发限制时仍显示实时并发 `x/x` 或 `x/∞`，便于观察供应商压力。

## 优化
- **首页并发限制入口更清晰**：代理开关旁新增“并发限制”开关标签与状态提示，Tab 紫色点表示当前平台已开启限流。
- **减少设置读取开销**：单次代理请求只读取一次并发限制开关状态，再复用于该请求内的降级/重试流程。

---

# Code Switch v2.8.79

## 优化
- **首页供应商 Tab 状态更直观**：Claude Code、Codex、Gemini 与自定义 CLI 的 Tab 增加双状态微章，绿色表示代理托管已开启，紫色表示当前托管供应商已配置实时并发上限。
- **状态微章适配托管路由**：并发提示按当前实际托管路由供应商计算，未命中时回退到默认托管供应商，避免只看当前卡片造成误判。
- **无障碍动效适配**：状态呼吸动画支持 `prefers-reduced-motion`，减少系统关闭动画时的视觉干扰。

## 修复
- **修复自定义 CLI 状态串扰**：切换自定义 CLI 工具后，旧工具的最近托管供应商不再影响当前工具 Tab 的代理与并发状态。

## 发布
- **本次发版版本号推进到 `v2.8.79`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.79` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.78

## 变更
- **供应商限制改为实时并发口径**：移除会话隔离入口与会话上限配置，供应商卡片统一显示当前并发 `x/x`。
- **并发上限默认无限制**：实时并发上限留空时不再默认限制为 5，首页显示为 `x/∞`。
- **无限并发也显示实时占用**：无限制供应商仍会记录当前请求数，便于观察真实并发压力。

## 修复
- **修复并发统计展示失真**：无限并发模式下不再一直显示 `0/∞`。
- **修复前端样式构建问题**：清理会话隔离样式时保留正确的响应式 CSS 结构。
- **清理会话隔离配置残留**：移除 `session_affinity` 设置读写与旧 Wails 服务注册，避免历史配置产生误导。

## 发布
- **本次发版版本号推进到 `v2.8.78`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.78` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.77

## 新增
- **供应商实时并发上限**：供应商配置支持设置实时并发上限，超过后自动尝试下一个可用供应商，避免单个供应商被瞬时请求打满。
- **会话详情显示 User-Agent**：首页会话详情可查看每个会话最近一次入站 `User-Agent`，便于区分 CLI / 客户端来源。
- **日志验证详情显示 User-Agent**：日志页“验证”列悬浮窗新增 `User-Agent`，并同时展示选中模型与返回模型。

## 优化
- **并发满载不计入供应商失败**：供应商因实时并发达到上限被跳过时，不写入失败计数，也不触发黑名单。
- **验证详情展示优化**：验证悬浮窗加宽并支持长 `User-Agent` 换行展示。

## 技术改进
- **请求日志扩展来源字段**：`request_log` 新增 `user_agent` 字段，并兼容旧库自动迁移。
- **补充并发与 User-Agent 回归测试**：覆盖供应商并发占位释放、满载跳过、会话状态 UA 返回与日志字段迁移。

## 发布
- **本次发版版本号推进到 `v2.8.77`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.77` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.76

## 修复
- **修复会话隔离路由顺序**：新会话按供应商顺序填满，未满供应商存在时不再跳到后面的满载供应商。
- **修复轮询干扰会话分配**：会话隔离路径不再受轮询顺序影响，也不再推进轮询游标。
- **修复满载绑定边界**：真正绑定前增加容量二次校验，避免并发下把新会话绑定到已满供应商。

## 优化
- **保留全满兜底**：所有可路由供应商都满载时，允许按最低压力超额兜底。
- **统一会话标签样式**：首页 `0/5` 会话标签与其他状态标签尺寸和视觉风格保持一致。

## 发布
- **本次发版版本号推进到 `v2.8.76`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.76` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.75

## 修复
- **修复会话容量排序口径**：供应商是否未满改为只按绑定会话数判断，避免正在请求数把未满供应商误判为满载。
- **修复新会话路由偏移**：排名靠前且仍有会话容量的供应商会继续优先参与新会话分配，不会被瞬时 activeRequests 提前挤到后面。

## 优化
- **保留请求压力排序**：activeRequests 仍作为同等容量下的瞬时压力参考，但不再计入会话上限。
- **补充回归测试**：覆盖普通 Provider 与 Gemini Provider 的会话容量排序口径。

## 发布
- **本次发版版本号推进到 `v2.8.75`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.75` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.74

## 优化
- **会话识别增强**：Codex 托管请求支持从稳定线程字段、项目目录与记录路径识别会话，Claude / Codex 工具调用链路可补充识别会话归属。
- **供应商负载调度优化**：新会话首次分配参考 sub2api 并发限制口径，按绑定会话数与正在请求数计算负载率，优先选择低负载供应商。
- **会话状态展示优化**：首页会话标签尺寸与现有标签对齐，会话详情弹窗改为更紧凑的匿名状态展示。

## 安全与稳定性
- **响应成功后再确认工具会话绑定**：流式失败、客户端中断或响应未完整结束时不落成绑定，避免错误占用供应商。
- **会话标识脱敏落库**：请求与响应日志捕获前会结构化脱敏 session、thread 与 tool call 相关字段，避免原始会话标识写入日志。
- **补充会话调度回归测试**：覆盖工具会话、负载排序、容量兜底与 payload 脱敏等关键路径。

## 发布
- **本次发版版本号推进到 `v2.8.74`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.74` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.73

## 新增
- **托管会话隔离路由**：托管请求可按平台开启会话隔离，同一真实会话会优先绑定到同一供应商，减少多窗口或多会话请求互相抢占第一个供应商的问题。
- **供应商会话容量配置**：每个供应商可配置会话上限与释放时间，支持按容量分配新会话，满载后按最少会话数与溢出程度兜底。
- **首页会话状态展示**：首页托管按钮旁新增会话隔离开关，供应商卡片显示占用会话数，点击可查看匿名会话详情。

## 优化
- **失败降级支持迁移绑定**：会话绑定的供应商失败时允许降级到下一个可用供应商，成功后自动迁移绑定；全部失败时保留原绑定。
- **轮询状态更准确**：会话隔离下仅在实际转发前推进轮询游标，避免未命中的供应商消耗轮询位置。
- **Gemini 会话隔离提示**：补充说明仅带稳定 `metadata` 会话字段的请求会参与会话隔离。

## 安全与隐私
- **匿名会话详情**：会话详情不展示原始 sessionKey、完整 hash、请求体、响应体、API Key 或用户内容。
- **内存态会话记录**：会话 hash 仅保存在内存中，不落库，应用重启后运行态会话编号清零。

## 修复
- **修复会话隔离并发边界**：修复 pending binding 误确认、失败回滚误删、activeRequests 计数不准等并发场景。
- **修复供应商禁用/删除后的绑定释放**：供应商被拉黑、禁用或删除后，会话绑定会按规则释放，不会被失败回滚重新复活。
- **修复 Wails 服务暴露范围**：会话状态通过专用 facade 暴露，避免前端直接访问代理启动/停止能力。

## 发布
- **本次发版版本号推进到 `v2.8.73`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.73` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.72

## 修复
- **可用性页面闪烁与导航栏不可见**：进入「可用性」页面时，loading 状态在骨架面板与真实内容间整块切换，导致卡片列表"消失→骨架→重现"地闪烁；叠加 `keep-alive` 下离开页面后倒计时/刷新定时器与主题观察器仍在后台运行。现将 loading 面板改为仅在"首次且无数据"时显示、切换探测/日志模式与时间范围时保留旧卡片平滑更新；新增 `onActivated`/`onDeactivated` 生命周期正确启停定时器、事件监听与滚动锁；并给侧边栏建立独立层叠上下文，缓解进入页面时的瞬时渲染异常。

## 发布
- **本次发版版本号推进到 `v2.8.72`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.72` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.71

## 修复
- **日志思考强度标签恢复间距与配色**：日志列表"模型"列的推理强度小标签此前面贴模型名、且不随档位变色，根因是标签样式写在父组件的 scoped 样式中、无法穿透到子组件 `LogsTable` 内部渲染的元素。现将样式下沉到表格子组件自身的 scoped 块，强度档位 `low`/`medium`/`high`/`xhigh`/`max` 可正确显示对应颜色，并与模型名保持 `0.35rem` 间距。

## 发布
- **本次发版版本号推进到 `v2.8.71`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.71` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.70

## 新增
- **日志模型列显示推理强度**：日志列表的“模型”列会在模型名后追加推理强度，例如 `gpt-5.5 high`。

## 技术改进
- **推理强度解析覆盖多协议**：支持从 `reasoning.effort`、`reasoning_effort`、Claude `output_config.effort`、Gemini `thinkingConfig`、模型后缀等来源提取并规范化显示。
- **Gemini 日志记录修正**：推理强度会在应用 provider `RequestBodyOverrides` 后重新提取，避免记录为空或旧值。
- **未知推理强度灰色显示**：未知值会保留并以灰色展示；`thinkingBudget: 0` 不再误显示为 `medium`。

## 发布
- **本次发版版本号推进到 `v2.8.70`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.70` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.68

## 修复
- **Windows 点击应用图标会稳定打开主界面**：Windows 主窗口改为在 `app.Run()` 前预创建，并在 WebView 首次加载完成后再校正尺寸、显示和聚焦，绕开启动事件回调时序导致主界面仍停在托盘的问题。

## 优化
- **Windows 首窗尺寸继续收口**：启动前使用更保守的默认窗口尺寸，运行时再按当前屏幕工作区居中适配，降低低分辨率设备上首屏过大的概率。
- **Linux / macOS 启动语义保持不变**：Linux 继续沿用启动事件自动显示，macOS 继续保持托盘优先与主窗口懒加载。

## 发布
- **本次发版版本号推进到 `v2.8.68`**：同步更新应用常量、Wails 构建配置和平台构建元数据，并准备推送 `v2.8.68` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.67

## 修复
- **Windows 启动会自动打开主界面**：安装后首次启动或正常启动时，Windows / Linux 会主动创建并显示主窗口，不再只停留在托盘。
- **Windows 窗口按钮恢复正常**：Windows 主窗口显式启用最小化、最大化和关闭按钮，并保留系统原生标题栏行为。

## 优化
- **Windows 默认窗口大小更适配小屏**：Windows 主窗口会按当前屏幕工作区计算初始尺寸并居中，避免固定大窗口在低分辨率设备上显示过大。
- **macOS 继续保持托盘优先**：启动自动打开仅作用于非 macOS 平台，避免破坏 macOS 的托盘优先和主窗口懒加载语义。

## 发布
- **本次发版版本号推进到 `v2.8.67`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.67` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.66

## 优化
- **日志弹窗按钮更整齐**：供应商日志弹窗内的按钮间距统一为 0，操作区看起来更规整。

## 修复
- **托管供应商错误红点会及时消失**：供应商的错误日志全部标记已读后，首页“供应商日志”按钮不再继续显示红点。
- **同名供应商提醒更准确**：托管和普通供应商同名时，错误提醒会按实际供应商区分，避免已读后仍被旧提醒打扰。

## 发布
- **本次发版版本号推进到 `v2.8.66`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.65

## 优化
- **供应商错误日志更安心**：首页供应商日志弹窗默认只看未读失败，点“标记已读”不会删除请求记录。
- **错误提醒更聚焦**：首页供应商红点只提示未读失败日志，已经处理过的旧错误不会一直打扰。

## 修复
- **成功率统计不再被已读操作影响**：标记失败日志为已读后，供应商成功率不会被改成 100%，日志页也仍能查到完整失败请求。
- **日志弹窗语义更清楚**：原来的清除动作已调整为已读/未读管理，避免误删错误日志造成排查断档。

## 发布
- **本次发版版本号推进到 `v2.8.65`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.64

## 新增
- **模型映射支持未命中策略**：编辑供应商时，可以选择模型映射没匹配上时直接拦截，或按原模型名继续转发给供应商，适合不同供应商的模型命名习惯。
- **模型映射配置更清晰**：模型映射列表现在会显示规则数量和匹配类型，新增、编辑和删除规则时更容易看清每条规则的走向。

## 优化
- **模型选择更顺手**：添加映射时支持从内置模型中搜索选择，减少手动输入错误。
- **只在适用供应商展示映射配置**：不支持这套路由能力的供应商不再显示无效配置项，避免保存后不生效造成误解。

## 修复
- **说明气泡不再被弹窗遮挡**：模型映射标题旁的说明气泡会自动选择合适位置展示，滚动弹窗或靠近边缘时也能完整看到。

## 发布
- **本次发版版本号推进到 `v2.8.64`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.63

## 优化
- **首页供应商卡片悬停更稳定**：鼠标放到供应商卡片上时，网址、花费金额、托管状态点和右侧操作按钮不再上跳，浏览供应商时画面更安静。
- **按钮反馈更克制**：保留颜色、阴影和提示气泡等必要反馈，减少不必要的位移动效。

## 发布
- **本次发版版本号推进到 `v2.8.63`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.62

## 优化
- **首页悬停体验更安静**：首页供应商 Tab 和供应商卡片在鼠标放上去时不再轻微上跳，浏览和切换供应商时更稳定。
- **日志统计卡片不再抖动**：日志页的 Token 消耗与费用消耗卡片保留可点击状态，但鼠标悬停时不会再上移或产生明显抬起感。

## 发布
- **本次发版版本号推进到 `v2.8.62`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.61

## 优化
- **托盘供应商更好认**：托盘弹窗显示启用供应商时，现在会在名称旁显示对应的供应商图标，并与首页供应商卡片保持一致。
- **供应商名称显示更稳**：托盘里的供应商名称继续保持紧凑单行，图标缺失时也会显示简短标识，避免出现空白。

## 发布
- **本次发版版本号推进到 `v2.8.61`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.60

## 优化
- **托盘余额更好读**：余额里的货币单位和数字之间增加了一点间距，看起来更清楚。

## 发布
- **本次发版版本号推进到 `v2.8.60`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.59

## 修复
- **托盘余额数字颜色已对齐首页花费**：托盘里的剩余余额数字现在和首页供应商卡片的花费金额使用同样的金色，更容易看清余额。
- **货币单位样式保持不变**：`US$` 继续沿用原来的颜色，只调整数字本身。

## 发布
- **本次发版版本号推进到 `v2.8.59`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.58

## 优化
- **托盘余额数字与首页金额颜色保持一致**：托盘里的剩余余额数字现在会使用和首页供应商卡片“花费”金额相同的高亮颜色，查看余额时更醒目。
- **货币单位保持原有样式**：`US$` 等货币单位仍沿用原来的颜色，仅调整后面的金额数字，避免单位和数值混在一起。

## 发布
- **本次发版版本号推进到 `v2.8.58`**：准备推送新版本 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.57

## 优化
- **托盘余额显示更清晰**：供应商余额现在会把货币单位和金额用不同颜色区分，扫一眼就能看清余额数字。
- **余额数字刷新更稳定**：托盘金额使用更稳定的数字宽度，余额变化或刷新时不容易出现视觉跳动。
- **中英文显示更自然**：托盘余额文案补齐独立中英文标签，避免后续语言切换或文案调整时出现不自然的显示。

## 发布
- **本次发版版本号统一推进到 `v2.8.57`**：同步更新应用版本与打包元数据，并准备推送 `v2.8.57` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.56

## 新增
- **Claude 原生供应商支持缓存 TTL 配置**：首页编辑 Claude 供应商时，Anthropic Messages 原生格式现在可以选择缓存 TTL：默认、5 分钟或 1 小时；不选择时保持原有默认行为。
- **供应商卡片显示 TTL 标签**：选择 5m 或 1h 后，首页供应商卡片会在优先级标签后显示对应 TTL 标记，并用不同颜色区分，鼠标悬停可查看说明。

## 优化
- **包含 `c16be83c8d09dfd647fec7ec89358ef90b83ae44` 的 Claude Responses 兼容增强**：完善 Claude 与 OpenAI Responses 的请求 / 响应转换、流式事件转换、工具调用收尾、prompt_cache_key 不兼容回退和 previous_response_id 续链绑定。
- **TTL 注入范围更克制**：供应商级 TTL 只会覆盖请求中已经存在的 Anthropic `ephemeral cache_control`，不会新增缓存断点，也不会影响 OpenAI Chat / Responses 兼容格式供应商。

## 技术改进
- **补齐 TTL 全链路测试**：覆盖后端 TTL 覆盖规则、前端表单归一化、供应商卡片展示和中英文文案。
- **锁定 prompt_cache_key 共享禁用契约**：新增 runtime 与 availability probe 的交叉测试，确保任一链路发现上游不支持 `prompt_cache_key` 后，另一链路会共享禁用状态并跳过自动注入。

## 发布
- **本次发版版本号统一推进到 `v2.8.56`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.56` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.55

## 修复
- **托盘平台图标与首页供应商 Tab 保持一致**：托盘弹窗的 Claude Code / Codex 头像不再显示 `C` / `X` 字母块，而是复用首页供应商切换 Tab 的同一套品牌图标映射，Claude Code 显示 Claude 图标，Codex 显示 OpenAI 图标。

## 技术改进
- **托盘品牌图标复用统一 provider icon helper**：托盘现在通过 `HOME_PROVIDER_TAB_OPTIONS` 和 `getProviderDisplayIconSvg` 获取平台图标，并预加载对应 SVG；图标加载失败时仍保留字母 fallback，避免弹窗出现空白。

## 发布
- **本次发版版本号统一推进到 `v2.8.55`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.55` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.54

## 新增
- **托盘额度支持回退到启用供应商**：Claude Code / Codex 的托盘全局额度未设置时，会按当前路由顺序扫描启用供应商，展示第一个已配置额度的供应商本地 5h / 周额度或远端余额查询结果。

## 修复
- **托盘额度不再被空配置供应商卡住**：当最低优先级的第一个启用供应商没有额度配置时，会继续按真实路由顺序查找同级或下一优先级中已配置 5h / 周额度、余额查询的供应商，不再提前退回“总消耗”。
- **托盘新增文案走统一 i18n**：移除托盘内硬编码翻译，新增“来自启用供应商”“剩余”“查询失败”等中英文文案，避免英文环境混入中文。

## 优化
- **托盘额度展示复用首页供应商额度语义**：余额、查询失败、剩余值等判断改为复用首页 helper，避免后续首页额度逻辑调整后托盘弹窗分叉。

## 技术改进
- **补齐托盘供应商 fallback 单测**：覆盖 level 归一化、同 level 顺序保持、无额度配置跳过、本地额度与远端余额查询识别等边界。
- **供应商 5h 额度倒计时支持秒级刷新**：托盘展示供应商级 5h 额度时会启用秒级 ticker，倒计时不再按分钟级刷新卡顿。

## 发布
- **本次发版版本号统一推进到 `v2.8.54`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.54` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.53

## 新增
- **可用性页支持用真实请求日志生成状态条**：新增“日志生成”模式，可按最近 15 分钟、1 小时、6 小时、24 小时和 7 天聚合实际请求日志，直接把真实调用成功率、失败情况和延迟表现展示成 Provider 可用性时间线，不再只能依赖主动探测样本。
- **端点检测与日志模式可一键切换**：可用性页顶部新增模式切换和时间范围选择，用户偏好会写入本地存储；日志模式下刷新按钮会变成“刷新日志”，历史标题、起止标签和数据来源提示也会跟随切换。

## 优化
- **日志可用性聚合更懂脏数据现实**：后端会同时用 `provider_id` 与 provider 名称匹配请求日志，兼容历史日志里 ID 缺失或名称回退的情况；空时间桶不参与可用率计算，平均延迟按请求级样本加权，结果更接近真实体感。
- **可用性时间线展示更加稳**：日志模式复用原有 `ProviderTimeline` 结构和卡片渲染链路，保留状态、模型、错误信息、平均延迟、uptime 与最新 bucket，避免前端维护两套互相打架的展示逻辑。

## 技术改进
- **新增日志可用性聚合服务与单测覆盖**：后端补齐时间范围解析、固定 bucket 构建、状态归类、失败/告警文案、Provider 映射和自定义延迟阈值逻辑，并用请求日志样本覆盖成功、失败、告警、空桶和加权平均等关键场景。
- **前端 mock 与 i18n 同步补齐**：浏览器预览模式新增日志可用性 mock 数据，中文 / 英文文案补齐模式、范围、刷新、历史标题和日志来源说明，开发态不连后端也能看完整交互。

## 发布
- **本次发版版本号统一推进到 `v2.8.53`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.53` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.52

## 修复
- **可用性页配置弹窗终于不再被页面层级卡脖子了**：供应商可用性配置弹窗改为 `Teleport` 到 `body` 渲染，并在打开时锁定页面滚动，避免弹窗被父级布局、滚动容器或遮罩层级影响，点背景关闭和点面板内部操作也分得清清楚楚。
- **可用性页浅色主题不再披着暗色马甲**：页面会实时监听全局 `dark` class 变化，为主页面和弹窗挂载明确的 light / dark 主题类；浅色模式下卡片、历史图、按钮、输入框、弹窗背景和状态色都补齐了专属配色。

## 技术改进
- **主题状态从隐式 CSS 选择器收口成显式状态**：新增 `MutationObserver` 同步根节点主题变化，并在组件卸载时断开观察和释放滚动锁，避免主题切换和弹窗生命周期留下幽灵状态。
- **可用性页视觉层级做了一次系统性补漏**：浅色模式下统一边框、阴影、文字层级和状态色 token，解决局部组件各自为政导致的“页面像拼贴画”问题。

## 发布
- **本次发版版本号统一推进到 `v2.8.52`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.52` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.51

## 修复
- **日志页四大统计卡片终于会听 light 模式的话了**：修复日志页切到浅色主题后，调用次数、Token 消耗、缓存命中和费用消耗四张统计卡片仍保持暗色背景的问题；现在会按全局主题切换为白色卡片、深色文字、浅色分隔线和柔和阴影，视觉风格与页面其它浅色面板保持一致。
- **统计卡片主题命中逻辑不再赌 `html.light`**：父级日志页把实时 `isDarkTheme` 传给统计卡片组件，组件内部通过 `logs-summary--light / logs-summary--dark` 控制配色，避免系统默认浅色或主题 class 未及时挂载时继续落回暗色样式。

## 发布
- **本次发版版本号统一推进到 `v2.8.51`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.51` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.50

## 新增
- **首页供应商 Tab 显示可配置**：通用设置新增“首页供应商 Tab”，可控制首页顶部显示 `Claude Code`、`Codex`、`Gemini`、`OpenCode` 与“其他”哪些供应商分类；默认仅显示 `Claude Code`、`Codex`、`Gemini`，并会至少保留一个可见 Tab，避免首页被自己手滑关成空城。
- **首页和设置项统一显示供应商图标**：设置页的供应商显示按钮、首页顶部 Tab 均改为“图标 + 名称”展示，复用同一套图标映射，`Codex` 使用 OpenAI 图标，`其他` 提供内置 fallback 图标，整体观感不再像原生 checkbox 临时客串。

## 技术改进
- **首页 Tab 可见性纳入 AppSettings 持久化**：新增 `home_provider_tabs` 字段并在前后端统一归一化，旧配置自动回退到默认三项，非法值与重复值会被过滤。
- **Tab 配置来源收口**：`MAIN_TABS` 改为复用共享的 `HOME_PROVIDER_TAB_OPTIONS`，首页渲染、设置页展示和持久化合法值不再三套配置各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.50`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.50` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.49

## 修复
- **OpenCode 模型参数不再内联展开拖垮编辑弹窗**：首页 OpenCode 供应商编辑里点击模型 ID 左侧按钮时，不再在主弹窗内部展开复杂参数 DOM，改为打开独立模型配置 JSON 编辑弹窗，绕开 WebView 长表单重排/绘制异常导致的“弹窗消失，只剩模糊遮罩”。
- **模型配置保存链路改为一次性 JSON 提交**：模型详情弹窗会校验 JSON 必须是对象，保存后同步回结构化模型配置与 OpenCode provider JSON，避免半编辑状态持续触发主弹窗渲染和配置同步。

## 技术改进
- **主弹窗和模型详情弹窗状态隔离**：模型详情拥有独立打开 ID、编辑文本和错误状态；重置表单、删除模型、重命名模型或从 JSON 同步模型列表时都会清理/迁移详情状态，避免残留状态把下一次编辑弹窗锁死。
- **新增模型不再自动触发展开渲染**：添加模型和拉取候选模型后只更新列表与配置，不再自动展开参数详情，减少 OpenCode 编辑器在 WebView 下的瞬时 DOM 压力。

## 发布
- **本次发版版本号统一推进到 `v2.8.49`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.49` tag 触发 GitHub 自动打包，作为 `v2.8.48` 后继续针对该弹窗崩溃问题的热修版本。

---

# Code Switch v2.8.48

## 修复
- **OpenCode 模型参数展开彻底切断误关弹窗链路**：首页 OpenCode 供应商编辑里点击模型行左侧展开按钮时，按钮现在阻断 `pointerdown` / `click` 冒泡，Provider 编辑弹窗也禁用 backdrop 点击关闭，避免 WebView 把面板内部点击误判成遮罩点击，导致只剩模糊遮罩。
- **模型详情渲染改成安全快照**：模型名称、扩展字段和 SDK options 改为在 `computed` 中预先生成安全条目，模板只消费数组，不再在 render 阶段重复调用解析函数，避免脏数据或瞬时状态把弹窗子树直接炸掉。

## 技术改进
- **InlineModal 面板内部点击统一阻断到 wrapper**：modal panel 增加内部点击阻断，确保所有弹窗内容区交互都不会冒泡到遮罩关闭逻辑；后续其他长表单弹窗也一起吃到这个防御。
- **OpenCode 模型编辑状态重置更干净**：打开/切换编辑弹窗时同步清空展开模型列表，避免上一次编辑遗留的展开状态影响下一次弹窗渲染。

## 发布
- **本次发版版本号统一推进到 `v2.8.48`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.48` tag 触发 GitHub 自动打包，作为 `v2.8.47` 的二次热修版本。

---

# Code Switch v2.8.47

## 修复
- **OpenCode 模型参数展开不再把供应商编辑弹窗炸没**：修复首页 OpenCode 供应商编辑里点击模型行左侧展开按钮后，弹窗内容消失、只剩模糊遮罩，后续再次打开也只能看到遮罩的问题。
- **模型详情渲染补上运行时防御**：展开模型参数时只按对象模型读取扩展字段和 SDK options，遇到异常/旧数据形态时退回空对象，不再让 `Object.entries` 在渲染阶段抛错把整棵弹窗子树拖下水。

## 技术改进
- **回滚 Provider 编辑弹窗的合成层覆盖**：移除 `v2.8.46` 中对 Provider 编辑弹窗面板 `transform` / `will-change` 的特殊覆盖，避免 WebView 在展开长表单时出现只绘制遮罩、不绘制面板的渲染异常。

## 发布
- **本次发版版本号统一推进到 `v2.8.47`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.47` tag 触发 GitHub 自动打包，作为 `v2.8.46` 的热修复版本。

---

# Code Switch v2.8.46

## 修复
- **OpenCode 模型编辑不再因为改 key 抖成筛子**：首页编辑 OpenCode 供应商时，额外 options、模型 id、模型扩展字段和模型 options 改名都改用稳定 UI key 渲染，避免 Vue 在输入过程中复用错 DOM，导致焦点跳动、输入值串台或列表项看起来“穿模”。

## 优化
- **供应商编辑弹窗减少合成层负担**：Provider 编辑弹窗单独关闭面板 `transform` / `will-change` 提示，避开 WebView 下长表单、backdrop-filter 和输入框频繁重绘叠在一起时的渲染抽风。

## 技术改进
- **OpenCode 编辑器渲染身份和业务 key 解耦**：新增 `uiKey` 状态维护、重命名迁移和删除清理逻辑，保留 provider/model 配置真实 key 的同时，让前端列表渲染拥有独立稳定身份。
- **模型嵌套字段的 key 生命周期补齐**：模型改名时同步迁移嵌套字段 / options 的 UI key，删除模型或字段时同步清理，降低后续编辑器状态泄漏概率。

## 发布
- **本次发版版本号统一推进到 `v2.8.46`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.46` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.45

## 优化
- **OpenCode 预设说明语义更清楚**：首页编辑 OpenCode 供应商时，预设说明统一调整为“选择完整供应商模板”，中英文同步更新，避免用户误以为这些预设只能用于特定导入场景。

## 技术改进
- **OpenCode 预设文案语义收口**：中英文 locale 保持同一语义口径，继续保留自动填充 NPM、Base URL、模型和 SDK 选项的说明，不改动实际预设选择、搜索和配置写入逻辑。

## 发布
- **本次发版版本号统一推进到 `v2.8.45`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.45` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.44

## 优化
- **OpenCode 预设选择器支持模糊搜索了**：首页编辑 OpenCode 供应商时，预设下拉从原生 `select` 升级为带搜索框的选择器，可按预设名称、描述、分类、Base URL、官网 / API Key 地址和 AI SDK `npm` 包名快速过滤，`ds` 这种字符序搜索也能命中 `DeepSeek`，不用再在一长串供应商里人工潜水。
- **OpenCode 预设下拉展示信息更清楚**：选项里直接展示供应商分类和 Base URL，当前选中项也会显示分类 badge，选错预设的概率降下来一点，省得配置表单像盲盒。

## 修复
- **OpenCode 预设选择不再触发 i18n 编译异常**：修复“模板参数”提示文案中的 `${...}` 被 `vue-i18n` 当作占位符解析，导致打包后选择预设时报 `Invalid token in placeholder: '...'`、弹窗内容发糊和预设无法选择的问题。

## 技术改进
- **预设搜索逻辑前端本地化收口**：新增轻量字符序 fuzzy match，搜索状态不进入表单持久化，切换预设和重置表单时会自动清理搜索关键字；下拉打开后自动聚焦搜索框，减少鼠标来回折腾。
- **OpenCode 预设文案转义按 `vue-i18n` message literal 规范处理**：中英文 locale 使用 `{'${...}'}` 保留字面量展示，避免后续生产构建再被 message compiler 偷袭。

## 发布
- **本次发版版本号统一推进到 `v2.8.44`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.44` tag 触发 GitHub 自动打包。

---

# Code Switch v2.8.43

## 功能
- **OpenCode 供应商配置正式补齐完整预设能力**：新增 OpenCode provider preset 配置库，编辑供应商时支持按 AI SDK 包选择预设、自动带出 Base URL / API Key / 官网 / API Key 地址 / 分类 / 合作伙伴信息，并保留原始 provider id，别再靠手抄 `opencode.json` 跟 JSON 拼刺刀。
- **OpenCode 模型配置入口补上了**：编辑 OpenCode 供应商时可直接维护 `models`、模型名称、展示名称、额外字段和 `options`，并提供预设模型建议，导入后的 OpenCode 供应商终于不是“只有壳没有芯”。

## 修复
- **OpenCode 余额查询不再依赖当天是否使用过供应商**：只要供应商在 OpenCode 配置中启用 / 存在且配置了余额查询，就会参与自动刷新并常驻显示余额，不再因为今天没跑过请求就把余额藏起来。
- **OpenCode tab 隐藏本地转发托管开关**：OpenCode 是 additive config 管理，不是 Claude / Codex / Gemini 那套本地代理模式；首页切到 OpenCode 时不再显示误导性的托管开关，只保留新增和刷新操作。
- **OpenCode 模型配置卡片布局收口**：修复模型配置行输入框撑爆卡片、删除按钮被遮挡、嵌套字段边距不齐的问题，小屏下也会自动堆叠显示，别再让按钮躲猫猫。

## 技术改进
- **OpenCode live provider id 能力补到前后端链路**：后端新增 live provider id 查询能力，前端编辑表单可用它校验 provider id 冲突，避免配置写进 live config 时出现重复 key 和幽灵供应商。
- **OpenCode provider 元数据完整持久化**：卡片、表单、序列化、OpenCode service 和 Wails bindings 全链路补齐 `apiKeyUrl`、`category`、`partnerPromotionKey`、`icon` 等字段，预设信息不会再保存一次就丢。
- **额度刷新与代理开关规则拆成可测试工具**：新增 OpenCode 余额自动刷新和代理开关显隐的独立规则单测，避免后续又把 OpenCode 当成本地代理平台处理。

## 发布
- **本次发版版本号统一推进到 `v2.8.43`**：作为 OpenCode 供应商配置迁移补齐、模型配置 UI 优化和余额常驻显示修复版本推送 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.42

## 优化
- **控制台运行信息展示全面换成 GitHub 风格日志面板**：保留原有日志内容、搜索、筛选、复制和自动滚动逻辑，重做顶部工具栏、级别筛选 tabs、错误卡片、供应商错误详情、诊断标签和操作按钮，日志终于不像“终端输出糊在网页上”了。
- **控制台支持跟随全局白天 / 黑夜主题**：不新增控制台私有主题开关，直接响应项目统一的 `html.light` / `html.dark` 状态；白天模式补齐浅色背景、卡片、badge、按钮和输入框，黑夜模式继续保持低亮度控制台观感。
- **控制台 tabs 与工具栏间距收口**：清理筛选按钮默认间距，压缩 tab 容器内边距和工具栏高度，让筛选按钮贴合边框，不再像隔着一条护城河。

## 修复
- **控制台页面外层不再出现滚动条**：修复控制台根容器使用 `100vh` 撑爆宿主布局的问题；现在页面外层锁定不滚动，仅日志显示区域独立滚动，避免右侧出现整页滚动条和内容区高度错位。

## 技术改进
- **控制台滚动层级重新收口**：控制台路由下主内容区禁用外层滚动，控制台根容器改为填充父容器高度，日志列表通过 `min-height: 0` / `overflow-y: auto` 成为唯一滚动层。
- **控制台主题色统一变量化**：将明暗两套颜色沉淀为控制台局部 CSS variables，降低后续继续调 UI 时把白天 / 黑夜样式改崩的概率。

## 发布
- **本次发版版本号统一推进到 `v2.8.42`**：作为控制台 UI、主题和滚动体验优化版本推送 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.41

## 修复
- **控制台标题不再被工具栏挤成换行**：修复控制台顶部操作按钮和筛选项较多时，左侧“控制台”标题被 flex 布局压缩后断行显示的问题；标题现在固定单行，右侧工具区自行换行，视觉终于不再像被折叠成小纸条。

## 技术改进
- **控制台顶部栏布局收口**：仅在控制台页面覆盖 `global-actions` / `global-eyebrow` 的 flex 行为，避免影响首页、MCP、Gemini 等共享顶部栏页面。

## 发布
- **本次发版版本号统一推进到 `v2.8.41`**：作为控制台标题显示修复版本推送 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.40

## 修复
- **修复 OpenCode 供应商导致的 release 打包失败**：Wails 在 CI 打包前会重新生成 bindings，`OpenCodeProvider.liveConfigManaged` / `isInConfig` 这类 Go 指针布尔值会被映射成 `boolean | null | undefined`；前端现在在转换卡片模型时统一收口为明确 `boolean`，避免 `vue-tsc` 在 Windows / macOS / Linux 打包阶段报 TS2322。

## 技术改进
- **补齐 CI 同款验证路径**：按 Windows production flags 重新执行 `wails3 generate bindings` 后再跑 `vue-tsc --noEmit`，确认生成 binding 后的类型检查通过，避免手写 binding 和 CI 生成结果不一致造成“本地绿、远端红”的阴间体验。

## 发布
- **本次发版版本号统一推进到 `v2.8.40`**：作为 `v2.8.39` 的打包热修复版本重新推送 tag，触发 GitHub 自动打包。

---

# Code Switch v2.8.39

## 功能
- **OpenCode 供应商管理正式接进首页供应商体系**：在 Gemini 后新增 OpenCode 供应商页，支持基础 CRUD、排序、复制、从 `~/.config/opencode/opencode.json` 导入 live provider，并接入 Wails service / 前端 binding，让 OpenCode 终于不用靠手改 JSON 续命。
- **OpenCode provider 表单补齐 AI SDK 配置入口**：支持 npm 类型、Base URL、API Key、models / options JSON 配置，Anthropic / Google / OpenAI Compatible 默认模型按包类型生成，不再一股脑塞 `gpt-4o`。

## 修复
- **OpenCode live config 不再被半截保存污染**：前端保存改为单次批量 RPC，后端按完整 provider snapshot 原子写入 managed 配置和 live config；写 live / 写本地任一步失败都会回滚，避免用户真实 `opencode.json` 被悄悄写歪。
- **OpenCode provider key 保留原样**：导入 live config 时保留用户原始 key，后续编辑不会把 `OpenAI.Custom` 之类的 key 归一化成 `openai-custom` 造成重复 provider。
- **OpenCode 开关语义收口**：普通启用 / 停用会同步驱动 `liveConfigManaged` / `isInConfig`，关闭卡片就会从 live config 移除；新建关闭和复制副本也保持 DB-only，不会出现“看起来禁用了，实际仍写进 opencode.json”的坑。
- **OpenCode 隐藏字段不再被表单擦掉**：编辑 OpenCode 时保留 `requestBodyOverrides` 等隐藏配置，不再因为 UI 没展示就保存成空对象。
- **开发预览 mock 不再污染真实桌面开发环境**：OpenCode dev mock 只在浏览器 dev 且没有 Wails bridge 时启用，连接真实 Wails 服务返回空列表时不再塞假卡片、保存也不再莫名 no-op。

## 技术改进
- **补齐 OpenCode 批量保存和 live config 回滚测试**：覆盖 raw key 保真、live 冲突、managed/live 双写失败回滚、DB-only provider、复制副本 live flags 清理、前端保存失败回滚和 dev mock 边界。
- **OpenCode 默认配置补上 `options.setCacheKey: true`**：统一默认表单行为，同时继续避免空 URL / 空 API Key 写入占位符污染 live config。

## 发布
- **本次发版版本号统一推进到 `v2.8.39`**：补齐更新日志并准备推送 `v2.8.39` tag 触发 GitHub 自动打包，这波主要是 OpenCode 供应商管理迁移和 live config 安全收口，别再让用户的真实配置文件当试验田。

---

# Code Switch v2.8.38

## 修复
- **日志金额终于不再被当前模型价格表“穿越重算”了**：请求日志列表金额现在优先保留写入时保存的 `total_cost` / 成本拆分快照，模型价格后续调整只影响新日志，不会把历史账本从 `$0.024` 又硬算成 `$0.086420`。
- **金额悬浮详情和日志记录金额统一口径**：当历史日志只有总金额、没有成本拆分时，悬浮详情只展示日志记录金额，不再拿当前价格表拼一套看似精确但并不属于当时请求的公式。
- **Provider API 历史金额不再回退到内置价格公式**：缺少 provider 价格快照和成本拆分时，接口计价日志会展示已记录金额；负向金额、退款或迁移脏数据也按历史快照处理。

## 技术改进
- **FE / BE 成本快照判定口径收口**：前后端统一使用“非零成本字段或 `has_pricing`”判断历史快照，覆盖免费模型、退款、负向调整和旧数据迁移场景，减少口径分叉导致的隐性账务问题。
- **日志价格来源脏数据自动规范化**：后端遇到 `price_source = none` 但已存在历史金额时，会在返回列表时规范成内置价格快照语义，避免 UI 展示成“无价格”但又有金额的别扭状态。
- **补齐金额快照回归测试**：新增后端 `applyLogPricing` 快照保护测试，以及前端 builtin / provider_api 悬浮详情回归用例，锁住“历史金额不随当前价格表重算”的核心规则。

## 发布
- **本次发版版本号统一推进到 `v2.8.38`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.38` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再搁那儿各唱各的调。

---

# Code Switch v2.8.37

## 修复
- **首页供应商卡片在性能数据还没就绪时也能老实显示余额状态了**：修复 `v2.8.36` 将非周期余额 / 查询失败面板收进性能信息行后，供应商统计未 ready 时余额面板跟着整行一起消失的问题；现在即便速度、延迟还没加载出来，余额、错误提示、更新时间和刷新按钮也会单独占一行兜底展示，不再让用户盯着空卡片犯迷糊。

## 技术改进
- **余额状态展示补上 ready / loading 双路径兜底**：新增 `showStandaloneQuotaStatusRow`，在 `stats.state !== 'ready'` 且存在余额或错误项时复用同一套 quota 面板渲染，避免 ready 态和未 ready 态出现两套展示口径。
- **独立余额行样式继续瘦身**：新增 `card-balance-quota-panel--standalone`，清掉内联性能行专用的左分隔和缩进，让 fallback 行看起来像卡片自己的指标补充，而不是硬塞进去的补丁膏药。

## 发布
- **本次发版版本号统一推进到 `v2.8.37`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.37` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再搁那儿各唱各的调。

---

# Code Switch v2.8.36

## 修复
- **首页供应商卡片余额信息不再另起一大块把卡片撑成小作文**：将非周期余额 / 查询失败面板收进性能信息行内展示，剩余金额、更新时间和刷新按钮现在跟速度、延迟这些指标站在同一排，卡片高度和视觉层级终于别再抢戏了。

## 技术改进
- **余额额度面板改成紧凑内联布局**：重写 `card-balance-quota-panel`、余额项、错误项与刷新按钮样式，去掉原来的大卡片容器阴影和内边距，窄屏下仍自动回落为整行显示，桌面端则保持轻量分隔线和紧凑指标排布。
- **供应商卡片模板顺序收口**：余额成功态与错误态从卡片主体下方移动到 `performance-stats` 区域内，避免额度查询信息和下方操作区互相挤压，后续维护也不用在两个视觉层级之间来回找 DOM。

## 发布
- **本次发版版本号统一推进到 `v2.8.36`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.36` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再搁那儿各唱各的调。

---

# Code Switch v2.8.35

## 功能
- **首页供应商卡片终于能把非周期余额当余额看了**：远端额度查询返回 `valueMode: currency` 且没有重置时间时，不再硬塞进预算进度条，而是在卡片里单独展示剩余金额、更新时间、刷新按钮和健康 / 预警 / 危险色阶；余额查询失败也会显示错误面板，不再静悄悄装没事。

## 修复
- **额度查询失败不再把旧数据一刀清空**：手动刷新失败时保留上一轮成功的远端余额，并追加“刷新失败仍显示缓存”的提示；首次查询无可展示数据时生成可见错误项，避免用户看着空白卡片怀疑人生。
- **单卡刷新不再拉着全场供应商陪跑**：首页卡片刷新按钮只刷新当前供应商，`useProviderQuotas` 新增 `targetRefs` 和刷新状态，不再手动刷新一个额度把同 tab 其他 provider 也一起请求一遍。

## 技术改进
- **远端额度展示模型拆成进度和余额两条链路**：新增 `providerQuotaCardDisplay` 工具和单测，把余额项、错误项、剩余值、相对更新时间与提示文本统一收口；周期额度继续走原进度条展示。
- **额度查询结果带上失败上下文和查询时间**：`resolveProviderQuotaQueryDisplay` 返回 `items / failureMessage / queriedAt`，数据总览弹窗同步适配，首页卡片可稳定展示缓存、错误、更新时间。
- **中英文文案补齐余额查询场景**：补充刷新、失败、空结果、剩余金额和更新时间文案，并把配置说明从 Token Plan 进度条改成“周期额度进度 / 非周期余额直出”的真实口径。

## 发布
- **本次发版版本号统一推进到 `v2.8.35`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本，并准备推送 `v2.8.35` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再搁那儿各唱各的调。

---

# Code Switch v2.8.34

## 修复
- **首页「编辑供应商 → 配置查询」这回把真正的内容初始化雷挖出来了**：空白诊断弹窗验证通过后，已将入口切回原 `ProviderQuotaQueryConfigModal.vue`，并修复额度查询配置文案里的裸花括号触发 `vue-i18n` message 编译错误的问题；打包版里不应再因为 `{baseUrl}` 或 `{ request, extractor }` 这类提示文案把弹窗首帧渲染狠狠干懵。

## 技术改进
- **额度查询弹窗排障过程正式沉淀成文档，后面别再一看遮罩就去莽改 z-index 了**：新增 `doc/provider-quota-query-modal-debug-notes.md`，记录 `v2.8.33` 空白弹窗隔离验证、`v2.8.34` 原内容初始化排查、i18n 报错证据、验证命令和后续排查顺序。
- **清理临时诊断入口并恢复真实配置弹窗链路**：删除 `ProviderQuotaQueryConfigBlankModal.vue`，`Main/Index.vue` 重新导入原 `ProviderQuotaQueryConfigModal.vue`；中英文额度查询提示文案统一改成不含裸花括号的安全写法，避免 `vue-i18n` 把普通代码提示误当插值语法解析。
- **发版元数据继续按同一版号收口**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.34`，避免 tag、更新日志和包内版本继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.34`**：补齐更新日志并准备推送 `v2.8.34` tag 触发 GitHub 自动打包，用真实打包产物验证原额度查询配置弹窗恢复后的表现。

---

# Code Switch v2.8.33

## 修复
- **首页「编辑供应商 → 配置查询」先切到空白诊断弹窗，别再让额度配置内容把排查现场搅成东北乱炖了**：配置查询按钮现在会打开一个最小化的空白 `InlineModal`，用于验证按钮事件、页面级弹窗状态和通用弹窗壳体是否能稳定工作；原额度查询配置内容暂时不参与渲染，方便判断“不显示”到底是弹窗实现问题，还是配置内容加载时把渲染链路干崩了。

## 技术改进
- **额度查询配置弹窗入口改成可回滚的隔离诊断组件**：新增 `ProviderQuotaQueryConfigBlankModal.vue`，保留原 `open / modelValue / providerApiUrl / providerApiKey / close / save` 接口，`Main/Index.vue` 仅切换导入目标，原 `ProviderQuotaQueryConfigModal.vue` 完整保留，后续确认根因后可以直接切回或继续精修内部内容。
- **发版元数据继续按同一版号收口**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.33`，避免 tag、更新日志和包内版本继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.33`**：补齐更新日志并准备推送 `v2.8.33` tag 触发 GitHub 自动打包，用打包产物验证空白诊断弹窗在真实 Wails / WebKit 环境下到底能不能正常显示。

---

# Code Switch v2.8.32

## 修复
- **打包版里的 JSON / 脚本编辑弹窗这回终于不再被 CodeMirror 首帧同步挂载狠狠干懵了**：修复 `JsonCodeEditor` 在 Wails / WKWebView 场景下首次挂载时，`EditorView + basicSetup + 长文本 doc` 的同步初始化可能吃掉父级 modal 首帧绘制，导致配置弹窗面板没及时画出来、编辑区空白或界面只剩模糊遮罩的问题；现在编辑器会延后到下一帧再创建，先让弹窗壳体稳定落地。

## 技术改进
- **通用编辑器挂载时序和入参兜底终于按一套口径收口**：`JsonCodeEditor` 改为用 `requestAnimationFrame` 延后构造 `EditorView`，同时把非字符串 `doc` 统一归一化为字符串并补上构造失败日志，减少配置弹窗在 WebKit / Wails 下因为同步阻塞或脏数据导致的挂载分叉。
- **发版元数据继续按同一版号收口**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.32`，避免 tag、更新日志和包内版本继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.32`**：补齐更新日志并准备推送 `v2.8.32` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各玩各的。

---

# Code Switch v2.8.31

## 修复
- **额度查询配置弹窗这回不再被“延后挂载编辑器”这套时序戏法反噬了**：撤掉 `ProviderQuotaQueryConfigModal.vue` 里 `JsonCodeEditor / CodeMirror` 的 `after-open + fallback timer` 延后挂载逻辑，脚本编辑区在 `Custom / General / NewApi` 模版下直接跟随内容区渲染，避免打包版里出现弹窗已经打开、脚本区却只剩空白占位或挂载时序判断继续绕圈的问题。

## 技术改进
- **额度查询弹窗的编辑器渲染条件正式收口到一处**：`showScriptSection` 现在同时决定脚本配置区和编辑器本体是否渲染，移除 `modalPanelReady / codeEditorReady / timer` 等中间状态，减少 Wails / WebKit 下弹窗打开时的状态分叉，后续排查也不用再跟一堆时序变量狠狠干架。
- **发版元数据继续按同一版号收口**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.31`，避免 tag、更新日志和包内版本继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.31`**：补齐更新日志并准备推送 `v2.8.31` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各玩各的。

---

# Code Switch v2.8.30

## 修复
- **通用弹窗这回不再跟 WKWebView 的 transform 合成层狠狠干架了**：修复部分 Wails 打包版弹窗在进场动画期间，如果初始焦点落到 `textarea` 或其它可编辑控件，可能因为 `transform` 过渡和遮罩 `backdrop-filter` 的合成冲突导致面板不绘制、界面只剩模糊遮罩的问题；现在 `InlineModal` 的进场动画改为仅过渡 `opacity`，并把初始聚焦明确放到 `opacity` 动画结束后执行，弹窗会先稳定画出来，再安全接管焦点。

## 技术改进
- **通用 modal 壳体补上合成层隔离兜底**：全局 `.modal` 样式新增 `position: relative`、`z-index`、`translateZ(0)`、`will-change` 与 `isolation: isolate`，减少 Wails / WKWebView 下弹窗内容层和模糊遮罩互相抢合成层时出现的渲染不确定性。
- **初始聚焦和进场动画终于按同一套口径说话**：`scheduleInitialFocus` 现在只监听 `opacity` 的 `transitionend`，不再继续等一个已经被移除的 `transform` 过渡，避免焦点时序判断和实际动画配置继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.30`**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.30`，并准备推送 `v2.8.30` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各玩各的。

---

# Code Switch v2.8.29

## 修复
- **额度查询配置弹窗这回终于不再被自己那套布局壳子狠狠干沟里了**：修复首页「编辑供应商 → 配置查询」在浏览器 `pnpm run dev` 预览正常、但 Wails 打包版里仍可能只剩模糊遮罩、配置面板继续装死的问题；这次对照“编辑通用配置”弹窗的稳定实现后，将额度查询配置弹窗改回由 `InlineModal` body 直接接管滚动，移除内部多层 `height / flex / overflow` 滚动壳并收窄面板宽度，打包产物里点击后会稳定显示配置面板，不会再被 WebKit 布局合成坑继续拽回沟里。

## 技术改进
- **额度查询弹窗结构正式向已验证稳定的实现收口**：`ProviderQuotaQueryConfigModal.vue` 现在对齐 `CLIConfigEditor` 这类已验证稳定的 modal 壳体，改用更保守的 `bodyScrollable` 和单层内容布局，减少 Wails / WebKit 在二层弹窗场景下对超宽面板、嵌套滚动和 `height: 100%` 组合的渲染不确定性。
- **发版元数据继续按同一版号收口**：同步更新应用常量、构建配置、Darwin `Info.plist` / `Info.dev.plist`、Windows 版本信息与 Linux `nfpm` 版本到 `v2.8.29`，避免 tag、更新日志和包内版本继续各唱各的调。

## 发布
- **本次发版版本号统一推进到 `v2.8.29`**：补齐更新日志并准备推送 `v2.8.29` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各玩各的。

---

# Code Switch v2.8.28

## 修复
- **打包版里“额度查询配置”这回终于不再跟 WebKit 动画互相狠狠干架了**：修复首页「编辑供应商 → 配置查询」在 `pnpm run dev` 浏览器预览正常、但 Wails 打包后仍可能只剩模糊遮罩、配置面板继续装死的问题；这次将额度查询配置弹窗里的 `JsonCodeEditor / CodeMirror` 改为在二层 `InlineModal` 面板进场完成后再挂载，避开 WebKit 在 transform 动画期间同步创建可编辑编辑器实例时的渲染合成 bug，打包产物里点击后会先稳定显示弹窗壳体，再安全加载脚本编辑器。

## 技术改进
- **通用弹窗补上“进场完成”显式信号，编辑器挂载时序彻底收口**：`InlineModal` 新增 `after-open` 事件，业务弹窗可明确感知面板进场结束时机；额度查询配置弹窗则增加脚本编辑器延后挂载与 fallback 定时器，占位骨架和真实编辑器按同一套时序切换，少让 Wails / WebKit 继续在动画期和 CodeMirror 抢渲染控制权。

## 发布
- **本次发版版本号统一推进到 `v2.8.28`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.28` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再各唱各的调。

---

# Code Switch v2.8.27

## 修复
- **额度查询配置弹窗这回终于不再被旧前端产物拖后腿了**：修复首页「编辑供应商 → 配置查询」在部分本地 / 打包产物里仍可能只剩模糊遮罩、配置面板继续装死的问题；这次重新同步最新前端构建资源到桌面端二进制，确保已经切到 `Main/Index.vue` 页面级同级挂载的额度查询配置弹窗真正进入发布产物，点击后会稳定显示配置面板，不会再被旧 bundle 拽回遮罩坑里。

## 技术改进
- **发版链路继续按“源码修复 + 嵌入产物”同一口径收口**：同步应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本到 `v2.8.27`，让本地构建、Git tag 和 GitHub Actions 自动打包都指向同一版资源，避免仓库代码早修好了，最终发布包里却还塞着旧前端的离谱场面。

## 发布
- **本次发版版本号统一推进到 `v2.8.27`**：补齐更新日志并准备推送 `v2.8.27` tag 触发 GitHub 自动打包，继续保证版本号、更新说明和最终产物别再各玩各的。

---

# Code Switch v2.8.26

## 修复
- **打包版里“额度查询配置”终于不再只剩模糊遮罩、面板继续装死了**：参考日志页“日志存储弹窗 → 请求/响应详情弹窗”的稳定实现，这次把额度查询配置弹窗从 `ProviderEditModal.vue` 内部彻底拆出，改为由 `Main/Index.vue` 页面级同级挂载并统一控制打开、关闭和保存回写；现在打包后的 Wails / WebKit 环境下，点击“配置查询”不会再出现界面被糊住、额度查询配置面板却完全不绘制的邪门场面。

## 技术改进
- **额度查询配置弹窗链路正式改成和日志详情弹窗同一套挂载策略**：`ProviderEditModal.vue` 不再在弹窗内部直接渲染 `ProviderQuotaQueryConfigModal.vue`，而是仅发出打开事件并通过 `defineExpose()` 接收保存结果；真正的额度查询配置弹窗改由 `Main/Index.vue` 与编辑供应商弹窗同级渲染，彻底避开“弹窗里再挂子弹窗组件”在 WebKit / Wails 打包态下容易触发的层级、焦点和渲染冲突。

## 发布
- **本次发版版本号统一推进到 `v2.8.26`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.26` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.25

## 修复
- **打包版里“额度查询配置”这回才算真从遮罩坑里爬出来了**：修复上一版只替换了内层额度查询配置弹窗、但外层“编辑供应商”弹窗仍停留在 `BaseModal` 的半截收口问题；现在外层 `ProviderEditModal` 也统一切到 `InlineModal` 链路，打包后的 Wails / WebKit 环境下不会再出现“全局遮罩糊住了、内层额度配置面板却不显示或交互失活”的离谱场面。

## 技术改进
- **供应商编辑链路正式彻底对齐项目现有的 WebView 兼容方案**：`ProviderEditModal.vue` 与 `ProviderQuotaQueryConfigModal.vue` 现在统一走 `InlineModal` 的 `Teleport + modal stack + 延后初始 focus` 方案，彻底避开 `BaseModal` 外层配 `InlineModal` 内层在 WebKit / Wails 打包态下容易触发的层级、焦点和交互冲突。

## 发布
- **本次发版版本号统一推进到 `v2.8.25`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.25` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.24

## 修复
- **打包版里“额度查询配置”终于不再只剩一层糊脸遮罩了**：编辑供应商弹窗中的“配置查询”现已切到项目内统一的 `InlineModal` 链路，内层额度配置面板会直接 `Teleport` 到 `body`，不再被外层 `BaseModal` 的层叠上下文和 WebView 合成层一起带沟里；现在打包后的 Wails / WebKit 环境下，点击后会稳定弹出额度查询配置面板，不会再出现全局模糊遮罩出来了、内容却死活不画的离谱场面。

## 技术改进
- **额度查询弹窗链路正式对齐项目现有的 WebView 兼容方案**：`ProviderQuotaQueryConfigModal.vue` 不再继续走 `HeadlessUI Dialog + translate-y` 的旧 `BaseModal` 套娃链路，统一切到 `InlineModal` 的 `Teleport + modal stack + 延后初始 focus` 方案，后续同类嵌套弹窗再进来，也不容易继续踩 WebKit / Wails 的渲染坑。

## 发布
- **本次发版版本号统一推进到 `v2.8.24`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.24` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.23

## 新功能
- **首页供应商额度查询这回终于不是“Token Plan 单选题”了**：编辑供应商弹窗里的额度配置正式升级为结构化模版系统，补齐 `官方余额 / 自定义 / 通用模版 / NewApi / Token Plan` 全套能力，并以独立弹窗承载配置、测试和保存链路，首页供应商卡片与概览弹窗也能继续复用同一套额度展示，不再只会认一根老旧的 `providerQuotaQueryType`。

## 修复
- **模版切换不再夹带私货、把隐藏凭证偷偷带去跑错接口**：额度配置弹窗在切换 `General / NewApi / Balance / Token Plan` 时会按模版语义清掉无关凭证字段，保存前也补齐脚本为空、NewApi 缺 `userId`、官方余额供应商不支持等阻断校验，少让用户存进一份“表面能保存、运行必炸”的配置。
- **官方余额失败态终于不再被前后端合伙吞掉了**：当 DeepSeek / StepFun / SiliconFlow / OpenRouter / Novita 这类官方余额查询遇到 `401 / 403` 时，后端现在会返回带 `invalidMessage` 的失败态额度项，首页卡片、概览弹窗和配置弹窗测试都会老老实实展示“认证失败”，不再凭空消失得跟闹鬼一样。
- **远端额度缓存不再稳定地展示假新数据**：自动轮询继续只打当前激活供应商，但当远端额度缓存过期且刷新失败时，只允许兜底回退一次旧值；连续失败会清空旧缓存，避免首页长期拿过期额度冒充最新结果。
- **Kimi Token Plan 自动识别终于不再乱点鸳鸯谱**：Kimi 识别规则收窄到明确的 `api.kimi.com/coding` 端点，普通 `api.moonshot.cn` / 常规 Kimi 地址不会再被误判成 Token Plan；同时官方余额模版补上当前命中供应商提示，用户一眼就知道当前 URL 到底识别成了谁。
- **额度相关 UI 这回也一起收了边**：编辑供应商弹窗、额度查询配置弹窗和首页主内容区统一隐藏滚动条但保留滚动能力，查询模版卡片的布局、按钮 margin、提示文案遮挡和内容溢出问题也一并修正，终于不像几块样式互相狠狠干架。

## 技术改进
- **供应商额度查询链路从前端到 Go 后端算是打通成一套正经契约了**：老的单值查询类型升级为结构化 `providerQuotaQueryConfig`，结果对象补齐 `invalidMessage / extra / isValid`，脚本变量注入改为 JS-safe 转义，Go 侧补上 `goja` 依赖，前端 bindings 也同步补齐 quota 相关字段，少让类型和运行时继续各唱各的调。
- **回归测试这回不只补 util 了**：新增并扩展额度查询相关单测，覆盖模版切换字段清理、NewApi 失败态透传、官方余额认证失败展示、Kimi 识别收窄、过期缓存兜底一次后清空等关键路径，后面谁再手痒把这条链改串味，测试会先狠狠干他一锤。

## 发布
- **本次发版版本号统一推进到 `v2.8.23`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.23` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.22

## 新功能
- **前端浏览器预览下终于也能直接打开消费明细弹窗看真东西了**：`pnpm run dev` 运行纯前端时，日志页费用消耗弹窗现在会自动走开发态 mock 数据，支持按当前供应商筛选命中预置演示行；即便输入一个不存在的供应商，也会生成 fallback 预览记录，前端联调不用再被桌面 runtime 卡脖子。

## 修复
- **消费明细弹窗的供应商列表不再挤成一坨、右边还被滚动槽啃半截了**：修复列表行被全局 button 兜底样式误伤后产生的宽度、盒模型和 margin 串味问题，供应商卡片现已明确收口 `margin / box-sizing`，右侧金额区、箭头和卡片边框不会再被挤出可视区。
- **弹窗滚动条总算跟当前暗色玻璃 UI 一个画风了**：费用明细弹窗补上深浅主题滚动条皮肤、稳定滚动槽预留和内容区安全边距，滚动条不再是系统默认那根突兀的家伙，列表滚动时整体观感也更顺眼。
- **消费明细弹窗底部的杂质提示已经清掉，不再抢戏**：移除“End of statement / 点击供应商可快速切换日志筛选”这类页脚提示，让界面在最后一个供应商卡片处干净收尾，少一块没必要的视觉噪音。

## 技术改进
- **开发态 mock 与正式请求链路这回算是分家了**：`useLogsDetailModals.ts` 新增浏览器预览判断、mock 数据构造和 `resolveLogsCostDetailStats()` 收口函数，开发态走 mock、正式态走 `fetchProviderStatsV2()`，少让 UI 组件继续把预览逻辑和生产请求搅在一起。
- **费用明细弹窗的预览链路补上单测保险丝**：新增 `useLogsDetailModals.test.ts`，把开发态 mock 列表、按供应商过滤、fallback provider 以及“不触发后端请求”这些关键行为钉进测试，后面谁再把预览链路改坏，Vitest 会先狠狠干他一锤。

## 发布
- **本次发版版本号统一推进到 `v2.8.22`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.22` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.21

## 新功能
- **日志筛选栏现在会老老实实提醒“你改了，但还没查”**：筛选区底栏新增 `待应用` 状态，用户修改平台、供应商或日期条件但尚未点击查询时，会明确提示当前只是草稿态，不再让页面假装一切都已经同步生效。

## 修复
- **消费明细弹窗和页面 summary 卡片终于不再各查各的了**：日志页新增“已应用筛选”快照后，顶部卡片、图表、自动刷新、分页和消费明细弹窗现在统一基于最后一次成功查询参数；用户改完筛选但没点查询时，不会再出现页面看旧数据、弹窗却偷跑去查新条件的串态事故。
- **总支出金额不再把小额消费硬吞成 `$0.00`**：消费明细弹窗头部总额已收口到和列表相同的金额格式化链路，小于 `1 USD` 的场景也会保留必要精度，避免列表明明有值、头部却装死归零的离谱画面。
- **接口失败时不会再一边报错一边展示假 0 汇总**：消费明细请求报错后，弹窗不再继续渲染伪造的 `Total spend $0.00 / 0 providers` 概览，而是切到明确错误态，少给用户喂错误暗示。

## 技术改进
- **日志筛选状态终于从“表单草稿”和“已应用查询”里分家了**：新增 `appliedFilters / appliedDateRange / appliedQuery` 这套稳定查询态，相关摘要提示也统一按最后一次成功查询参数生成，后续再补图表或详情联动时不用继续在响应式草稿上玩火。
- **消费明细弹窗总算从超胖组件里拆出可测的纯状态逻辑**：新增 `logsCostDetailModalState.ts` 负责金额、占比、高消耗标记和错误态汇总策略，`LogsCostDetailModal.vue` 少背一坨计算细节，后面改样式和交互不至于一动就牵出满屏副作用。
- **这条链路的单测保险丝补上了**：补齐已应用筛选一致性、小额金额精度和错误态展示策略相关测试，后面谁再把弹窗口径改串味，Vitest 会先狠狠干他一锤。

## 发布
- **本次发版版本号统一推进到 `v2.8.21`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.21` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.20

## 新功能
- **日志筛选栏终于从“普通表单三件套”升级成一块能看的控制台面板了**：日志页筛选区重做为暗色科技风卡片，平台、供应商、查询日期类型三组主筛选统一切到自定义下拉交互，补上图标标签、状态信息栏、工具按钮和查询按钮的整体视觉收口，筛选区总算不再像后台默认表单临时拼出来的半成品。
- **供应商下拉正式支持边输边筛，不用再在长名单里肉眼翻垃圾场**：供应商筛选新增模糊搜索输入框，支持按 `label / providerName / value` 组合匹配；“全部供应商”保持置顶，没结果时会显示空态提示，长名称也能完整展示，不会再被窄下拉框硬生生截成半截。

## 修复
- **供应商下拉的层级、宽度、对齐和滚动条这回算是一次性收拾利索了**：修复下拉面板被下方内容遮挡、弹层相对触发框偏左、长供应商名称把面板顶出横向滚动条，以及全局 `button` 样式把伪输入框按钮 margin 顶歪等问题；现在弹层会按内容自适应宽度并在视口边界内自动回挪，交互终于不再东倒西歪。
- **日期次级筛选不再自己跟自己打架了**：修复年 / 月 / 日 / 时间段切换时旧条件残留、次级输入宽度和主筛选列宽不一致，以及 `VueDatePicker` 内置清除按钮和自定义清除按钮同时出现导致一框俩 `X` 的问题；现在切换日期类型会重置旧值，输入框宽度与上方列对齐，也只保留一套清除入口。

## 技术改进
- **日志筛选状态机和表现层总算分工清楚了**：`LogsFilterBar.vue` 重构为自定义下拉 + 动态次级筛选结构，外部点击关闭、`Esc` 收起、Provider 弹层宽度同步与搜索聚焦都统一收口到组件内；`useLogsFilters.ts` 也补上日期类型切换时的字段重置逻辑，少让脏状态继续串到下一轮筛选。
- **样式兜底和文案补全顺手补齐了保险丝**：全局按钮预设现排除日志筛选专用按钮，避免 Tailwind / 全局 reset 再把局部交互带歪；中英文文案补齐了筛选底栏状态、工具提示和供应商空结果提示，后续 GitHub 打包产物里的多语言表现不会再一半中文一半默认文案。

## 发布
- **本次发版版本号统一推进到 `v2.8.20`**：同步更新更新日志，并准备推送 `v2.8.20` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.19

## 新功能
- **可用性监控页面这回总算不像临时拼的管理表了**：监控页重写为深蓝黑卡片化布局，补上全局概览、供应商状态标签、延迟、最后更新时间、自动刷新倒计时、全局“全部检测”和单节点“立即检测”入口；故障卡片还会带轻量呼吸灯提醒，管理员一眼就能看出哪家上游在掉链子。
- **可用历史条终于按设计稿收口成可读的离散健康方块**：历史状态从糊成一条的堆叠实现改成独立色块阵列，hover 会显示对应日期、状态和延迟信息；同时浏览器 dev 预览补上监控供应商 mock 数据，调页面不用再硬等真实后端状态回流。

## 修复
- **健康检查这次不再是“接口回个壳子就算活着”那套糊弄学了**：可用性探测改成真实模型请求与 `pong` 响应校验，响应内容不符合预期时会明确落到 `validation_failed`，并正确计入自动拉黑连续失败次数，少让监控面板看着挺热闹、联动逻辑却在旁边装睡。
- **Claude / OpenAI 兼容探测的边角坑顺手一锅端了**：Claude 探测补齐 `x-api-key + Authorization + anthropic-version` 兼容头，`TestEndpoint` 会统一 trim 后再拼接目标地址，Responses API 的 refusal 文本也不再被误判成成功，省得上游稍微有点兼容性脾气就把监控结果带歪。

## 技术改进
- **手动测试、连通性测试、可用性监测总算往一条共享 probe 链路上收了**：新增统一的 `availability probe` 执行器，按端点形态区分 Anthropic Messages、OpenAI Chat Completions 和 OpenAI Responses，请求体还能继续吃 `modelMapping` 和 `requestBodyOverrides`，后续维护不用再守着三套半生不熟的探测口径互相打架。
- **preset 候选和 retry fallback 也补上了，探测容错终于更像正经发布前的样子**：现在会按模型提示优先尝试 Codex / GPT 风格 Responses preset，并在 `400/404/405/415/422` 或内容不匹配时自动 fallback 到下一候选，必要时再切换 `/responses` 与 `/v1/responses` 等备用端点；网络错误和超时则保持保守，不会为了装聪明把同一个坏链路反复打爆。
- **回归测试把关键容错边界狠狠干实了**：补上 preset 优先级、同路径 body fallback、备用 endpoint fallback、网络错误不重试、`validation_failed` 拉黑联动等定向测试，少让这套探测器后面又被人改回“能跑就行”的散装状态。

## 发布
- **本次发版版本号统一推进到 `v2.8.19`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.19` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.18

## 新功能
- **侧边栏正式升级成暗色玻璃导航控制台**：主界面侧边栏重做为分组式信息架构，首页、提示词、MCP、技能、可用性、测速、环境、日志、控制台和设置现在统一收口到一套暗色玻璃风格；品牌区、导航区和底部版本状态卡终于不再各长各的，整体更接近 AI 工具该有的冷静质感。
- **菜单图标和激活态交互补上微动效**：激活项新增细蓝指示条、淡蓝描边和轻量内发光，图标在 hover / active 态也补上更克制的缩放和 glow 反馈，不再是“点中就糊一坨蓝”，交互重心终于回到整行导航本身。

## 修复
- **侧边栏布局终于不再“左边空一大截、右边还被壳子啃掉半边”了**：收口导航组 padding、分组标题间距和 active item 的宽度约束后，菜单项左侧留白、分组标题对齐和激活边框裁切问题一并修正，不会再出现选中框右侧像被吃掉一截的别扭场面。
- **侧边栏滚动与状态区细节不再互相拖后腿**：修复导航区横向滚动条乱冒、底部状态卡退化成散架文本、在线状态与版本信息层级发虚等问题，侧边栏在窄宽度下也能保持稳定排版，不再像一块拼接失败的半成品面板。

## 技术改进
- **Sidebar 组件正式从“平铺列表”重构成结构化导航壳层**：`frontend/src/components/Sidebar.vue` 现统一管理分组标题、路由激活态、已访问 NEW 标识、在线状态、版本展示和品牌光效，减少后续继续在模板和样式里东补一块西缝一块的维护负担。
- **主界面容器同步补齐暗色壳层与视觉承托**：`frontend/src/App.vue` 为侧边栏和主内容区补上统一的外层 glow、玻璃背景、圆角裁切和响应式留白，避免只重做侧边栏却让主内容还停留在旧容器语义里，继续把页面层级拉成两套画风。

## 发布
- **本次发版版本号统一推进到 `v2.8.18`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.18` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.17

## 修复
- **日志页模型分布卡片终于不再“主题一套、图表一套、文案还夹点英文”各玩各的了**：收口 `LogsChartsPanel` 的 light / dark 双主题样式后，模型分布卡片与趋势图容器现在都会跟着 `isDarkTheme` 一起切换；浅色主题下不再出现整张卡片黑着、图表 legend 和刻度却按浅色文本渲染，导致对比度直接废掉的尴尬场面。
- **模型分布中心文案和 tooltip 不再偷偷往中文界面里塞英文碎片**：`Total Calls`、`req` 这类硬编码已改为走 i18n，中心标签、请求单位和 tooltip 输出现在会按当前语言老老实实显示，不会再出现中文界面里夹着半截英文的串味感。
- **模型分布卡片的配置契约不再“看着能配，其实配不进去”**：Doughnut 图的交互配置正式从父层假透传改为组件内自主管理，避免之前 `LogsChartsPanel` 一边接收 `modelShareChartData / Options`、一边自己偷偷重算覆盖，后面维护时不再容易出现“你明明传了配置，结果压根没生效”的扯淡问题。

## 技术改进
- **日志模型分布面板职责边界重新收口**：父层 `useLogsChartsPresentation` 现在只负责产出 `modelShareRows` 和趋势图数据，Doughnut 图的 hover glow、tooltip、中心锚点和交互态统一下沉到 `LogsChartsPanel` 组件内部，少让表现层和数据层继续互相踩脚。
- **统计口径切换正式补上回归测试保险丝**：新增 `frontend/src/components/Logs/utils.test.ts`，把 `buildModelShareRows` 的 `requests -> tokens -> cost` 排序规则，以及 `formatModelShareTooltipLabel` 按请求数和占比输出的口径钉进 Vitest，后面谁再手痒改回去，测试会先狠狠干他一锤。

## 发布
- **本次发版版本号统一推进到 `v2.8.17`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.17` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.16

## 修复
- **日志统计顶部 4 张 summary 卡片终于不再“外表一套，口径四套”了**：日志页新增统一的 `SummaryRangeV2` 聚合链路后，请求数、成功率、QPS、Token 总量、峰值、缓存收益和费用对比现在都基于同一时间窗口计算；默认 / 历史筛选下不再出现“调用次数按今天算、失败数却是全历史、QPS 还是最近 1 分钟采样”的串味场面。
- **Token 消耗卡终于把 `Cache Read` 当人看了**：比例条从原来的 `Input / Output` 两段升级成 `Input / Cache Read / Output` 三段结构，缓存读取单独拿琥珀色标识并展示占比标签，不再把缓存命中的 token 悄悄塞进总量里却在结构图里装失踪。
- **日志卡片的空态和视觉比例终于不像放大版 demo 了**：summary 卡片现在补齐了 `subtitle / trend / ring value / footer meta / animated` 等关键展示字段，空数据会老实显示 `—` 而不是假装 `$0` 或 `100%`，同时整体布局、留白和卡片宽度也重新收紧到参考稿那套紧凑比例，不再在宽屏上中间挤一坨、两边空得能跑马。

## 技术改进
- **日志汇总正式从前端多请求拼 KPI 收口到后端聚合接口**：新增 `LogService.SummaryRangeV2` 与前端 `fetchLogSummaryV2`，统一输出 `total_tokens / peak_tokens / previous_cost_total / activity_points` 等卡片所需指标；`useLogsPageData` 也补上 summary / stats / model stats 的 requestId 防旧请求回写，少让快速切筛选时旧响应把新状态冲掉。
- **summary 卡片模板与表现层重构成一套可维护结构**：`LogsSummaryCards.vue` 现在按 tone、value size、ratio segments、ring、trend 和 metrics 统一渲染，三色比例条、缓存环形值精度、费用趋势标签与收益动效都落到了模板里，后面再调单卡样式不用继续在模板和计算层之间来回缝补丁。
- **前后端校验链路顺手补强**：Vitest 补上了 `fetchLogSummaryV2` mock 和 stale request 场景，前端 `vue-tsc` / 单测继续全绿；后端 summary 聚合的投影日成本兜底也补成 `NaN/Inf` 安全判断，避免运行时再冒出奇怪的无穷大费用。

## 发布
- **本次发版版本号统一推进到 `v2.8.16`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.16` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.15

## 修复
- **日志页金额口径终于不再“列表一套、汇总一套、图表再来一套”各算各的了**：`StatsRangeV2`、`ProviderStatsRangeV2` 和 `ModelStatsRangeV2` 现统一从请求明细走纠偏后的金额聚合，旧日志和新日志都会优先按 `response_model` 对应价格重算展示，日志列表、汇总卡片、Provider 统计和模型占比终于能对上账。
- **金额计算链路不再偷偷回退到本地模型把账带歪**：后端写日志计费、读日志纠偏以及前端 tooltip / 明细候选链都已移除对本地 `model` 的兜底依赖，缺少 `response_model / requested_model` 时宁可老实承认没命中价格，也不再拿本地路由模型价格硬凑金额。

## 技术改进
- **日志统计聚合正式收口到“明细纠偏后再汇总”**：新增聚合明细加载 helper，让统计接口直接复用 `buildRequestLogList -> applyLogPricing` 这条现成链路，少让金额修正逻辑在列表、汇总和图表里各自拷一份野路子。
- **回归测试把 response model 计价和 fallback 边界钉死了**：补齐 Go 和 Vitest 用例，覆盖日志写入计费、日志读取纠偏、统计聚合、前端价格 tooltip 以及“缺少返回/请求模型时不得回退本地 model”这几条关键路径，后面谁再手痒改回去，测试会先出来狠狠干他一锤。

## 发布
- **本次发版版本号统一推进到 `v2.8.15`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.15` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.14

## 修复
- **首页供应商卡片悬停时终于不再像自己抽一下似的乱窜了**：去掉 hover 态那段会把整张卡片往上拽的 `translateY(-3px)` 位移，只保留边框高亮和边缘发光增强，鼠标移上去现在是稳稳发亮，不会再有那种视觉上抖一下、窜一下的别扭感。
- **Claude 供应商标题区的接口转换提示终于能完整浮出来见人了**：卡片 hover / focus 态会临时放开裁剪并抬高层级，`api-format-badge` 和它的 tooltip 也同步补上更高 `z-index`，所以“开启接口转换”这类说明现在会完整显示在卡片上层，不再被供应商卡片边缘啃掉半截。

## 技术改进
- **供应商卡片的 hover 反馈正式从“位移动效”收口成“阴影与 glow 驱动”**：`provider-card.css` 里的 hover shadow 重新分层，浅色和深色主题都改成更克制的边缘发光组合，同时把底部光晕的缩放变化收住，少让卡片交互继续拿位移假装灵动。
- **首页卡片 tooltip 的堆叠上下文顺手补齐了兜底**：卡片根节点在 hover / focus-within 时会切到可见溢出并建立更高显示层级，接口转换胶囊和共用 tooltip 伪元素也补上独立层级约束，后面再加同类提示时不容易继续被卡片自己的 `overflow: hidden` 反手一刀切。

## 发布
- **本次发版版本号统一推进到 `v2.8.14`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.14` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.13

## 修复
- **首页供应商类型切换在打包版里终于不再“边框只包半截，后面内容悬空裸奔”了**：`MainPlatformTabs` 不再继续赌 `grid + max-content` 在桌面 WebView 里的玄学宽度计算，改为在组件内按 tab 按钮真实像素宽度动态测量并回写容器尺寸，开发态和打包态现在终于按同一套结果老实显示。

## 技术改进
- **首页 tabs 布局正式从“浏览器自己悟”改成“组件自己量”**：新增 `ResizeObserver`、`requestAnimationFrame`、字体加载后复测和窗口 resize 重算，把 `main-platform-tabs` 的专属布局收回组件作用域；全局 `style.css` 里那段会和组件私有布局打架的旧 `grid` 覆盖规则也一并撤掉，少让构建产物和运行时内核继续互相整活。

## 发布
- **本次发版版本号统一推进到 `v2.8.13`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.13` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.12

## 修复
- **首页供应商类型切换外边框这回终于按真实内容宽度老老实实包裹了**：为首页 `MainPlatformTabs` 单独收口 tabs 区布局后，`Claude Code / Codex / Gemini / 其他` 这排标签现在会按内容宽度完整计算外框，不会再出现边框只包住一半、后半截标签被裁掉的别扭场面。

## 技术改进
- **首页 tabs 区正式从通用 flex 挤压逻辑切到专属 grid 收口**：`main-platform-tabs` 现改为 `max-content + minmax(0, 1fr)` 的双列布局，左侧 tab 组独立按内容宽度计算，右侧 controls 单独靠右；移动端再回退为可滚动布局，少让桌面端继续被通用宽度规则拖后腿。

## 发布
- **本次发版版本号统一推进到 `v2.8.12`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.12` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.11

## 修复
- **首页供应商类型切换这回终于不再被自己容器啃半截了**：收口 `tab-group / section-controls` 的桌面端布局约束后，`Claude Code / Codex / Gemini / 其他` 这排 tabs 现在会按真实内容宽度完整展示，不会再出现最后几个标签被裁成半拉的别扭场面。

## 技术改进
- **首页 tabs 布局正式改成“内容宽度优先，移动端单独回退”**：桌面端改用 `inline-flex + max-content + margin-left: auto` 收口 tab 组与右侧 controls 的空间分配，小屏场景再回退为 `100% + overflow-x:auto`，避免继续拿一套互相打架的宽度规则硬糊所有视口。

## 发布
- **本次发版版本号统一推进到 `v2.8.11`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.11` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.10

## 修复
- **首页供应商面板这回终于不再“这儿亮一点、那儿歪半截”各唱各的调了**：统一收口首页 tabs 外框、托管胶囊开关、供应商图标居中放大、当前在用背光、额度进度条渐变、删除按钮悬停细节与深浅主题配色，首页供应商区终于从壳子到卡片都是一套语言，不再东一榔头西一棒子。
- **供应商日志弹窗这回终于更像正经的错误看板了**：弹窗整体重做成暗色玻璃风格，错误卡片左侧状态条贴合边框，首页供应商日志入口补上“今日有错”红点提示，并在前端 `pnpm run dev` 预览时为 Claude `0011` 注入几条模拟失败日志，调样式不用再硬等真实错误。
- **“清除失败日志”不再只会清当前页那几条装样子了**：现在会按当前供应商一次性清空全部失败请求明细，同时继续严格避开 hourly / daily 聚合统计表，终于把“该清的全清、不该碰的别碰”这条边界说人话了。
- **浏览器预览模式下首页托管按钮不再一点击就把 Wails runtime 点炸了**：补上前端 dev 预览环境的 `_wails.environment` 兜底，并为首页托管状态增加 browser preview 本地 mock，托管按钮在纯前端调样式时也能正常切换，不会再报 `Cannot read properties of undefined (reading 'OS')` 这种抽风错误。

## 技术改进
- **首页托管开关正式从通用 `mac-switch` 拆成专属交互组件**：`MainPlatformTabs` 改用独立的 `relay-toggle-switch` 结构和样式，单独管理轨道、滑块、闪电图标、发光和状态动画，避免全局开关样式继续互相连坐。
- **供应商日志清理链路正式升级成“前端语义对齐 + 后端能力补齐”**：前端 `clearProviderFailedRequestLogs` 改成按供应商全量清理，后端新增 `ClearProviderFailedRequestLogs` 并补齐 provider / legacy provider 场景测试，后面谁再把清理范围改回分页凑数，测试会先出来掀桌子。
- **浏览器预览与桌面运行时的边界判断继续往共享 helper 收口**：`ProviderLogsModal`、`useProviderCards`、`useMainPageShell` 与 `main.ts` 现在统一补齐“是否存在桌面 bridge”的判断和前端 dev fallback，少让纯前端预览继续踩 Wails 运行时环境假设的老雷。

## 发布
- **本次发版版本号统一推进到 `v2.8.10`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据、Linux 包版本与更新日志，并准备推送 `v2.8.10` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.9

## 修复
- **首页供应商卡片的暗色主题这回终于不再一到 dark mode 就满屏发白了**：供应商卡片改成显式挂载 `theme-dark / theme-light` 状态类，暗色配色统一切到参考稿那套 `#09090b / #121318 / indigo` 层级，标题、标签、URL、统计行、工具按钮和开关控件都跟着落回深色语义，不再继续赌 `:global(.dark)` 祖先命中链路什么时候抽风。
- **“当前正在使用”的供应商终于和“只是启用着”这俩状态分家了**：首页卡片新增 `is-currently-active` 视觉态，只要当前托管路由命中，或者在关闭代理时处于 direct apply 生效状态，就会拿到独立背光、强化顶部高亮线和图标容器发光效果，终于不再和普通启用态挤成一锅，看一眼就知道现在请求到底走谁。
- **首页外层壳子和控制区的明暗主题也顺手对齐了**：主背景、tabs、全局开关和操作按钮全部改成同一套浅/深色 token，暗色模式下会落到磨砂黑底 + Indigo 霓虹高亮的组合，白色模式则同步做对应反相处理，避免首页壳子和卡片各演各的。

## 技术改进
- **供应商卡片主题命中逻辑正式从“靠祖先 class 猜”收口成显式状态驱动**：`ProviderCard.vue` 直接下发 `theme-dark / theme-light / is-enabled / is-currently-active` 等卡片状态类，样式层改成围绕组件自身状态做选择器收口，少让 scoped CSS + 全局主题链路继续互相拧巴。
- **首页样式 token 又往组件化和可维护性迈了一步**：`provider-card.css` 与 `style.css` 新增主页背景、容器、tabs、按钮、开关等成组变量，后续再调暗色配色、hover 反馈或者 glow 强度时，不用继续满文件翻色值打地鼠。

## 发布
- **本次发版版本号统一推进到 `v2.8.9`**：同步更新应用常量、Darwin `Info.plist`、Windows 元数据、Linux 包版本、构建配置与更新日志，并准备推送 `v2.8.9` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和最终产物别再各唱各的调。

---

# Code Switch v2.8.8

## 修复
- **provider 图标这回终于不再在不同页面里一会儿彩色、一会儿黑白演分裂了**：把供应商图标展示统一收口到同一套 provider 彩色图标映射后，`设置 -> 模型价格`、首页供应商卡片、以及 `编辑供应商` 弹窗里的图标预览现在都会优先吃对应的彩色品牌资源；像 `Azure`、`Anthropic`、`Bedrock`、`Qwen`、`Fireworks`、`Volcengine` 这些图标终于不再到了本项目就像被人抽了色一样一片灰。
- **编辑供应商弹窗里的图标下拉终于不再是“资源很多，但找 provider 图标像翻垃圾桶”了**：图标选择下拉现在会把常用 provider 图标优先排到前面，并统一使用和首页 / 模型价格同一套展示逻辑；用户选 `claude`、`azure`、`openrouter`、`volcengine` 这类图标时，看到的预览和最终保存后的实际展示终于对得上，不再出现“下拉一个样、卡片另一个样”的串味体验。
- **首页供应商卡片和设置页模型价格的图标预加载也顺手收稳了**：provider 相关图标现在会按展示用的彩色资源 key 预加载，不再只预热单色默认 SVG，避免进入页面后图标先黑白糊一脸、再慢半拍切彩的别扭观感。

## 技术改进
- **provider 图标展示能力正式抽成通用工具层**：新增 `providerIconAssets` 工具，把“存储的 icon key”“展示时优先使用的彩色资源 key”“预加载 key 收集”和“provider 图标优先排序”几件事统一收口，少让 `Main/Index.vue`、`ProviderEditModal.vue`、`ModelPricingModal.vue` 各自偷写一套土法逻辑，后面再扩 provider 图标也不用继续满地抄胶水代码。
- **前端回归测试继续朝最容易串味的图标映射补刀**：补上 provider 彩色资源映射、去重收集和 provider 图标优先排序的专项测试，并确认 `providerIconAssets / modelProviders / modelPricingFilters` 相关单测与 `vue-tsc --noEmit` 继续通过，省得后面谁一手把彩色图标逻辑改回黑白遗照风，测试还在旁边装睡。

## 发布
- **本次发版版本号统一推进到 `v2.8.8`**：同步补齐更新日志，并准备推送 `v2.8.8` tag 触发 GitHub 自动打包，继续保证 Git tag、发布说明和最终产物版本别再各唱各的调。

---

# Code Switch v2.8.7

## 修复
- **模型价格页的供应商筛选这回终于按 provider 语义说人话了**：`设置 -> 模型价格` 顶部新增的供应商 tabs 现在优先按显式 provider 前缀归类，像 `azure/...`、`openrouter/...`、`bedrock/...`、`fireworks_ai/...`、`groq/...` 这类模型都会稳定落回各自 provider，不再被尾巴上的 `gpt-*`、`claude-*`、`llama-*` 抢走分类，终于和 `litellmProvider` 筛选口径对上了。
- **模型价格列表的“本地”过滤终于不再把同步覆盖项一锅端了**：原来前端偷懒拿 `is_override || is_custom` 当“仅自定义/覆盖”过滤，结果 `claude_sync / cloud_sync` 这类同步覆盖模型也会被顺手算进去；现在改成只认 `source === manual`，真正把本地手动修改和同步来源分开，用户点“本地”终于不会再看到一堆串味条目。
- **模型价格列表项上的供应商图标和标签也顺手收口到 provider 口径**：列表里展示的标识不再按底层模型厂商瞎猜，而是和顶部 provider tabs 共用同一套前缀优先规则；像 `openrouter/anthropic/claude-*`、`bedrock/.../anthropic.claude-*` 这类嵌套路径模型，终于不会再挂着 Anthropic 标签却被归进第三方 provider 路由里，整出前后不一致的别扭场面。

## 技术改进
- **模型价格 provider 识别逻辑正式抽成独立工具并分成“两段式优先级”**：新增 `modelProviders` 工具，先做显式 provider 前缀匹配，再做模型家族 fallback 兜底，同时把 tabs 顺序收口到当前核心顺序，后面再补更多 provider 前缀时不至于继续把语义搞串。
- **模型价格 source/manual 过滤单独抽成纯函数并补齐回归测试**：新增 `modelPricingFilters` 工具，把 `manual` source 过滤逻辑独立出来，专门补了“`cloud_sync` 且 override 不算本地”的测试；以后谁再想图省事把覆盖层和来源层混成一锅，测试会先出来掀桌子。
- **前端专项回归测试继续朝最容易串味的 provider 路径补刀**：补上 `azure/gpt-4o -> Azure`、`openrouter/anthropic/claude-* -> OpenRouter`、`bedrock/.../anthropic.claude-* -> Bedrock`、`fireworks_ai/.../deepseek-* -> Fireworks` 等定向测试，并确认 `vue-tsc --noEmit` 继续通过，省得这次把语义修正了，下次又被人悄悄改回去。

## 发布
- **本次发版版本号统一推进到 `v2.8.7`**：同步补齐更新日志，并准备推送 `v2.8.7` tag 触发 GitHub 自动打包，继续保证 Git tag、发布说明和最终产物别再各唱各的调。

---

# Code Switch v2.8.6

## 新功能
- **模型价格终于补上“云端同步”这条腿了**：`设置 -> 模型价格 -> 同步` 现在除了 Claude 官方价格外，还新增了云端价格表同步入口，后续补模型价格不用再靠手填或者等人肉搬运。
- **云端同步终于不再是“一把梭过去，manual 自求多福”了**：前端点击云端同步时会先预检冲突，把本地 `manual` 模型单独拎出来给用户勾选覆盖；没勾的继续保留本地价格，勾了的才会转成云端同步结果，终于像个正经的冲突处理流程。

## 修复
- **云端 TOML 解析方式这回真按弹性 schema 口径收口了**：后端不再把价格表硬反序列化成一撮固定字段，而是先按 raw `models` 结构解析，再统一提取当前系统可用的 token/cache/group 字段，并支持 `pricing.*`、`selected_pricing_provider`、`litellm_provider` 的 fallback，后面云端 schema 再长点儿肉，也不至于一变就碎。
- **模型价格来源标签终于不再把 Claude 和云端同步炖成一锅了**：设置页列表现在会明确区分 `Claude 同步`、`云端同步`、`手动修改` 和 `内置价格`，tooltip 也会带上来源和同步时间，排查价格从哪儿来的终于不用靠猜。
- **模型价格编辑弹窗不再拿同步项逗你玩了**：删除/恢复按钮现在只对 `manual` 来源开放，像 `claude_sync` / `cloud_sync` 这类同步项不会再摆出一副“你点我试试”的姿势，省得用户看着像能删，实际只是演戏。
- **云端同步写库不再把主 overrides blob 往死里抡**：模型价格覆盖层现在拆成 `localOverrides + cloudOverrides` 两层，云端同步单独写 `model_pricing_cloud_overrides_v1`，最终效果再和本地层合并；这样既保住 manual / Claude 的优先级，也把大 JSON blob 持续膨胀的风险压下去。

## 技术改进
- **云端同步与 Claude 同步的存储职责终于分清了**：`Claude sync` 继续写 primary/local 层，`cloud sync` 单独写 cloud 层，启动时还会自动把旧 `cloud_sync` 数据从 primary key 迁移拆出去，避免历史数据继续和新逻辑拧巴。
- **Go 侧回归测试终于不再测复制出来的假逻辑了**：补上云端 TOML raw parse、nested pricing fallback、manual 冲突预检、真实 `SyncCloudPriceTable()` 跳过/覆盖 manual 的测试，后面谁再把云端同步改回“表面能跑，细节全糊”，测试会先出来掀桌子。
- **前端冲突弹窗和同步状态机也顺手收稳了**：新增独立的 `CloudPricingConflictModal`，把冲突预检、勾选覆盖、同步执行和状态重置拆开，少让 `ModelPricingModal.vue` 继续长成一锅乱炖面条。

## 发布
- **本次发版版本号统一推进到 `v2.8.6`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备推送 `v2.8.6` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.8.5

## 修复
- **首页供应商列表的刷新按钮这回终于不再只会“刷新个寂寞”了**：当前活动 tab 下，只要供应商启用了 `providerQuotaQueryType`，手动点击首页刷新就会强制重新拉取对应的远端 Token Plan / Code Plan 数据，不再继续吃 5 分钟 TTL 缓存；首页卡片里的额度用量终于会跟你刚点下去的刷新动作对上，不会再表面转了一圈、实际数据半天没动静。
- **切换供应商查询类型或更换凭据后，首页卡片不再抱着旧 plan 数据装死**：远端 quota 缓存现在不只认 `providerRef`，还会同时校验 `providerQuotaQueryType`、`apiUrl` 和 `apiKey`；像 `token_plan_kimi -> token_plan_glm`、或者改了 API 地址 / Key 这类场景，首页卡片会立刻改走新的查询身份重新拉取，不会再在缓存 TTL 内继续显示上一套 plan 用量糊弄人。
- **供应商 quota 刷新排队时序顺手收稳了**：当首页正在刷新额度、又撞上新的强刷请求时，后续请求不再只是悄悄排队然后让上层先结束 loading，而是会并入同一条 refresh promise 跑完整轮队列；这样刷新动画、数据落地和 providers-updated 事件链路终于没那么容易出现“动画停了，数据还在路上磨叽”的错拍体验。

## 技术改进
- **远端 quota 缓存身份正式收口成“查询类型 + URL + 凭据”组合键**：`useProviderQuotas` 里的远端缓存条目新增 identity 校验，彻底堵上“同一 providerRef 下换了 queryType / 凭据却继续命中旧缓存”的暗坑，后面再扩供应商 query 类型时也不容易继续串味。
- **手动强刷与普通刷新语义彻底分家**：首页手动 refresh 现在会为当前 tab 内启用远端额度查询的供应商收集 `forceRemoteRefs`，而自动轮询、切 tab 和倒计时刷新仍然按正常缓存策略走；既把用户主动刷新的预期兑现了，也避免把上游额度接口怼得直冒烟。
- **专项回归测试继续朝 quota 缓存和刷新队列的薄弱点补刀**：补上显式强刷、查询类型切换、凭据轮换以及刷新排队 promise 时序相关前端测试，后面谁再把“改了 queryType 还显示旧额度”这种阴间回归带回来，测试会先出来掀桌子。

## 发布
- **本次发版版本号统一推进到 `v2.8.5`**：同步补齐更新日志，并准备推送 `v2.8.5` tag 触发 GitHub 自动打包，继续保证 Git tag、发布说明和最终产物版本别再各唱各的调。

---

# Code Switch v2.8.4

## 修复
- **GLM 官方额度窗口这回终于不再把“周额度”错扮成 `5h` 了**：`ProviderQuotaQueryService` 现在只有在 `unit` 或窗口文案能提供明确证据时，才会把 GLM `TOKENS_LIMIT` 映射到 `five_hour / weekly`；未知窗口不再脑补成 `five_hour`，中文窗口文案（如 `周额度 / 每周 / 5 小时窗口`）也能稳定识别，首页供应商卡片终于不会再出现“标签写着 `5h`，刷新时间却像周额度”的穿帮场面。
- **GLM 多窗口结果不再原封不动把上游顺序和重复项甩给前端**：远端 quota 结果现在会先在后端按 `five_hour -> daily -> weekly -> monthly -> total` 固定顺序归一，并按逻辑 key 去重合并，避免上游返回顺序飘忽或重复窗口时，首页卡片跟着一块儿抽风。

## 技术改进
- **GLM 额度识别与归一化 helper 正式收口**：新增窗口标签归一化与结果去重排序逻辑，把“中文 / 英文窗口别名识别”“未知窗口宁缺毋滥”和“固定展示顺序”这几条口径统一压回服务层，少让前端继续背锅。
- **专项回归测试继续往 GLM quota 的歪门邪道补刀**：补上 GLM 多窗口乱序、重复 key、中文窗口标签识别以及未知本地化标签不误判的后端测试，后面谁再把周额度硬标成 `five_hour`，测试会先出来掀桌子。

## 发布
- **本次发版版本号统一推进到 `v2.8.4`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备推送 `v2.8.4` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.8.3

## 修复
- **GLM 内置模型价格这回终于按美元口径说人话了**：修正首页 / 设置里 `GLM-5.1 / GLM-5-Turbo / GLM-5 / GLM-4.7 / GLM-4.5-Air / GLM-4.7-FlashX / GLM-4-Plus / GLM-4-Long / GLM-4-Air-250414 / GLM-Z1-Air / GLM-Z1-FlashX` 等模型的内置价格，把之前误把人民币数值当成美元写入默认表的锅收回来；现在前端按 `/1M tokens` 展示出来的价格终于和真实 USD 口径对上，不会再一眼看着就觉得哪儿不太对劲。
- **设置页“模型价格”弹窗不再套娃出两个纵向滚动条了**：模型价格弹窗外层 `InlineModal` 改为不接管 body 滚动，列表区域自身独占滚动容器，并补齐 `flex / min-height / overflow` 约束，终于把“外面一个滚动条、里面一个滚动条，滚着滚着人都麻了”的别扭交互收掉。

## 技术改进
- **GLM 新模型价格表和回归断言统一回到 per-token USD 语义**：同步更新内置 pricing JSON 与 `ModelPricingService` 定向测试，把 `cache read` 的新值也按真实美元换算写回；后面谁再把人民币价格当美元塞进去，测试会先跳出来掀桌子。
- **模型价格弹窗滚动容器层级重新收口**：列表视口改成唯一滚动源，modal body 保持静态布局，后面继续扩筛选、虚拟列表或说明区时，不容易再把滚动链路改成“里外各滚各的”这种阴间手感。

## 发布
- **本次发版版本号统一推进到 `v2.8.3`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备推送 `v2.8.3` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.8.2

## 新功能
- **供应商预算额度现在能直接查官方 Token Plan 了**：编辑供应商时新增“供应商查询类型”，支持为 GLM / Kimi / MiniMax 选择对应查询模式；首页卡片会复用供应商的 API Key 与基础配置自动拉取 Token Plan 额度，并直接复用现有预算额度组件展示，不用再手动盯着后台或自己脑补剩余额度。

## 修复
- **Kimi / MiniMax / GLM 的额度查询地址终于不再瞎拼了**：`ProviderQuotaQueryService` 现在按供应商查询类型走官方查询域策略，GLM 统一落到 `api.z.ai`，Kimi 固定查 `api.kimi.com`，MiniMax 自动区分 `.com / .io`，不再把用户填写的代理子路径或 `moonshot`/兼容地址生硬裁 host 后去碰运气，免得界面看着支持、点开实际全扑街。
- **远端额度查询不再每 30 秒莽着刷上游**：供应商 Token Plan 查询新增 5 分钟缓存，并在额度窗口跨过 reset 时才强制刷新，既保住首页卡片的实时性，也避免供应商一多就把上游接口刷出 429 或风控。
- **数据概览弹窗和首页卡片终于统一成一套额度口径**：供应商启用远端 Token Plan 后，详情弹窗会和首页卡片共用同一份 quota 数据结构；`count` 型额度不再被硬格式化成美元，tooltip、remaining 文案和图表提示全都按真实口径显示，彻底告别“首页一个数、详情另一个数”的穿帮场面。
- **供应商卡片即使“今日暂无数据”也不会把剩余额度一起藏掉了**：卡片空态现在只影响当天请求/Token/成本统计，不再误杀 quota 展示；只要该供应商配置了本地预算或远端 Token Plan，剩余用量和倒计时照样会亮出来，不会再出现“明明有额度，卡片却装死”的憋屈体验。

## 技术改进
- **供应商额度查询链路正式收口成独立服务与共享展示模型**：新增 `ProviderQuotaQueryService`、前端 `providerQuotaQuery` 服务与共享 display helper，把编辑表单、持久化字段、首页卡片、详情弹窗和测试链路全串成一套，后面再扩展新的供应商查询类型也不用继续东一榔头西一棒子。
- **专项回归测试继续盯最容易翻车的边界补刀**：补上官方查询域路由、远端额度缓存 TTL、reset 强刷、概览口径与空态 quota 展示相关测试，后面谁再把 Kimi / MiniMax 查错地址，或者把“今日暂无数据也要显示额度”改没了，测试会先出来掀桌子。

## 发布
- **本次发版版本号统一推进到 `v2.8.2`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备推送 `v2.8.2` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.8.1

## 修复
- **模型价格编辑器终于分得清“没填 cache”和“明确填 0”了**：`设置 -> 模型价格` 里手动编辑价格时，`cache create / cache read` 字段现在会显式携带 presence 语义；未填写时继续按 `input` 默认补齐，明确填 `0` 时则稳定保留免费 cache，不再出现“你明明想走默认，它却偷偷给你写死成 0”或“你都明确填 0 了，结果又被默认价反盖回去”的拧巴场面。
- **免费模型和零缓存模型不再被价格列表当空气**：`ListModelPricing` 现在会按显式字段存在性判断 token 定价，像 `GLM-4.7-Flash` 这类 `input / output = 0` 的免费模型，以及 `cache create = 0`、`cache read` 仍收费的模型，都能稳定出现在设置页列表里，不会再被“值等于 0 就当没价格”这套糊涂逻辑顺手过滤掉。
- **模型价格编辑后再次打开不会把 cache 输入模式带偏**：编辑器现在会按后端返回的 `has_cache_*` 标志回填 `价格 / 倍率` 输入模式，默认补齐模型和显式 `0` 价模型终于各归各位，避免刚保存完再点开，表单显示口径已经跟真实落库状态对不上。

## 技术改进
- **模型价格弹窗切成变量高度虚拟列表**：大价目表现在改用 `buildVariableHeightVirtualList` 做按需渲染，并补上可视区域测量、滚动定位和 overscan，模型一多时打开弹窗和上下滚动不再跟拖水泥似的。
- **内置价格表补上 GLM 新模型梯队**：资源文件新增 `GLM-5.1 / GLM-5 / GLM-5-Turbo / GLM-4.7 / GLM-4.5-Air / GLM-4.7-FlashX / GLM-Z1-Air / GLM-Z1-FlashX` 等内置价格，免费模型和零缓存场景也一并覆盖，后续映射和日志金额展示少靠用户手填。
- **presence 语义前后端统一收口并补齐回归测试**：`PricingEntry`、`ModelPricingRow`、表单 payload 和服务层 apply helper 现在统一传播 `has_cache_creation_input_token_cost / has_cache_read_input_token_cost`，后面谁再把“显式 0 vs 缺省字段”改糊，测试会先出来掀桌子。

## 发布
- **本次发版版本号统一推进到 `v2.8.1`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备推送 `v2.8.1` tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.8.0

## 修复
- **Claude 价格同步这回不再把“官方新模型”当空气了**：`设置 -> 模型价格 -> 同步 -> Claude` 遇到官方价格页里尚未内置映射的模型时，后端现在会自动生成稳定 fallback key 并新增价格项，不再直接跳过，省得官方模型刚上线、你这边同步完还跟没同步一样。
- **这些 fallback 新增项终于按“手动修改”口径落库了**：未映射模型写入 overrides 时统一标记 `source=manual`，列表来源标签、后续编辑删除和候选集筛选不再串味，避免“明明是自动补出来的自定义项，UI 却硬说它是网络同步”这种拧巴场面。
- **Claude 官方价格预览弹窗不再满嘴跑偏**：未映射行现在会明确提示“将新增为自定义模型”，只有连有效目标 key 都生成不了的离谱条目才会提示不会同步，交互语义总算跟真实写库行为对上了。

## 技术改进
- **Claude 价格同步 fallback 规则收口成独立 helper**：新增统一的 fallback model key 归一化逻辑，sync 和 preview 共用同一套口径，少留一份这里 kebab-case、那边还在瞎拼字符串的坑。
- **同步摘要终于不再拿一个 `changed` 糊弄人**：设置页同步完成提示改成 `新增 / 更新 / 未变化` 三段统计，真遇到未识别项也只在“无法生成有效目标 key”时才单独告警，不再把已经自动新增成功的模型还说成“已跳过”。
- **专项回归测试继续往最容易回退的边界补刀**：补上 Go 侧 fallback key / manual source / preview fallback / invalid display name 场景，以及前端 preview target mode 的 Vitest，后面谁再把未映射模型改回空气，测试会先出来掀桌子。

## 发布
- **本次发版版本号统一推进到 `v2.8.0`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，并准备用新 tag 触发 GitHub 自动打包，继续保证仓库版本、Git tag 和发布产物别再各唱各的调。

---

# Code Switch v2.7.99

## 修复
- **Claude 供应商的“直接应用”这回终于不再乱闯禁区了**：当供应商 API 格式切到 `OpenAI Chat Completions` 或 `OpenAI Responses API` 时，首页闪电按钮、编辑弹窗里的“保存并应用”链路都会明确拦截，并给出“当前 API 格式仅支持托管路由，不能直接应用”的提示，避免把只能走托管路由的供应商硬塞进 Claude Code 本地直连配置里，点一下就把自己整懵。
- **后端直连入口也补上了同口径兜底**：`ClaudeSettingsService.ApplySingleProvider()` 现在会在真正落盘前再次检查 `apiFormat`，凡是需要协议转换的 Claude 供应商都会被后端拒绝直连，避免前端校验被绕过后，依然把不兼容的上游配置直接写进 `settings.json`。
- **首页供应商卡片终于把接口转换状态亮出来了**：Claude 供应商一旦启用非原生协议，卡片标题区现在会显示紧凑的接口转换图标，并支持悬浮查看正式说明文案；同时把图标样式收成更克制的胶囊形态，避免标题区被冗长标签挤得发胀。

## 技术改进
- **Claude 直连限制逻辑正式收口成独立 helper**：新增 `providerDirectApply` 工具，统一判断“当前供应商是否必须走托管路由”，卡片按钮禁用、保存后直接应用拦截与后续交互扩展都复用同一套口径，少留那种前后端各写一份、迟早漂移的坑。
- **纯前端开发预览补上了可见的接口转换示例**：`pnpm dev` 的浏览器预览场景下，默认 Claude 卡片会注入两个仅开发环境可见的协议转换示例，方便直接验证 UI 呈现；一旦接入 Wails 桌面桥或真实 provider 数据，这层演示注入就会自动退出，不会污染正式配置。
- **专项回归测试继续朝直连限制边界补刀**：补上前端 `useProviderCards / useProviderForm` 与后端 `claudesettings` 的定向测试，确认“需要协议转换的 Claude 供应商不能直接应用”这条约束在按钮点击、保存并应用和服务层入口三条链路上都能稳定拦住。

## 发布
- **本次发版版本号统一推进到 `v2.7.99`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 已经飞出去了、仓库里还在喊上一版的散装活。

---

# Code Switch v2.7.98

## 修复
- **Claude 供应商编辑弹窗终于把“高级选项 -> API 格式”这块补齐了**：`首页 -> 供应商 -> 编辑供应商` 里的 Claude 表单现在新增可折叠的高级选项区，支持在 `Anthropic Messages / OpenAI Chat Completions / OpenAI Responses` 三种协议之间切换，并且创建、编辑、保存、再次打开时都会按同一字段口径回填，不再出现“界面能选、配置存不住”这种半截活。
- **Claude 转第三方 OpenAI 兼容上游这回不再靠撞大运了**：当 Claude 供应商切到 `openai_chat` 或 `openai_responses` 时，转发层现在会自动把原始 `/v1/messages` 请求改写到对应上游端点，并完成请求体、普通响应和 SSE 流式事件的双向转换；Claude Code 继续说自己的 Messages 协议，上游说 OpenAI 兼容协议，中间这一层终于把人话翻明白了。
- **Claude 的连通性测试和健康检查不再拿旧协议去瞎测了**：连通性测试 / 可用性监控现在会根据 Claude 供应商的 `apiFormat` 生成对应协议的探测请求，并按 `content / choices / output` 各自的成功字段做校验，避免 OpenAI Chat / Responses 风格的 Claude 上游明明能跑，却被检测链路误判成“请求错误”或“内容不匹配”。

## 技术改进
- **Claude 协议探测链路正式收口成统一 helper**：新增统一的 probe request builder，把“按端点 / apiFormat 推导协议、构造测试请求、识别成功字段”这套逻辑从连通性测试和健康检查里抽出来，少写两套分叉逻辑，也少给自己埋两套不一致的坑。
- **前端 provider 持久化边界顺手收干净了**：`apiFormat` 现在只在 Claude tab 下序列化 / 反序列化和表单映射，Codex、Gemini 以及自定义 provider 不会再被顺手塞个默认 `anthropic`，配置结构终于不再带着无关字段到处乱窜。
- **专项回归测试继续往 Claude 协议转换的薄弱点补刀**：补上 Claude 请求规划、probe 请求构造、Responses named event SSE 转换和字段存在性判断测试，尤其把 `output_tokens` 误判成 `output` 这种字符串匹配假阳性先钉死，省得后面谁一图省事又把检测逻辑写回字符串 contains。

## 发布
- **本次发版版本号统一推进到 `v2.7.98`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都飞出去了、包里还在喊上一版的糊涂账。

---

# Code Switch v2.7.97

## 修复
- **供应商终于能配“总额度”这条长期上限了**：首页 `供应商 -> 编辑供应商` 现在在原来的 `5 小时 / 日 / 周 / 月` 四档之外，新增了独立的 `总额度` 配置和已用金额展示，首页供应商卡片也会按同一口径把总额度直接亮出来，不再让“这家供应商总共还能烧多少钱”只能靠脑补。
- **总额度统计不再把重命名前后的老日志漏算半截了**：修复 `CostByProvider / CostSinceByProvider` 之前先把 `providerID` 和 `providerName` 压成一个 ref，导致“新日志带 `provider_id`、旧日志只有 `provider` 名称”时只能命中一头的兼容问题；现在改为 `provider_id` 精确命中 + 历史无 `provider_id` 日志按名称兜底，供应商改名或补 ID 后，总额度终于不会继续算糊涂账。
- **首页 30 秒轮询不再默认拿总额度去硬扫整张明细表了**：`CostByProvider` 现在优先查询 `request_log_stats_daily` 日聚合表，只有老库缺表 / 缺列时才回退 `request_log`，把首页供应商卡片在“配置了总额度”的场景下从明细累加收口到聚合累加，日志量上来后也不至于把首页刷新整得喘粗气。
- **安装包首启对新增总额度字段的兼容回归也补上了**：补齐 `InitDatabase()` 之后再加载 provider / gemini 配置的定向测试，确认这次新增的是 JSON 配置字段，不会把数据库初始化顺序又整劈叉，避免“本地能跑、安装包首启翻车”这种老阴影再来一遍。

## 技术改进
- **供应商额度模型正式补齐 `total` 维度**：前端 `budgetUsage`、首页 quota snapshot、供应商编辑弹窗、供应商卡片与 provider/gemini 配置映射统一支持 `total`，额度展示和持久化口径这回算是彻底对上了，不再一边能配一边不能看。
- **供应商额度统计链路拆成“总额度走聚合、周期额度走明细”两条清晰路径**：总额度读取日聚合，带时间窗口的周期额度继续走 `request_log` 明细表，既保住动态窗口查询的准确性，也避免首页为了一个累计值反复扒拉全量历史。
- **专项回归测试继续往最容易翻车的边界补刀**：新增 provider 总额度兼容匹配、日聚合优先、老库 fallback，以及 `InitDatabase()` 后加载带 `total` 字段配置的后端测试，把这次发版最怕的“兼容老日志 / 兼容老库 / 兼容安装包首启”几条线一并钉死。

## 发布
- **本次发版版本号统一推进到 `v2.7.97`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都推上去了、包里还在喊上一版的散装活。

---

# Code Switch v2.7.96

## 修复
- **日志页模型价格变更终于能被及时感知了**：模型价格在设置页新增、删除或同步后，前端现在会显式广播 `model-pricing:changed` 事件，日志页收到后只标记本地价格表为 stale，不再继续抱着旧缓存硬算，`qwen3.6` 这类刚新增的自定义模型终于不会出现“设置页里有、日志 tooltip 还说未命中价格表”的拧巴场面。
- **日志金额 Tooltip 不再靠 UI 文案结构瞎猜业务状态了**：移除 `info tooltip` 通过 `pricing-line-empty` 这种展示层 row key 反推是否要刷新价格表的土办法，改为依赖显式的 `loaded / stale` 状态判断，后面就算 tooltip 行结构或文案再调整，也不会把价格刷新链路顺手改劈叉。
- **无价格日志不会再每次 hover 都傻乎乎重拉一遍价表**：成本 Tooltip 和模型 Tooltip 现在只会在“首次未加载”或“确实收到价格表变更通知”时刷新模型价格，真正没有匹配价格的历史日志不再每悬停一次就打一枪 `listModelPricing()`，少做无用功也少给自己找抖动。

## 技术改进
- **模型价格服务前端接口补齐事件语义**：`frontend/src/services/modelPricing.ts` 新增统一的价格表变更事件常量与 payload 类型，`upsert / delete / sync` 三条写链路统一发事件，后续如果别的页面也要跟着做缓存失效，不用再各写各的野路子。
- **日志页价格表缓存从“只会加载”升级成“能感知失效”**：`useLogsPricingDetails` 现在把 `modelPricingLoaded` 和 `modelPricingStale` 分开管理，并提供 `markModelPricingStale()` 显式失效入口，缓存状态总算不再全靠 hover 时的临场发挥。
- **前端回归测试补到“价表失效后应自动重载”这条链上**：新增 `useLogsPricingDetails.test.ts`，覆盖初次加载、标记 stale、再次读取后自动刷新到新价格的场景，后面谁再把价表失效通知改没了，测试会先站出来抽脸。

## 发布
- **本次发版版本号统一推进到 `v2.7.96`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 推上去了、包里还在喊上一版的糊涂账。

---

# Code Switch v2.7.95

## 修复
- **首页热力墙默认回到“按天看总览”这条正道了**：前后端默认值、首页初始化态、本地缓存兜底和异常回退口径统一收口到 `daily`，避免首页刚打开或设置未就绪时偷偷按小时渲染，搞出“一格一天还是一格一小时”这种糊涂账。
- **热力墙自动刷新不再在页面隐藏后偷偷诈尸了**：修复首页热力墙定时 reload 在 `deactivated / unmounted` 后仍可能因异步 `finally` 复活的问题，回到别的页面后不再继续后台轮询，请求和内存总算老实了。
- **当前日期高亮与 Tooltip 可读性这回终于像个人样了**：当前格子高亮改成外扩 ring 并补齐边缘留白，最底行 / 最右列不再被裁边；Tooltip 日期也补上年份和星期，跨年窗口里不会再出现两个 `4 月 10 日` 逼你靠猜。

## 技术改进
- **热力墙自动刷新抽成独立控制器，生命周期终于能测也能控**：新增 `heatmapAutoRefresh` 工具，把定时调度、stop 后不再重排和活跃状态判断从组件里拆出来，后面再改刷新节奏时不容易把 keep-alive 生命周期再次玩炸。
- **热力墙宽度估算把 grid 内边距也算进去了**：`useAdaptiveHeatmap` 现在会把 `.contrib-grid` 新增的左右 padding 一并扣掉，列数计算和真实布局终于说同一种人话，不再一边 CSS 收边、一边 TS 还按旧宽度瞎乐观。
- **前端专项测试继续往热力墙薄弱处补刀**：新增 `appSettings` 默认粒度归一化测试、`heatmapAutoRefresh` 生命周期测试，并同步更新自适应热力墙临界宽度断言，后面谁再把默认口径或隐藏轮询改劈叉，测试会先出来抽脸。

## 发布
- **本次发版版本号统一推进到 `v2.7.95`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都推上去了、包里还在喊上一版的散装活。

---

# Code Switch v2.7.93

## 修复
- **首页供应商卡片额度展示不再把 Vue 模板源码直接往页面上怼了**：修复额度区块开始标签丢失，导致 `v-if / class` 模板片段被当正文渲染的问题，供应商额度重新稳定挂回首页卡片 `首 / 速` 指标后面，不再整那种 UI 直接现原形的尴尬活。
- **供应商额度进度条这回终于按真实使用量走了**：修复进度条填充宽度遗漏 `%` 导致视觉表现异常的问题，卡片上的 quota 进度现在按 `used / total` 百分比计算，并补齐使用百分比显示，没超额时不会再假装自己已经跑满。
- **额度倒计时刷新不再卡在旧格式判断里装死**：修复 `useProviderQuotas` 仍依赖 `00:00:00` 旧文案判断刷新时机的问题，额度跨过刷新点后会自动重新拉取统计值与进度状态，避免首页卡片继续抱着过期数据不撒手。

## 技术改进
- **供应商额度展示逻辑从模板临时拼接收口成独立 helper**：新增 `providerQuotaDisplay` 工具，统一封装进度百分比、状态色和展示文案，后续再扩 quota 样式或额度维度，不用继续把计算逻辑硬塞回组件模板里。
- **额度倒计时定时器补齐跨环境兼容和专项测试**：定时器统一改为 `globalThis` 调度，并新增“进度条比例 / 超额展示 / 倒计时跨刷新点”定向测试，把这块最容易边改 UI 边带崩的逻辑先钉死。
- **本次发版版本号统一推进到 `v2.7.93`**：同步更新应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物别再各唱各的调。

---

# Code Switch v2.7.92

## 修复
- **供应商编辑弹窗的额度配置这回终于和托盘弹窗说同一种人话了**：将供应商预算额度区域重做为托盘同款卡片布局，按 `5 小时 / 日 / 周 / 月` 分卡展示预算总额、当前已使用、刷新规则与当前额度上限，避免供应商编辑页和设置页一套东一套西，用户看一眼还得怀疑是不是两个功能。
- **供应商首页卡片额度进度不再“看着像没花过钱”了**：修复额度进度条填充层仍按内联元素渲染导致宽度样式失效的问题，同时把额度展示并到 `首 / 速` 指标后面，并补上使用百分比与更紧凑的人类可读倒计时格式（`xdxh` / `xhxm`），首页终于不再装死。
- **供应商级“当前已使用”这次不再只是个好看的摆设**：补齐 provider / gemini 的 `budgetQuotaUsedAdjustments` 前后端结构与持久化映射，让编辑弹窗里手动录入的当前已使用金额会真正保存下来，并同步参与首页供应商额度进度与百分比计算。

## 技术改进
- **供应商额度编辑链路从“只存配置”升级为“配置 + 校准值”双通道**：前端 `AutomationCard / VendorForm / provider mapper` 和后端 `Provider / GeminiProvider` 统一新增额度校准字段，后续如果还要扩更多供应商级额度状态，不至于再把“显示值”和“可编辑值”混成一锅粥。
- **首页额度展示口径和编辑口径正式打通**：`useProviderQuotas` 现在按“日志统计值 + 手动校准值”计算供应商已用额度，前端还补了专项测试覆盖倒计时格式、进度比例和 adjustment 叠加逻辑，免得下次有人改 UI 时把真实进度又抹没了。

---

# Code Switch v2.7.91

## 修复
- **macOS 安装包启动即退这次算是把雷点揪出来了**：修复供应商级 `5 小时额度` 持久化新增的 SQLite trigger 在 `fmt.Sprintf` 里占位符和参数错位，导致应用启动阶段 `InitDatabase()` 初始化 `request_log` 相关表结构时直接报 `near "%": syntax error`，最终在 macOS arm64 上一打开就闪退。
- **发版版本号链路这回不再各唱各的调了**：补齐应用常量、构建配置、Darwin `Info.plist`、Windows 元数据和 Linux 包版本到 `v2.7.91`，避免仓库版本、包内版本和 Git tag 再出现“表面一个号、包里另一个号”的散装场面。

## 技术改进
- **额度周期 trigger SQL 不再靠肉眼数 `%s` 硬撑**：将全局 / 供应商级 `5 小时额度` trigger 统一改为 indexed placeholder（`%[1]s / %[2]s / %[3]s`），把这类一不留神就多传少传参数的低级坑直接堵上，后面再改 SQL 模板不容易继续把启动链路炸穿。
- **GitHub Release 工作流补上版本同步与 Darwin 元数据校验**：CI 现在会从 tag 同步更新 `build/config.yml`，并在 macOS 打包前校验生成的 `Info.plist` 版本字段，提前拦住“tag 已经是新版本、App bundle 还挂旧版本号”的糊涂账。

---

# Code Switch v2.7.90

## 修复
- **发版打包这回不再被供应商额度类型映射反手掀桌子了**：修复首页供应商额度配置在前端内部 `camelCase` 结构与 Wails bindings 生成的 `snake_case` 持久化结构之间直接混用，导致 CI 在重新生成 bindings 后，`providerCardMappers` / `useProviderCards` 构建阶段出现 `BudgetQuotaSettings` 类型不兼容并直接中止打包的问题。

## 技术改进
- **预算额度的“界面类型”和“持久化类型”正式拆账**：将 `budgetUsage` 里的 quota normalize / serialize 能力收口，前端内部继续统一使用 `refreshTime / refreshWeekday / refreshMonthDay`，真正进入 providers / gemini / app settings 持久化边界时，再统一序列化成 `refresh_time / refresh_day / refresh_month_day`，避免同一份数据结构在 UI 和 bindings 之间来回串味。
- **Provider 加载 / 保存映射链路补齐统一适配层**：新增通用 provider 反序列化与预算额度配置判空逻辑，把普通供应商与 Gemini 供应商的 quota 设置都在 adapter 层归一化，后面再扩字段时不用继续在页面和 composable 里到处手搓兼容。
- **前端专项回归继续补到预算额度序列化边界**：新增额度设置序列化与“全 0 配置视为未配置”的单测，配合既有 `useProviderQuotas` 测试，把这次 CI 才炸出来的类型边界问题提前钉在本地静态检查和单测里。
- **本次发版版本号统一推进到 `v2.7.90`**：同步更新应用版本常量、构建配置和 Windows 元数据，直接用新 tag 重新触发 GitHub 自动打包，别再拿已经失败过的 `v2.7.89` 来回薅。

---

# Code Switch v2.7.89

## 修复
- **首页供应商额度统计与设置这回终于说同一种人话了**：供应商编辑弹窗里的 `5 小时 / 日 / 周 / 月` 额度配置现在会稳定保存到 `Claude / Codex / Gemini` 供应商，首页供应商卡片也会只统计当前供应商自己的消耗，不再出现“设置能填、刷新就丢”或者不同供应商额度串味的离谱场面。
- **供应商卡片上的额度倒计时和刷新时机不再装死**：修复保存供应商、手动刷新和外部 `providers-updated` 事件后额度展示仍停留旧数据的问题；现在会在重新加载供应商后立即刷新额度，`5 小时额度` 未激活时也会明确显示状态，不再留一截空白假装无事发生。
- **供应商级 `5 小时额度` 终于和托盘口径统一了**：修复首页供应商卡片继续靠扫描历史 `request_log` 临时回放周期，导致删历史、改名或补 `provider_id` 后当前窗口可能漂移的问题；现在 provider 级 `5h` 展示改为读取独立持久化周期状态，和 tray 预算说同一种人话。

## 技术改进
- **首页 quota 状态正式按平台分桶，不再拿一张全局表硬扛全部供应商**：`useProviderQuotas` 现在按 `tab -> providerRef` 管理额度缓存，并收口为“先 `loadProviders` 再刷新 quota”的顺序，后面再扩更多平台或出现相同 `providerRef` 时，不容易再被串台问题反手抽脸。
- **provider 级 `5 小时额度` 持久化链路补齐到独立状态表**：新增 `request_log_provider_quota_cycle_state` 及其触发器、回填与身份同步逻辑，让 provider 改名、补 `provider_id` 或历史日志清理后，都还能稳定恢复当前窗口状态，不再让运行时查询每次从头刨历史。
- **专项回归测试补到供应商额度这条新链路上**：新增前端 `useProviderQuotas` 测试覆盖 tab 隔离与 inactive 展示，后端补齐 provider quota cycle 状态相关测试文件，把这次改动从“看起来能跑”往“后面不容易被顺手改炸”又推了一把。
- **本次发版版本号统一推进到 `v2.7.89`**：同步更新应用版本常量、构建配置和 Windows 元数据，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都推上去了、仓库里还在喊上一版的糊涂账。

---

# Code Switch v2.7.88

## 修复
- **首页新增未启用供应商终于不再被甩到未启用列表最底下去了**：修复新增供应商未启用时仍按“组尾追加”处理的问题；现在新建未启用供应商会直接插到未启用分组第一位，启用供应商仍保持追加到启用组末尾，列表语义总算说人话了。

## 技术改进
- **供应商新增入组逻辑正式从“无脑 append”改成按分组语义插入**：将排序工具入口调整为 `insertProviderToStatusGroup`，把“新增时如何入组”和“切换启用状态时如何恢复历史位置”两套语义拆开，避免继续拿一个 helper 硬扛两种完全不同的顺序规则。
- **回归测试补上“新增未启用应置顶”这条漏网路径**：新增排序工具和 `useProviderForm` 两侧测试覆盖，把“新建未启用供应商必须出现在未启用组首位”钉死，省得后面谁手一抖又把它塞回尾巴上。
- **本次发版版本号统一推进到 `v2.7.88`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都打出去了、仓库里还在报上一版的糊涂账。

---

# Code Switch v2.7.87

## 修复
- **模型价格、Skill 仓库、CLI 配置这些技术型输入框，终于不再被浏览器自动纠错瞎掺和了**：为模型价格编辑器、Skill 仓库地址 / 分支输入、共享模型搜索输入和配置文本框统一补上 `autocomplete`、`autocorrect`、`autocapitalize`、`spellcheck` 控制，像模型名、分支名、JSON / YAML 这种本来就不该被“热心纠错”的内容，现在不会再被系统乱改大小写、乱加红波浪线。

## 技术改进
- **技术输入的行为策略这回算是往组件层收了收**：共享 `BaseTextarea` 和 `SearchableModelInput` 统一声明输入辅助策略，减少以后再遇到 Teleport、内联弹窗和原生 `input` 混搭时，这边关了那边没关、修一半漏一半的破事。
- **本次发版版本号统一推进到 `v2.7.87`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 都飞上天了、仓库里还在报上一版的糊涂账。

---

# Code Switch v2.7.86

## 修复
- **设置页“模型价格 -> 新增模型”的模板选择终于不用在一大坨模型名里瞎翻了**：新增模型时的模板选择改成支持模糊搜索的下拉输入，能按模型名做宽松匹配和排序；模型一多时终于能靠搜，不用继续拿原生 `select` 硬顶。
- **首页供应商编辑里的 CLI 模型选择，这回总算从“双轨并行”收口成一个正常控件了**：`CLI 配置` 里的 `CLI model` 字段，以及 `模型映射` 里的内置 CLI 模型键选择，现在都统一成同一个“可模糊搜索 + 可手动输入”的输入框，不再左边选内置、右边再手敲，填表终于不拧巴。

## 技术改进
- **通用模型搜索输入组件正式抽出来了**：新增共享 `SearchableModelInput` 组件和字符串模糊匹配工具，统一处理模型名的分隔符归一、前缀命中、分词命中和子序列匹配，后面再有模型选择场景不用到处复制一坨搜索逻辑。
- **CLI 模型候选集的交互和业务提交边界彻底拆清楚了**：搜索输入组件只负责改草稿值，真正写入 CLI 配置或模型映射仍由各自页面控制，避免“选个候选顺手就把配置偷偷改了”这种阴间副作用。
- **本次发版版本号统一推进到 `v2.7.86`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 已经飞出去了、包里还在报上一版的糊涂账。

---

# Code Switch v2.7.85

## 修复
- **供应商启用后再关闭时，不会再一脚掉到未启用列表最底下去了**：修复此前切到启用组时会丢失原未启用组内位置记忆，导致再次关闭后只能被塞到未启用组尾部的问题；现在供应商在启用组和未启用组都会各自记住自己的历史顺序，来回切换后会优先恢复到原来的组内位置。

## 技术改进
- **供应商排序模型补齐“双分组隐藏顺序”记忆字段**：为前后端供应商数据新增 `enabledSortOrder` 和 `disabledSortOrder`，分别记录启用组与未启用组内顺序；`sortOrder` 继续表示当前所在分组的显示顺序，终于不再拿一个字段硬扛两组历史位置，扛着扛着就扛炸了。
- **排序工具改成“显示顺序”和“历史顺序”一起维护**：切换启用状态时，列表会优先按目标分组的历史顺序插回原位置，只有从未在目标分组出现过的供应商才会落到组尾，既保住“启用置顶”，也保住“来回切换不失忆”。
- **回归测试补上启用开关往返切换场景**：新增“未启用 -> 启用 -> 再关闭必须回到原未启用位置”的测试覆盖，把这种最容易在手动点点点时暴露、但没专门测就容易漏掉的坑先钉死。
- **本次发版版本号统一推进到 `v2.7.85`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种修复都推上去了、产物版本还在装睡的邪活。

---

# Code Switch v2.7.84

## 修复
- **首页供应商拖到位后松手又弹回原位的问题，这次总算把根子掐住了**：修复拖拽预排已经生效，但保存前又按旧 `sortOrder` 重新归一化，导致列表在松手时被错误洗回原顺序的问题；现在启用组和未启用组在各自分组内拖动后，会按当前可见顺序正确提交，不再出现“眼看排好了，一松手又装没发生”的邪门场面。
- **桌面 WebView 下拖拽过程里的假 `dragleave`，不再把有效拖拽误判成离开列表了**：补上基于列表容器命中状态的拖拽在场判断，避免 `TransitionGroup` 重排和原生 DnD 事件抖动把组内有效拖拽错误当成取消，减少“明明还在列表里拖，结果收口走了回滚分支”的离谱情况。

## 技术改进
- **排序工具正式拆成“加载已保存顺序”和“提交当前可见顺序”两套语义**：`applyNormalizedProviderOrder` 继续负责按持久化 `sortOrder` 还原启用 / 未启用分组内顺序，新增 `commitProviderOrder` 专门按当前列表顺序回写组内 `sortOrder`，彻底避免加载语义和保存语义混用把拖拽结果自己打回去。
- **拖拽回归测试补上“旧 `sortOrder` 不同也必须保住新顺序”这条关键路径**：新增针对真实递增 `sortOrder` 数据的单测覆盖，把“预排成功但保存时被旧排序号反向覆盖”的线上根因直接钉死，后面谁再把这块改炸，测试会先出来抽他。
- **本次发版版本号统一推进到 `v2.7.84`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种仓库说修好了、打出来的包还在报上一版的迷惑操作。

---

# Code Switch v2.7.83

## 修复
- **首页供应商拖拽排序这回终于按真正的松手位置收口了**：修复桌面 WebView 下 `dragend`、`drop`、`dragleave` 事件乱序时，卡片明明已经拖到目标位置，松手却又被错误还原回原顺序的问题；现在无论是启用组还是未启用组，只要在各自分组内完成有效拖拽，都会按最终落点稳定保留排序结果。
- **启用置顶和分组内排序规则继续稳住，不会一边修拖拽一边把分组语义修没了**：启用供应商仍然默认整体显示在最顶部，未启用供应商不能跑到启用供应商前面；同时两组各自保留独立组内排序，跨组拖拽依旧被拦下，不再出现“拖拽修好了，分组却乱套了”的反向翻车。
- **拖出列表取消时，不会再把半路预排结果偷偷持久化**：只有真正命中列表内释放，或者收到明确 `drop` 的情况下才会保存新顺序；如果拖到列表外松手，列表会恢复原顺序，不再给你来一手“取消了但还是记账”的阴间操作。

## 技术改进
- **拖拽完成判定改成“源卡片上报坐标，列表容器自己判定是否在列表内结束”**：`ProviderCard` 在 `dragend` 时补充鼠标坐标，`ProviderCardGrid` 基于容器边界计算 `endedInsideList`，不再把最终收口建立在浏览器愿不愿意老老实实按顺序派发事件这种玄学前提上。
- **`useProviderCards` 的收口逻辑正式从时序猜谜切到显式状态判断**：优先依据 `drop`、`endedInsideList` 和分组内真实顺序变化决定是否持久化，仅在拿不到坐标信息时才走旧兜底分支，最大限度兼容桌面 WebView 的原生 DnD 怪脾气。
- **拖拽回归测试继续补齐列表外释放场景**：新增“`dropEffect = move` 但实际在列表外松手时不得保存排序”的覆盖，避免以后有人又把这类最爱在真实环境里抽风、在理想事件模型里装死的边界情况改炸。
- **本次发版版本号统一推进到 `v2.7.83`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种 tag 已经飞出去了、包里还在报上一版的糊涂账。

---

# Code Switch v2.7.82

## 修复
- **首页供应商拖拽排序这回不再被桌面 WebView 的事件乱序拿捏了**：修复 `dragend` 早于 `drop`、甚至 `drop` 丢失时，列表会把已经拖到位的预排结果错误恢复为原顺序的问题；现在启用组和未启用组在组内拖动后，松手会按最终落点稳定保留排序结果，不再来回弹。
- **把卡片拖出列表再松手时，也不会再误把半路预排结果偷偷保存**：补上列表容器级 `dragleave` 状态收口，只有真正还在列表内完成拖拽，或者明确命中 `drop` 时，才会持久化新的排序；拖到列表外取消时，会老老实实恢复原顺序，不再瞎记账。

## 技术改进
- **拖拽完成判定改成 session 化收口，不再靠时间窗和浏览器心情硬猜**：`useProviderCards` 现在按每次拖拽维护独立 session，统一在延后一拍的 `finalizeDrag` 里处理 `drop / dragend / dragleave` 三种时序，避免桌面 WebView 把原生 DnD 事件顺序整得乱七八糟时，前端自己先把自己打回去。
- **回归测试补齐“列表外释放”和“dragend 先于 drop”两条高危路径**：新增拖出列表取消恢复、桌面 WebView 乱序事件收口等测试，把这类最容易线上抽风、单纯靠浏览器理想事件模型又测不出来的坑先钉死。
- **本次发版版本号统一推进到 `v2.7.82`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种修复刚合进去、打出来的包还在报上一版的迷惑操作。

---

# Code Switch v2.7.81

## 修复
- **首页供应商拖拽排序终于不再看“鼠标最后刚好压在哪张卡上”的脸色了**：拖到目标位置后，即便鼠标松开时落在卡片缝隙、列表边缘或 `TransitionGroup` 重排后的空白区域，排序也会按当前真实落点完成，不再出现“明明拖过去了，松手又弹回原位”的邪门场面。
- **启用和未启用两组的组内拖拽都回到正常状态**：因为最终落点改为由列表容器统一计算，启用组和未启用组在各自分组内拖动时都能稳定命中目标前后位置，不再因为单卡 `drop` 丢事件导致整次拖拽白忙活。

## 技术改进
- **拖拽目标解析正式从卡片节点下沉到列表容器层**：移除单张卡片上的 `dragover/drop` 目标判定，新增基于 DOM 边界和鼠标 `clientY` 的统一落点解析逻辑，列表会按所有卡片当前实际位置计算最终插入点，拖拽收口终于不再依赖子节点事件有没有命中。
- **新增拖拽目标解析的纯逻辑回归测试**：补上卡片上半区、下半区、卡片间隙、列表首尾边缘这些边界场景覆盖，把这类最爱在桌面 WebView 里抽风的落点问题先在测试里钉死，省得以后再被拖拽事件时序反手教育。
- **本次发版版本号统一推进到 `v2.7.81`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种修复已经进仓库了、构建产物还在报上一版的糊涂账。

---

# Code Switch v2.7.80

## 修复
- **首页供应商拖到位后一松手不再弹回原位了**：补上拖拽结束阶段的有效落点兜底处理，即便浏览器没有把 `drop` 事件稳稳打到卡片节点上，只要这次释放被判定为有效移动，列表也会按当前拖拽结果保存，不再给你来一手“看着排好了，松手又装没发生”的邪门操作。
- **卡片间隙和容器边缘的拖拽落点终于不再丢单**：给卡片和列表容器统一补上 `dropEffect = move` 协议，并收紧 `drop` 事件冒泡处理，避免拖到卡片缝里或列表空隙时，浏览器把这次排序当空气，最后又被 `dragend` 还原回去。

## 技术改进
- **拖拽排序收口逻辑补上 `dragend` 兜底持久化**：`useProviderCards` 现在会根据 `dropEffect` 判断本次拖拽是否真实完成；命中有效移动时直接按当前预览顺序归一化并持久化，没落成时才恢复原顺序，拖拽预览和最终保存终于说同一种人话。
- **拖拽回归测试继续补齐浏览器事件缺口场景**：新增“`drop` 没触发但 `dragend` 报告为 `move`”的单测覆盖，把这类最容易在真实 WebView 里抽风、在理想化测试里装死的边界情况钉死。
- **本次发版版本号统一推进到 `v2.7.80`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种修复都推上去了、包体版本还停在上一版的糊涂账。

---

# Code Switch v2.7.79

## 修复
- **启用供应商终于老老实实待在最上头了**：首页供应商列表现在始终按“启用在前、未启用在后”分组展示，未启用卡片再也不能蹿到启用卡片前面抢位置，把常用供应商顶下去这种离谱场面算是收拾干净了。
- **启用和未启用两组现在都能各排各的，不再互相拽头花**：启用供应商支持在启用组内独立拖拽排序，未启用供应商也支持在未启用组内调整顺序；跨组拖拽会直接拦下，避免用户手一滑把列表语义拽稀碎。
- **刷新、编辑、新增和切换启用状态后，手动顺序不再被偷偷洗回默认**：首页列表会持久化保存每个供应商的分组内排序，新增供应商或切换启用状态时也会落到目标分组末尾，保证“启用置顶”和“组内自定义顺序”两条规则同时成立。

## 技术改进
- **首页供应商顺序正式切到隐藏 `sortOrder` 字段托底**：前后端统一按“启用状态分组 + `sortOrder` 组内排序”归一化数据，展示顺序不再混用 `level` 这种路由优先级字段，UI 语义和调度语义终于各回各家。
- **拖拽与表单保存链路补齐分组排序回归覆盖**：新增并更新 `useProviderCards`、`useProviderForm` 相关测试，重点钉死“启用置顶、禁跨组拖拽、分组内顺序持久化、Gemini 新增配置完整回写”这几条关键行为，后面谁再手痒改炸，测试先出来抽他。
- **本次发版版本号统一推进到 `v2.7.79`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，别整那种包都发出去了、版本文件还在装睡的破活。

---

# Code Switch v2.7.78

## 修复
- **首页供应商列表终于不再把“未启用”卡片当二等公民了**：未启用供应商现在也能直接拖拽排序，常用但临时关闭的卡片终于能老老实实放在你顺手的位置，不用每次开关状态都跟列表顺序斗智斗勇。
- **刷新、改 Level 和新增供应商不再偷偷把手动顺序洗回默认了**：首页展示顺序现在以用户拖拽结果为准，刷新数据、修改供应商 `Level` 或新增供应商后，都不会再按启用状态或优先级把列表悄悄重排。

## 技术改进
- **首页展示顺序和路由优先级正式拆开，各管各的别瞎掺和**：移除前端加载和表单保存时对 `enabled + level` 的强制重排逻辑，让首页列表顺序专心服务切换效率，`Level` 继续只负责后端路由优先级，避免 UI 和调度语义缠成一锅粥。
- **供应商排序回归测试补齐到完整链路**：新增 `useProviderForm` 顺序保持测试，并把 `codex / gemini / others` 三条刷新恢复路径一起补上覆盖，后面谁再手痒把排序改回玄学行为，测试先出来拍脸。
- **本次发版版本号统一推进到 `v2.7.78`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那种 tag 打出去了、仓库文件还在装睡的邪活。

---

# Code Switch v2.7.77

## 修复
- **供应商编辑弹窗切换对象后，模型映射草稿终于不再串台了**：给模型映射编辑器补上和 CLI 配置编辑器一致的重建时机，关闭弹窗、切换供应商或切平台后，本地编辑态、未保存输入和错误提示会一起清空，不再把 A 供应商的草稿带到 B 身上恶心人。
- **首页供应商模型映射终于能正经编辑已有规则了**：已添加映射现在支持直接进入编辑态并保存回写，不用再靠“删掉重建”这种土法炼钢折腾。
- **CLI 模型选择器不再藏在浏览器联想里装神弄鬼**：供应商模型映射和 CLI 配置里的 `CLI model` 现在都支持“显式下拉选择 + 手动输入”，能从现有内置模型表里直接挑，也保留自定义输入。

## 技术改进
- **内置模型候选集筛选逻辑统一收口**：新增共享 helper，按平台过滤并排序可直接写入 CLI 的内置模型，避免模型映射编辑器和 CLI 配置编辑器各自拷一坨相似逻辑，后面继续改规则终于有一个固定落点。
- **模型映射编辑器的交互状态管理补齐收口**：编辑、删除、冲突提示和外部同步更新现在走同一套状态清理路径，避免界面提示和真实数据各唱各的调。
- **本次发版版本号统一推进到 `v2.7.77`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那种标签先飞了、包内版本还在装睡的邪活。

---

# Code Switch v2.7.76

## 修复
- **首页供应商模型映射终于不再拿“新增”偷偷冒充“编辑”了**：修复模型映射输入同一个 CLI 模型时会直接覆盖旧值的坑；现在重复 key 会明确拦下，想改映射就老老实实先删旧项再新增，避免用户手一抖把已有映射悄悄改没了。
- **CLI 模型配置终于不用只靠手敲硬怼了**：供应商编辑里的 CLI 模型现在支持从现有内置模型表直接联想选择，同时仍保留手动输入能力，配模型不再像默写单词。

## 技术改进
- **CLI 模型草稿提交流程收口到统一提交链路**：补上提交前对模型草稿的强制落盘，不再把最终保存结果赌在浏览器 `change` 事件时序上；现在点保存时，模型选择器和配置文件编辑器会一起按同一套 pending state 收口。
- **内置模型候选集改成“能直接写进 CLI”的干净口径**：按平台过滤模型候选时，排除了 `azure/...`、`openrouter/...`、`vertex_ai/...` 和 `anthropic.xxx` 这类 provider 路由 key，避免把价格表里的供应商前缀模型直接塞进 CLI 下拉，省得用户选完还给自己埋雷。
- **模型映射编辑器的残留错误状态顺手收拾干净**：删除冲突映射或外部同步更新映射列表后，重复 key 的错误提示会及时清理，不再挂着一条过期报错恶心人。
- **本次发版版本号统一推进到 `v2.7.76`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那种刚打完 tag、版本文件还在装睡的邪活。

---

# Code Switch v2.7.75

## 修复
- **首页供应商花费指标终于和其他统计站到同一条线上了**：修复 `花费` 这一组在卡片统计行里基线下坠、像后贴上去一块的排版问题，现在会和成功率、请求数、`Tokens` 这些指标保持同一平面对齐，不再自己单飞。
- **花费标题和金额之间的间距继续收紧**：压缩 `花费：` 与金额之间的空隙，同时同步收紧货币单位和数字之间的节奏，让这一段信息更像 dashboard 指标，而不是散开的两截文本。

## 技术改进
- **供应商花费行的对齐逻辑进一步收口**：将金额容器、标签和金额 parts 的垂直对齐统一调整为同一套 `center` 节奏，避免局部 `baseline` 规则把整行排版拖歪，后续继续微调花费样式时也更容易控住。
- **本次发版版本号统一推进到 `v2.7.75`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那种版本刚发完又对不上的破活。

---

# Code Switch v2.7.74

## 修复
- **首页供应商花费金额显示终于不再把本地化顺序写死了**：修复此前为了拆分货币单位和数字样式，直接把金额固定渲染成“单位在前、数字在后”的问题；现在会保留 `Intl.NumberFormat` 的原始顺序，后续即便扩展到货币后置的 locale，也不会把金额格式整成别扭反人类的样子。
- **供应商花费金额链路对异常数值更稳了**：补上 `cost_total` 的显式数值归一化，遇到 `NaN`、空值或异常输入时会稳定回落到 `0`，避免卡片金额显示逻辑被脏数据带偏。

## 技术改进
- **供应商花费金额显示逻辑抽成独立 helper**：新增本地化金额显示工具，统一输出完整格式化字符串和有序金额 parts，首页卡片不再自己手搓货币拆分逻辑，后续要调样式、语言或货币格式终于有一个固定落点，不用在模板里掰字符串。
- **金额格式化器改为按 locale 缓存复用**：避免首页供应商卡片每次统计刷新时重复 new 多个 `Intl.NumberFormat` 实例，减少无意义对象分配和 GC 压力，这种小活儿不显山不露水，但攒多了是真烦人。
- **金额显示补齐本地化和兜底测试覆盖**：新增针对 `zh`、货币后置 locale 以及异常数值输入的单测，把“顺序不能乱、缓存得生效、脏值要归零”这几条关键行为钉死，后面谁再手痒改劈叉，测试先出来拍脸。
- **本次发版版本号统一推进到 `v2.7.74`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那种发布一版、版本号分裂成三家的活。

---

# Code Switch v2.7.73

## 修复
- **首页供应商花费金额终于不再被全局按钮样式带歪了**：修复 `花费` 金额触发器仍被全局 `button` reset 强行加上大块 padding 和行高的样式串扰问题，现在金额会按预期显示为紧凑的文字型点击入口，不再和标题隔得老远，看着像个跑错片场的小按钮。
- **累计消费弹窗的标题层级不再自己和自己打架**：移除弹窗头部重复出现的图表标题装饰，只保留 modal 标题和图表模块标题两层关键信息，避免一屏里三处都在强调“我是标题”，视觉和语义都更清爽。

## 技术改进
- **金额触发器正式加入全局按钮样式豁免名单**：从全局 `button` 样式修正规则里排除首页供应商花费入口，后续这类“看着像文本链接、语义上又必须是 button”的组件终于能按局部样式正常生效，不会再被 `!important` 生吞。
- **累计消费弹窗信息层级继续收口**：保留供应商摘要、核心统计卡和图表区三层结构，把重复说明和装饰节点压缩到最少，让 dashboard 弹窗更像一个正经的数据模块，而不是几个标题凑热闹。
- **本次发版版本号统一推进到 `v2.7.73`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整那些版本号互相装不认识的邪活。

---

# Code Switch v2.7.72

## 新功能
- **首页供应商花费金额现在可以直接点开“累计消费金额折线图”了**：正式把这条交互接进首页真实供应商卡片，不再是独立 demo 页面；点击卡片里的 `花费` 金额后，会弹出基于当天请求日志生成的 ECharts 累计消费折线图，支持 tooltip、dataZoom、平滑折线与面积阴影，终于不是看个图还得另开示例页。

## 修复
- **快速切换供应商时累计消费弹窗不再串图**：补上弹窗请求代次隔离，避免旧请求慢吞吞回写，把新打开供应商的累计消费曲线顶掉，解决“明明点的是 B，弹出来却像 A”的竞态串台问题。
- **高请求量供应商的今日累计消费不再被静默截断**：移除之前最多只拉取固定页数日志的硬限制，当前供应商今日请求日志现在会按分页完整拉完，累计金额和记录笔数终于能和真实当天数据对上，不再偷偷少算一截还装作没事。
- **图表面积阴影颜色不再靠字符串硬拼 alpha 碰运气**：新增颜色归一化逻辑，统一兼容 `#RRGGBB`、`#RRGGBBAA`、`rgb(...)` 和 `rgba(...)`，避免遇到 8 位 hex 或非 hex 颜色时把 ECharts 渐变颜色拼成非法值，导致图表样式抽风。

## 技术改进
- **累计消费趋势数据转换补齐测试覆盖**：新增趋势转换与图表颜色处理的单测，覆盖排序累加、异常金额兜底和颜色安全转换这几条关键路径，后面谁再把这块改劈叉，测试会先出来掀桌子。
- **首页花费金额按钮补齐可访问性语义**：点击入口新增更明确的 `aria-label` 和本地化文案，辅助技术不再只听到一个孤零零的金额值，知道这玩意儿点下去是“查看今日累计消费趋势”。
- **本次发版版本号统一推进到 `v2.7.72`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，继续保证仓库版本号、Git tag 和 GitHub 自动打包产物是一套数，不整版本号各唱各的调。

---

# Code Switch v2.7.71

## 新功能
- **新增供应商累计消费金额折线图前端示例页**：补上一份可直接运行的 dashboard 风格示例，完整演示“点击首页供应商花费金额 -> 弹出弹窗 -> 展示今日累计消费金额折线图”这条链路，图表基于 ECharts，包含 tooltip、dataZoom、平滑折线、面积阴影和金额格式化，可直接拿来当接入模板。

## 修复
- **示例页不再依赖外部 CDN 才能活**：将 ECharts 改为本地静态资源加载，同时移除远程字体依赖，避免桌面 WebView、离线环境或公司网络策略一收紧，示例页就直接白屏装死。
- **示例渲染不再拿字符串拼 HTML 裸奔**：将供应商卡片和摘要区域从 `innerHTML` 拼接改成受控 DOM 构建，补上基础转义与错误兜底，避免后续接真实供应商数据时把 XSS 风险请进门。
- **时间与累计金额计算链路更稳当**：原始记录现在会先做时间规范化和排序，再生成 `cumulativeCost`；同时去掉对固定时间字符串切片的硬编码，避免后续换成 ISO 时间或带时区格式后 UI 当场说胡话。
- **图表弹窗补齐空数据和资源异常兜底**：当 ECharts 资源缺失或当天没有有效消费记录时，页面会展示明确状态提示，并在关闭弹窗时释放图表实例、恢复焦点，不再留下半死不活的交互尾巴。

## 技术改进
- **发布内容补齐本地化示例资源**：前端公共静态目录新增本地 ECharts 构建文件，示例页可以直接跟随仓库发布，避免发版后还要赌第三方脚本地址和网络环境脸色。
- **本次发版版本号统一推进到 `v2.7.71`**：同步更新应用版本常量、构建配置、Windows 元数据和 Linux 包版本，保证仓库内版本号、Git tag 和 GitHub 自动打包产物不再各唱各的调。

---

# Code Switch v2.7.70

## 新功能
- **供应商编辑新增请求体强制字段能力**：现在可在供应商编辑中配置请求体字段的强制覆盖或新增内容；仅当请求实际转发到当前供应商时，才会将这些字段写回请求体对应位置，避免别家供应商跟着一起“被连坐”。

## 修复
- **强制覆盖 `model` 后的供应商筛选终于按最终模型说人话了**：修复此前供应商兼容性判断仍拿覆盖前模型做筛选的问题；现在会先生成最终请求体，再基于最终模型判断当前供应商是否支持，避免明明配置了覆盖却在路由前就被旧模型提前筛掉。

## 技术改进
- **请求体预处理逻辑统一收口到共享请求计划**：标准供应商、自定义 CLI 和 Gemini 三条链路现在统一复用请求体覆盖与最终模型解析逻辑，减少重复分支，也降低后续再把路由判断改劈叉的回归风险。

---

# Code Switch v2.7.69

## 修复
- **首页供应商托管动态点存在感进一步增强**：将托管指示点放大并提高亮度，在供应商卡片的深色背景上更容易被扫到，不再像之前那样得盯着看才知道它在动。

## 技术改进
- **托管动态点波纹节奏和对比度继续调优**：同步提高 pulse 与 idle 两种动画的波纹密度、光晕强度与暗色模式对比，让“当前命中”和“默认候选”的动态反馈既更明显，又仍保持强弱分级。

---

# Code Switch v2.7.68

## 修复
- **首页供应商“托管”标签风格重新拉回统一体系**：移除之前过于突兀的亮绿渐变贴片效果，改回与 `L1 / BL0 / 默认` 同一套扁平 badge 风格，只保留更高辨识度的绿色底色与动态指示点，避免整排标签里就它一个像跑错片场。

## 技术改进
- **供应商状态标签视觉语言进一步收口**：同步压平托管标签在亮色和暗色主题下的背景、边框与阴影层级，让首页供应商列表中的状态标签更像一套系统，而不是临时拼起来的几个小牌子。

---

# Code Switch v2.7.67

## 修复
- **首页供应商托管标签终于更像“正在工作”的状态了**：将 `托管` 标签进一步提亮为更醒目的亮绿色，并恢复动态指示点，让托管态在供应商列表里不再闷得像没开一样。
- **动态图标按实时态与默认态分级展示，不再一刀切**：修复此前为了避免误导而把默认态动态指示点直接砍掉后，导致整组托管标签看起来发木的问题；现在真实当前命中会显示更明显的 pulse，默认候选则保留更轻的呼吸提示。

## 技术改进
- **托管标签的亮暗主题细节继续打磨**：同步强化亮色主题下绿色托管 badge 的渐变和投影，并补齐暗色主题下的绿色高光与默认蓝色对比，让两种状态在深浅主题里都更容易一眼区分。

---

# Code Switch v2.7.66

## 修复
- **首页供应商托管标签颜色层级重新校准**：将 `托管` 标签提升为更鲜亮的绿色，并把 `默认` 标签切换为原先托管态的蓝色风格，让“实时托管中”和“默认候选”在视觉上更容易一眼分清。
- **默认路由状态不再冒充实时托管状态闪烁**：修复默认态也会带绿色脉冲点、容易让人误以为该供应商已在实时处理中转流量的问题；现在只有真实当前命中时才显示脉冲动效。

## 技术改进
- **托管标签暗色样式质感补强**：同步微调 dark mode 下绿色托管标签的内阴影，以及蓝色默认标签的边框存在感，让这组 badge 在暗色主题里不发飘、不糊成一片。

---

# Code Switch v2.7.65

## 修复
- **首页供应商托管状态标签不再横着抢版面**：将 `托管 / 当前 / 默认 / 上次` 这组状态标签收进供应商标题行，并放到 `L1 / BL0` 等调度标签后面，避免原先单独占一行把卡片头部撑散。
- **托管状态标签的说明和可访问性不再缺半截**：补齐整组状态标签共享的 tooltip 与 `aria-label`，鼠标悬停和辅助技术读取时都能拿到完整状态说明，不再只有一半标签带解释、一半靠猜。
- **“上次” 标签不再和实时状态抢戏**：将历史命中状态从更扎眼的样式收成弱化标签，降低视觉优先级，避免用户把历史命中误读成当前正在使用。

## 技术改进
- **首页供应商状态标签组排版进一步收口**：托管状态 badge 组改为同一语义块内不拆行显示，并同步微调字号、间距、边框和暗色模式表现，让状态标签和 `L1 / BL0` 这排信息在视觉上更像一套系统。

---

# Code Switch v2.7.64

## 新功能
- **首页供应商列表补上托管态“当前路由 / 默认路由”可视标识**：托管开启后，供应商卡片现在会直接展示当前实际命中的供应商；在尚未产生实时路由记录、且未开启 `round robin` 时，还会额外标出按当前优先级最先尝试的默认供应商，首页终于不用再靠用户自己猜流量到底走了谁。

## 修复
- **托管态供应商标识改成走真实运行时路由，不再拿旧命中记录硬充当前状态**：后端新增 `provider:routed` 事件，只在实际成功路由后广播当前命中的供应商；前端则在轮询、窗口聚焦和事件回调时同步刷新状态，避免“明明这次没走它，卡片还挂着当前路由”的假现场。
- **自定义 CLI 工具的托管命中状态不再在 `others` 标签页里串台**：修复后端把平台写成 `custom:<toolId>`、前端却只按 `others` 一锅端的状态错配问题；现在会按 `tool_id` 精确过滤，只在当前选中的自定义工具下显示对应命中供应商。
- **`round robin` 模式下不再误显示默认供应商**：修复轮询模式仍然把首个可用供应商硬标成“默认路由”的误导展示；现在只在顺序尝试模式下才展示默认供应商提示，避免 UI 说人话说一半。
- **托管命中状态在后端无返回结果时会及时清空，不再残留上一轮高亮**：修复前端仅在拿到非空结果时才重置 `lastUsedProviders` 的问题，防止后端当前无命中数据时，页面还挂着上一次的“最近命中 / 当前路由”残影。

## 技术改进
- **托管供应商状态判断拆成共享 helper 并补齐测试**：新增 `providerRoutingState` 与 `lastUsedProvider` 工具，统一处理默认路由选择、custom CLI 平台归一化和工具级过滤；相关单测已覆盖默认供应商选择、`custom:<toolId>` 映射以及 `round robin` 相关边界。

---

# Code Switch v2.7.63

## 修复
- **首页供应商“当前路由”标识不再把过期状态当成实时状态硬贴脸上**：修复托管开启后仅凭最近一次命中记录就显示“当前路由”的问题；现在只有供应商仍处于可路由状态（已启用、密钥和地址完整、且未被拉黑）时才会显示“当前路由”，否则会退回“最近命中”。
- **首页供应商托管状态动效现在会尊重系统的 reduced motion 设置**：修复状态脉冲点无条件无限播放的问题；当系统偏好减少动态效果时，这个提示会停掉，不再瞎扑棱。

## 技术改进
- **托管路由状态判断收口到共享 helper**：新增 `providerRoutingState` 工具，统一管理“托管开启 + 最近命中 + 仍可路由”这条判断链，避免组件里再散着写一坨半截逻辑。
- **补齐首页供应商路由状态单测**：新增针对托管开关、供应商禁用、缺少凭据和黑名单场景的测试，后面谁再把“最近命中”写回“当前路由”，单测会先把他拦下来。

---

# Code Switch v2.7.62

## 修复
- **日志查询切到“按年”时会自动回到本年**：修复之前年份筛选会残留上一次选中的旧年份，来回切换 `year / month / day` 后容易查着查着跑偏的问题；现在每次切到“按年”都会直接选中当前年份。
- **日志年份筛选范围不再是 UI 一套、查询校验一套**：修复年份选择器虽然已经改成“当前年份上下各 10 年”，但状态层和查询层还放行 `1970..9999` 的历史包袱；现在可选范围、回填校验和查询构造统一收口到同一套年份窗口规则。

## 技术改进
- **日志年份规则提炼成共享 helper**：新增统一的当前年份、年份窗口和合法性判断工具，避免组件层、composable 层各自写一份判断，后面谁再改年份逻辑也不至于东一榔头西一棒子。
- **补齐日志筛选器年份切换回归测试**：新增 `useLogsFilters` 单测，覆盖切到 `year` 自动选本年、年份窗口边界校验，以及围绕当前年生成可选范围这几个关键场景。

---

# Code Switch v2.7.61

## 修复
- **首页供应商日志“清除失败日志”不再有误删成功日志或其他供应商日志的风险**：修复之前清理动作只按日志 ID 删除、后端没有锁死失败状态和供应商身份约束的问题；现在删除必须同时命中当前供应商、失败状态和当前弹窗已展示的日志 ID。
- **首页供应商日志清理范围收紧为“当前已展示失败明细”**：现在只会删除当前弹窗已加载出来的失败请求明细，不再扩大到未展示的旧失败记录，也不会碰 `hourly / daily` 统计汇总。

## 技术改进
- **失败日志清理接口改成“供应商身份 + 日志 ID + 失败状态”三重校验**：后端新增更严格的删除接口，并把返回结构从带 stats 的旧结果中拆分出来，后续复用这条链路时不容易再把语义带歪。
- **补齐成功日志 / 跨供应商误删回归测试**：新增针对重复 ID、非法 ID、legacy provider identity，以及把成功日志或其他供应商日志 ID 混进删除请求的测试，避免这条清理链路后面再被谁改回裸删。

---

# Code Switch v2.7.60

## 修复
- **首页供应商日志“清除占用”不再点了像没点一样**：修复该按钮之前依赖浏览器原生 `window.confirm`，在桌面 WebView 环境下可能根本不弹确认框、导致用户体感像按钮失效的问题；现在改成应用内确认弹窗，确认、执行、反馈整条链路都在站内完成。
- **供应商日志清理确认不再可能被宿主环境吞掉**：清除动作现在统一走项目已有 modal 栈和按钮状态控制，避免系统弹窗被遮挡、无响应或焦点异常时把清理交互整成哑炮。

## 技术改进
- **供应商日志清理流程切到可控的站内确认流**：弹窗内部新增独立确认 modal，清理中状态、关闭保护和结果 toast 都走同一套前端状态机，后面继续扩这个操作链路不用再赌宿主对原生 confirm 的脸色。

---

# Code Switch v2.7.59

## 新功能
- **首页供应商日志弹窗新增“清除占用”快捷入口**：现在在“已展示 xx 条”旁边可以直接清掉当前供应商对应的请求日志与统计占用，清理完成后弹窗会自动刷新，不用再绕去日志存储页翻供应商明细。

## 修复
- **首页供应商日志弹窗 dark mode 终于不再靠运气吃主题**：修复弹窗之前内部卡片想走暗色、外层 modal shell 和标题栏却可能继续沿用浅色壳子的割裂问题；现在外壳、标题栏、关闭按钮、hero 区、日志卡片和代码块都绑定到同一套主题状态，黑夜模式下整体层次总算统一了。
- **系统主题切换和跨窗口主题同步时，首页供应商日志不再串回错误配色**：修复首页 `keep-alive` 缓存场景里，DOM 上的主题 class 已切换但弹窗仍可能拿着旧 `resolvedTheme` 渲染，导致页面黑了弹窗还亮着的问题。

## 技术改进
- **主题状态收口到统一 ThemeManager 事件链**：新增已解析主题广播与订阅能力，首页不再自己维护一份半截子的主题快照，供应商日志弹窗改为跟随真实已应用主题响应更新。
- **InlineModal 的 panel class 扩展口升级为标准 class 形态**：弹窗外壳样式现在支持字符串、数组和对象类名，供应商日志 modal 用结构化 class 接入主题壳层，后面其他弹窗复用这条能力也更顺手。

---

# Code Switch v2.7.58

## 新功能
- **日志存储弹窗新增“供应商日志占用”统计与清理能力**：在日志存储明细与统计汇总之上补充按供应商聚合的日志占用估算，能直接看到各供应商请求明细 / 汇总统计大概吃了多少空间，并支持按供应商一键清理，不用再靠猜。

## 修复
- **首页供应商日志 dark mode 卡片和文字配色终于不再白得扎眼**：修复首页供应商日志弹窗在黑夜模式下卡片背景、文字层级和提示区配色还沿用浅色方案的问题，整体视觉现在和首页黑色主题对齐，信息可读性也稳当多了。
- **legacy 空 `provider_id` 的日志不再把统计和清理链路搅散**：修复旧数据里 `provider_id` 为空时，供应商日志占用会被拆成多份、按供应商清理还删不干净的问题；现在后端会统一归一化 provider identity，统计口径和删除口径总算是一套了。

## 技术改进
- **后端补齐按供应商聚合的日志存储统计 / 清理接口**：围绕 `request_log` 与统计汇总新增统一的供应商维度聚合与清理能力，前后端对“供应商日志占用”这件事终于不是各说各话。
- **补上 legacy provider identity 的回归测试**：新增针对空 `provider_id` 场景的服务层测试，防止后面谁再把聚合键和清理键改成两套，回头又得在脏数据里挖雷。

---

# Code Switch v2.7.57

## 修复
- **首页供应商错误详情匹配不再只会盯着“下一行”瞎猜**：修复首页供应商日志之前只会把控制台里紧跟在 `Upstream ... url=...` 后面的单行 JSON 当成候选 continuation 的问题；现在会在短时间窗口内扫描并合并连续的多行 JSON 错误体，`header + pretty JSON` 这类日志也能还原出真正的 provider error。
- **首页供应商日志优先命中真实错误正文，不再让 `url=...` 元数据抢戏**：修复控制台里同时存在 metadata-only 候选和真实 provider error 候选时，首页有机会选中 `url=... content_type=...` 这类伪详情的问题；现在候选会按错误正文质量加权，`No available providers` 这类真实消息会被优先采用。

## 技术改进
- **供应商错误候选构建与匹配逻辑从页面里抽离成纯函数模块**：把首页弹窗里分散的 console candidate 解析、continuation 合并和匹配评分收口到独立 helper，页面代码少了一坨临时判断，后面继续修这块不至于把 UI 和解析逻辑一起搅成浆糊。
- **补齐首页错误详情回归测试，覆盖多行 JSON 与候选择优场景**：新增针对 `header + 单行 JSON`、`header + 多行 pretty JSON`、以及 metadata-only / rich error 同时存在时的匹配优先级测试，后面谁再把这条链路改歪，单测会先给他一巴掌。

---

# Code Switch v2.7.55

## 修复
- **首页供应商错误详情不再被伪 payload 带沟里**：修复部分失败日志把 `url=... content_type=...` 这类上游请求元信息错当成“供应商错误详情”的问题；现在首页会先识别这类伪 payload，并继续回退匹配控制台里的真实 provider error，像 `No available providers` 这类正文终于能老老实实显示出来。
- **带花括号的元信息不再冒充结构化错误正文**：修复错误解析器之前只要看到 `{...}` 就容易当成嵌入 JSON 的漏洞；现在只有真正能成功解析的 JSON 片段才会被视为结构化 payload，`headers={x-request-id:...}` 这类日志边角料不会再把详情链路带偏。

## 技术改进
- **供应商日志 payload 解析结果改成单次计算、全链路复用**：首页供应商日志弹窗现在把 `response_body` 的解析状态收口成 `PayloadErrorState`，不再在“是否走 console fallback”和“最终渲染详情”两段逻辑里重复 parse 同一份文本，代码路径和性能都顺眼了不少。
- **供应商错误解析补齐元信息场景回归测试**：新增“纯元信息伪 payload”“元信息前缀 + 真 JSON”“带花括号但并非 JSON 的元信息”这几类测试，避免后面谁再把这块改回见括号就信、见字符串就上墙的状态。

---

# Code Switch v2.7.54

## 新功能
- **首页供应商日志补齐“供应商错误详情”查看与复制能力**：现在从首页点开单供应商日志弹窗后，每条失败日志都可以直接查看对应的供应商错误详情，并支持一键复制，不用再跑去控制台手搓 JSON 捞报错正文。

## 修复
- **首页供应商日志不再动不动装死说“没采集到返回体”**：修复首页弹窗之前只认 `request_log.response_body`、看不到控制台里真实供应商错误详情的问题；现在会在缺少请求返回体时，按供应商、状态码、模型和时间窗口去兜底匹配控制台错误日志，把能还原的详情尽量还原回来。
- **分页更深时供应商错误详情不再越翻越丢**：修复供应商日志弹窗只抓最近一小撮 console logs、导致加载更多旧失败日志后越来越容易匹配不到详情的问题；现在会根据当前日志时间范围和未命中情况，按需扩展到更完整的控制台日志覆盖范围。
- **单供应商错误详情匹配进一步收紧，减少串台误认**：修复时间戳异常或高频同状态码失败场景下，首页供应商日志可能把同供应商别条错误详情错贴过来的风险；现在缺少可靠时间信息时，至少还要同时命中模型线索才会采用控制台兜底详情。
- **供应商日志暗色样式重新压实，和首页黑色主题不再打架**：修复弹窗卡片、提示区和代码块在 dark mode 下发灰发亮、像没融进整站视觉体系的问题；现在整体色板和层次更贴近首页黑色模式，错误详情区的可读性也更稳。

## 技术改进
- **供应商日志解析改成“请求日志优先，控制台兜底”的双源策略**：首页供应商日志现在统一走 `detailPreview / detailSource / copyText` 这一套结构，不再把请求返回体和 console provider error 当两套互不相干的展示链路，前端状态模型总算收口成一套。
- **剪贴板 fallback 抽成公共 helper，控制台与首页复用同一套复制逻辑**：新增前端通用 clipboard 工具，避免控制台页和首页供应商日志各抄一份 `navigator.clipboard + textarea fallback`，后面谁再改复制行为不用两头追着补锅。

---

# Code Switch v2.7.53

## 新功能
- **首页供应商卡片新增“供应商日志”入口**：现在可以直接在首页模型列表按钮旁边点开当前供应商的失败日志弹窗，专门看这家供应商最近的 `4xx / 5xx` 报错，不用再跳全量日志页自己筛，排障总算不像在垃圾堆里翻发票。

## 修复
- **单供应商错误日志不再串台**：修复首页供应商日志弹窗前端自己追加名称兜底查询、导致同名或历史遗留日志有机会混进来的问题；现在查询边界收回后端，优先按 `provider_id` 精确命中，只在确实没有 `provider_id` 的旧数据场景才回退名称，点谁就看谁的失败日志。
- **供应商日志弹窗切换目标时不再卡死“继续加载”状态**：修复弹窗开着时从一个供应商切到另一个供应商，如果上一轮正处于 `load more`，新供应商入口可能残留旧的 loading 状态、按钮半死不活的问题；现在重载时会统一重置分页与异步状态。

## 技术改进
- **失败日志过滤和响应体回传下沉到后端分页接口**：新增失败日志分页接口，后端直接按 `http_code >= 400` 过滤并返回 `response_body / response_body_truncated`，前端不再先捞全量再硬筛，也不用一条条补发 payload 详情请求，首屏链路清爽多了。
- **供应商日志列表补齐查询字段与回归测试**：新增失败日志字段白名单测试、`provider_id` 优先隔离测试和旧日志名称回退测试，把“只看当前供应商失败日志”这条约束写进测试里，后面谁再想手滑改歪，测试先狠狠干他一棍子。

---

# Code Switch v2.7.52

## 修复
- **内置模型价表和供应商价表都补上分组倍率**：现在两套价格配置都支持独立设置 `group multiplier`，默认值统一为 `1`；不配就是原价结算，别再让默认值偷偷跑偏，搞出一堆“怎么金额突然缩水了”的玄学事故。
- **请求日志金额口径统一按“基础成本求和后再乘分组倍率”结算**：输入、缓存、输出和按次计费会先按原始单价分别求和，再统一乘以分组倍率；日志详情、Tooltip 展示和实际落库口径终于对齐，不再各算各的。
- **供应商按次计费快照不再被分组倍率污染原始单价**：修复按次费用快照把倍率提前揉进单价字段的问题；现在快照保留原始价格，分组倍率只参与最终金额计算，后续排账和排查才不至于看得脑瓜子嗡嗡的。

## 技术改进
- **价格配置反序列化补齐 `group_multiplier` 存在性识别**：内置模型价格配置现在会直接识别 `group_multiplier` 字段，不再非得依赖额外标记位才生效，兼容性总算整利索了。
- **日志计费说明和前后端计算细节同步收口**：前后端统一补齐分组倍率公式展示、日志详情字段和价格响应结构，默认值策略和实际计算路径保持一致，后面谁再想把这套口径改歪，至少没那么容易糊弄过去。

---

# Code Switch v2.7.51

## 修复
- **首页供应商拖拽排序终于不再像搬砖瞬移**：修复供应商卡片拖动排序时只有最终落位、过程中几乎没有让位反馈的问题；现在拖拽过程中会实时重排，列表位移动画、目标卡片高亮和拖拽卡片反馈会一起跟上，排序手感不再硬得像拿铁锹铲。
- **取消拖拽不再把列表顺序悄悄搞乱**：修复拖拽过程中临时重排后，一旦中途取消或没正常落下，列表顺序可能残留在半成品状态的风险；现在 `drop` 成功才保留新顺序，`dragend` 取消时会恢复拖拽前顺序。

## 技术改进
- **拖拽命中逻辑从粗暴 `dragenter` 升级为按上下半区判定前后插入**：卡片排序不再只会“插到目标前面”，现在会根据鼠标落在卡片上半区还是下半区决定插前还是插后，拖拽语义和视觉反馈终于对上号。
- **拖拽状态机补齐空重排短路和单测**：新增目标区域去重短路，避免同一张卡片反复触发无意义重排；同时补上拖拽落下持久化、取消回滚、前后插入判定这几类单测，后面谁再手滑把排序状态机改回玄学行为，测试先拦着。

---

# Code Switch v2.7.50

## 修复
- **长编辑器滚到底后滚轮终于会继续带动外层弹窗**：修复 `JsonCodeEditor` 在内容已经滚到顶部/底部时，滚轮事件被编辑器自己死死吃住，导致父级弹窗或滚动容器像卡住了一样的体验问题；现在编辑器内部滚不动时，会把滚动平顺交还给外层容器，不用再疯狂甩滚轮。

## 技术改进
- **滚轮边界处理收口到编辑器内部统一逻辑**：新增滚轮增量归一化、可滚动祖先查找和边界判断逻辑，统一处理 `DOM_DELTA_LINE / PAGE / PIXEL` 三种模式，减少不同输入设备和浏览器下继续出现“滚动锁死”或滚过头的玄学行为。

---

# Code Switch v2.7.49

## 修复
- **Codex stream 成功判定终于不再嘴硬**：修复代理层只要拿到 `2xx` 就提前记成功的问题；现在 `stream=true` 的 Codex 请求会校验协议完成态，缺少 `response.completed`、中途断流或流复制失败时，不会再在控制台和日志里硬记成 `Level 1 成功`。
- **响应写出一半后不再乱切下一个 provider**：修复首个 provider 已经开始向客户端输出 stream 时，后端仍可能继续重试/降级的风险；现在一旦响应已部分写出，就会立即停止切换，避免多家 provider 的流被硬拼成一锅粥。
- **流式兼容路径不再误杀整块 JSON 回包**：修复部分 Codex 兼容供应商在 `stream=true` 下直接返回完整 JSON、却被误判为“未完成 stream”的回归问题；现在会兼容识别 `status=completed` 这类完成态，不再把能正常工作的 provider 顺手打死。

## 技术改进
- **下游断开与上游读失败的错误分类收紧**：客户端中断判定现在只在明确的下游写失败场景生效，上游 `streaming response` / `non-standard response` 读取失败不会再被误算成用户断开，失败计数和黑名单统计终于不再背锅背错人。
- **stream 收尾逻辑统一收口并补齐回归测试**：成功收尾改为统一走 `finalizeForwardSuccess`，同时新增 Codex incomplete stream、完整 JSON fallback 和客户端断开分类测试，后面谁再把这套成功判定改回“只看 2xx”，测试会先狠狠干他一锤子。

---

# Code Switch v2.7.47

## 修复
- **CLI 配置备份不再无限繁殖时间戳垃圾文件**：`.claude`、`.codex`、`.gemini` 相关配置文件的备份策略改为固定滚动 `.bak` 文件，不再每次保存都额外生成一串 `*.bak.<timestamp>`；同目录下历史时间戳备份也会在后续写入时自动清理，配置目录终于不再像个临时文件回收站。
- **恢复默认现在会优先吃真正的当前滚动备份**：修复新旧备份格式并存时，恢复逻辑可能被较新的旧时间戳备份“抢优先级”，导致回滚结果和当前滚动备份不一致的问题；现在只要 `.bak` 存在，就会优先使用当前滚动备份恢复。

## 技术改进
- **WSL 侧 CLI 备份脚本统一收口并补上 symlink 防护**：Claude / Codex / Gemini 的 WSL 自动配置脚本统一复用滚动备份 helper，固定备份路径会先校验是否为 symlink，再通过临时文件原子落位，减少固定 `.bak` 路径被异常链接劫持或脚本三处继续分叉的风险。
- **备份链路回归测试补齐**：新增滚动备份优先级测试与 WSL 脚本字符串级回归测试，明确卡住“不再生成 `.bak.$ts`”和“backup 路径 symlink 防护必须保留”这两个关键约束，后续谁手滑把旧逻辑抄回来，测试会先把他拦住。

---

# Code Switch v2.7.45

## 性能优化
- **macOS 冷启动待机内存继续下砍**：主窗口不再随应用启动立即创建，改成首次打开时按需初始化；macOS 托盘弹窗也改为首次点击托盘时才创建，减少仅托盘常驻时的 WebView / WebKit 常驻占用。
- **前端首屏资源改成按路由和页面拆包加载**：主页面、日志、控制台、可用性检测、托盘等页面统一改为动态导入，`vite manualChunks` 同步拆分共享依赖，降低启动阶段一次性加载的脚本体积。
- **Lobe 图标库不再全量 eager 打包**：图标索引和 SVG 资源改为按需加载，首页供应商卡片、编辑弹窗、MCP、Gemini 等入口按实际图标预热，避免主页面和托盘页面继续背整包图标启动。

## 技术改进
- **后台服务链路补上保守内存调优**：Go 运行时新增 `GOGC` 兜底调优，relay 切到 `gin.ReleaseMode` 并补齐 HTTP timeout，HealthCheck Transport 和 SQLite 连接池 / PRAGMA 也做了保守收口，在不砍后台能力的前提下降低常驻开销。
- **内存诊断和优化进度文档一起落库**：新增仅允许 loopback 访问的 `/debug/memory` 接口，并把 `MEMORY_OPTIMIZATION_GUIDE.md` 更新为带进度勾选的落地文档；当前唯一未闭环项只剩 macOS 打包态实测与完整回归。
- **前端构建环境门槛对齐到新工具链**：`frontend/package.json` 增加 Node 版本约束，新增 `.nvmrc` 固定到 `20.19.0`，避免本地和 CI 因 Node 版本不一致把构建链路搞崩。

---

# Code Switch v2.7.44

## 修复
- **Codex 通用配置弹窗终于能顺畅滚到底**：修复首页供应商配置里，Codex 通用配置模板内容较长时弹窗无法向下滚动、底部开关和保存按钮被直接挤出可视区的问题；现在长 `TOML` 配置可以稳定滚到底，不会再出现“下面那截凭空消失”的情况。

## 技术改进
- **编辑器高度控制升级为正式组件能力**：`JsonCodeEditor` 新增受控 `surfaceHeight` 能力，Codex 模板编辑器不再依赖父组件 `:deep()` 覆盖内部样式，后续调整编辑区高度时不容易再和组件实现细节绑死。
- **弹窗滚动约束收口到局部区域**：移除全局 `modal-body` 上有回归风险的裁剪样式，滚动与滚轮边界控制只作用于实际需要滚动的模板编辑区域，避免其它弹窗里的下拉菜单、浮层后续被顺手裁掉。

---

# Code Switch v2.7.43

## 修复
- **Codex 通用配置终于按 TOML 说人话**：修复首页供应商配置里，Codex 通用配置模板仍被前端按 JSON 校验，导致合法 `config.toml` 内容无法保存的问题；现在模板编辑、格式化、保存和回显统一走 TOML 解析链路。
- **模板格式化不再反手吞掉用户刚输入的内容**：修复点击“格式化”后异步归一化返回较慢时，会把用户后续继续输入的新内容覆盖掉的问题；现在模板格式化增加请求序号保护，只会回写当前这次格式化对应的文本。
- **Codex 模板里的托管字段不再假装能生效**：修复 `model_provider`、`preferred_auth_method`、`model_providers.code-switch-r` 这类系统托管字段可以被模板保存、但实际落盘会被静默丢弃的误导行为；现在模板归一化阶段就会剥离这些字段，避免“看着能配，实际上白给”。

## 技术改进
- **模板编辑器统一改走后端单一真相源**：前端不再分别维护 JSON/TOML 两套模板解析逻辑，模板的渲染、格式化、实时校验统一消费后端 `RenderTemplateEditorContent / NormalizeTemplateEditorContent`，减少前后端格式规则继续跑偏的概率。
- **模板锁定字段过滤逻辑跨平台复用**：Claude / Codex / Gemini 的通用配置模板现在都会复用对应平台的锁定字段剥离逻辑，模板可编辑范围与主配置编辑器保持一致，后续继续扩展时不容易再冒出“编辑器能写、保存不生效”的妖怪问题。

---

# Code Switch v2.7.42

## 新功能
- **供应商 CLI 配置改成同屏配置文件编辑器**：编辑供应商时，模型映射下方的 CLI 配置不再分裂成“预览”“快照”“可编辑字段”几套视图来回折腾；现在统一展示代理托管字段和配置文件编辑器，直接在当前区域编辑最终会写入的配置内容。
- **配置文件编辑器补齐格式化与语法校验体验**：Claude / Codex / Gemini 三个平台的 CLI 配置现在统一支持行号、格式化、实时校验和内联错误提示；Codex 按 TOML 展示，Gemini 按 `.env` 展示，终于不是拿 JSON 壳子硬套所有平台。

## 修复
- **Codex 编辑器终于展示完整配置文件而不是残血字段**：修复编辑供应商时，Codex 配置区只显示少量可编辑字段、看不到完整 TOML 内容的问题；现在会按当前供应商上下文渲染完整配置文件，并正确回填代理托管字段与当前 provider table。
- **保存通用配置和取消注入不再吞掉未应用编辑**：修复主编辑器里还有 pending 配置时，保存“编辑通用配置”或取消“写入通用配置”会直接覆盖用户刚敲内容的问题；相关入口现在都会先冲刷当前编辑器状态，再执行模板注入/回滚。
- **保存供应商前会先吃掉待应用的 CLI 配置**：修复提交供应商时 `CLI` 编辑器仍可能停留在未应用状态、导致表单和磁盘写入内容不一致的问题；现在提交链路会等待异步应用完成，再生成最终 payload。

## 技术改进
- **CLI 锁定字段改为后端单一真相源**：前端不再自行猜 `provider key` 或本地拼锁定字段，统一改为消费后端 `RenderEditorContent / NormalizeEditorContent` 返回的 `lockedFields`，展示内容和最终落盘结果走同一套逻辑。
- **历史静态字段定义与当前编辑器逻辑收口复用**：`GetConfig()` 里展示给前端的锁定字段改为直接复用当前编辑器 builder，减少 Claude / Codex / Gemini 三套配置继续各自维护、后面越改越串味的概率。

---

# Code Switch v2.7.41

## 新功能
- **供应商 CLI 配置支持通用配置模板复用**：供应商编辑弹窗里的 CLI JSON 现在可以直接打开“编辑通用配置”维护平台级模板；保存后可设为新供应商默认值，当前供应商也能通过“写入通用配置”一键补齐当前 JSON 里缺失的配置项，不用再手工一段段抄。

## 修复
- **通用配置勾选状态不再跨供应商串台**：修复同平台下切换不同供应商时，上一条供应商残留的“写入通用配置”状态会继续污染下一条表单的问题；现在编辑器实例会按供应商上下文重建，切换后不会再把别人的模板偷偷带过来。
- **取消“写入通用配置”不再误删用户后续编辑**：修复旧实现用整份快照回滚，导致勾选期间用户自己新增或修改的字段也被一起抹掉的问题；现在只撤销模板注入的缺失字段，用户手工改过的内容会原样保留。
- **CLI JSON 清空后终于会按预期落盘**：修复把 CLI 配置清成空对象后前端直接跳过保存，磁盘上仍残留旧配置的问题；现在提交链路会显式区分“未改动的空态”和“用户确认清空”，真正清空时会正确写回默认/锁定字段结果。

## 技术改进
- **CLI 编辑器与预览链路统一同源**：新增基于 `editable` 的 dry-run 快照生成接口，CLI JSON 编辑区、预览结果和磁盘当前值现在走同一套生成逻辑；像 Claude 这类代理托管字段自动注入的场景，编辑区看到的内容和下方预览终于说的是同一种人话。
- **模板注入逻辑改成可逆 delta 模型**：通用配置注入不再靠整对象快照硬回滚，而是按路径记录本次补进来的字段和原值，再按 delta 精准撤销；这套状态模型更稳，后面再加模板能力也不容易把用户编辑链路掀翻。

---

# Code Switch v2.7.40

## 新功能
- **供应商 CLI 配置改成内联 JSON 编辑器**：编辑供应商页面中，模型映射下方的 CLI 配置不再继续堆一排零碎可编辑字段；除“代理托管”外，其余可编辑内容统一切到 JSON 编辑模式，支持行号、自动格式化、JSON 校验和友好错误提示，改配置终于像个正经编辑器。

## 修复
- **CLI 配置不再藏在手风琴里折腾人**：CLI 配置区域和配置文件预览现在默认直接展开显示，不用再连点多层折叠才能看见内容，编辑路径收口到一个区块里，少来回找。
- **恢复默认终于恢复的是磁盘真实配置**：修复恢复默认后前端又把旧 `modelValue` 覆盖回去的问题；现在恢复后会重新拉取对应平台的实际 CLI 配置并同步回表单，避免“点了恢复，结果啥也没恢复”的假动作。
- **未应用 JSON 不再跨平台串台回写**：修复在平台切换或上下文刷新时残留旧 pending JSON，可能误写到新平台配置的问题，并补上异步加载竞态保护，避免旧请求回写新界面。

## 技术改进
- **JSON 编辑器状态切换不再重建实例**：新增 `frontend/src/components/common/JsonCodeEditor.vue`，基于 CodeMirror 的 `Compartment` 做动态重配置；主题、只读态、占位提示变更时不再重建编辑器实例，光标位置和 undo history 都能保住。

---

# Code Switch v2.7.39

## 修复
- **供应商原始错误终于能一路透传到终端**：修复 `xrequest` 在上游 `5xx` 重试耗尽时同时返回 `resp + err`、但网关错误分支只认 `err` 不认 `resp` 的问题；现在像 `503 new_api_error` 这类真实供应商 JSON 会保留原始 `status / headers / body` 返回给终端，不再被外层糊成“所有 provider 均失败”的 `502` 套娃废话。
- **控制台现在认得真实多行上游报错了**：修复控制台错误解析只兼容 `upstream status 503:` 老格式、却吃不动后端实际输出的 `provider=... status=503` 多行日志的问题；现在供应商返回的原始 JSON 正文会被稳定提取，实际错误信息终于能在页面上正常展示。
- **中文过载报错不再被傻乎乎归类成普通 `5xx`**：补上“负载已经达到上限”“模型繁忙”“服务繁忙”等中文语义识别，像 `claude-opus-4-6` 负载上限这种错误现在会正确标成“模型负载过高”，不用再靠人工脑补。

## 技术改进
- **Provider relay 错误控制流不再偷懒复用日志字段**：`forwardRequest` 现在显式使用局部响应状态码驱动错误分支，而不是拿 `requestLog.HttpCode` 兼职当控制流条件；后面谁再改日志记录逻辑，也不至于顺手把上游错误透传链路掀翻。
- **真实线上报文的前后端回归测试补齐**：新增单 provider `503 + new_api_error + request id` 的后端透传测试，并补上控制台对真实多行日志与中文过载语义的前端解析测试；这次不是“看着像修好了”，而是链路两头都拿真实样例压过了。

---

# Code Switch v2.7.38

## 修复
- **控制台错误日志现在直接展示供应商返回的具体错误信息**：修复控制台里失败日志只剩原始长行、用户还得自己从 JSON 里扒 `message/code/type` 的问题；现在像模型负载过高、接口 `500`、限流、鉴权失败这类上游错误都会被结构化提取并直接展示，排障不用再靠肉眼淘金。
- **成功日志不再被误标成红彤彤的错误项**：修复供应商错误解析对 `upstream status 200` 场景防守不严的问题；即使日志里带了 JSON payload，正常成功记录也不会再被误判成 `ERROR`。

## 技术改进
- **控制台供应商错误解析链路收口并支持单独复制**：新增 `frontend/src/utils/providerError.ts`，统一处理上游状态码、错误摘要、`code/type/status/param` 提取和复制文本格式化；控制台页面现在可一键复制供应商原始错误详情，不再只能复制整条混杂日志。
- **错误分类与回归测试补齐**：补充 `providerError` 针对空输入、`status 200 + JSON`、畸形 JSON、限流、鉴权等场景的单测，同时收窄 `unavailable` 语义匹配范围，减少后续继续把正常日志误伤成“模型过载”的概率。

---

# Code Switch v2.7.37

## 修复
- **托盘 `5 小时额度` 重置倒计时改为真实 `HH:mm:ss` 秒级展示**：修复托盘弹窗里 `5 小时额度` 倒计时只按分钟刷新、却硬显示秒数的假精度问题；现在倒计时会按秒实时跳动，展示口径终于和实际剩余时间一致，不再出现“看着有秒，实际上在演”的情况。
- **多平台托盘倒计时不再被单一卡片到点事件卡住**：修复托盘动态标签更新链路误用 `some()` 导致的短路问题；当某个平台额度到达重置边界时，另一个平台的倒计时与预测文案不会再被顺手冻住，双平台显示能稳定一起更新。

## 技术改进
- **托盘静态额度标签与时间标签刷新逻辑拆分收口**：将已用金额、总额度、进度条这类静态信息，与重置倒计时、预计耗尽这类时间文案拆开处理；秒级 ticker 现在只更新真正会变化的内容，逻辑更清楚，也避免每秒重算一堆根本没变的数据。
- **秒级倒计时纯函数与回归测试补齐**：新增 `frontend/src/utils/trayCountdown.ts`，统一处理 `HH:mm:ss` 格式化、秒级 ticker 启用判定和批量更新聚合，并补齐针对“未到重置却先显示 `00:00:00`”和“更新循环被短路”的前端测试，后面谁再乱改这块，测试先拦着。

---

# Code Switch v2.7.36

## 修复
- **`5 小时额度` 重置倒计时恢复准确**：修复托盘弹窗和设置页把 `5 小时额度` 误算成“最近 5 小时滚动窗口”的问题；现在会按首个请求触发固定 `5 小时周期`，周期结束后若没有新请求不会自动滚进下一轮，倒计时与已用金额终于和实际用量规则对上了。
- **清理历史日志不再把当前 `5 小时周期` 算歪**：修复旧实现依赖全量 `request_log` 回放周期边界，删除早期日志后会把当前窗口开始时间和已用金额一起带偏的问题；现在清理历史明细不会再把正在生效的额度周期搞乱。

## 技术改进
- **`5 小时额度` 改为写入时持久化周期状态**：新增 `request_log_quota_cycle_state` 存储与 `request_log` 插入触发器，日志写入时即维护当前平台的 `5 小时周期` 起点、重置时间和周期内已用金额，托盘刷新不再每次扫描历史日志重建周期。
- **老库升级回填与回归测试补齐**：启动时会为已有日志回填当前 `5 小时周期` 状态，并新增覆盖“跨周期重启”“删历史后不漂移”“老数据回填恢复当前周期”等场景的回归测试，后面谁再乱改这块，至少测试会先吱一声。

---

# Code Switch v2.7.35

## 新功能
- **托盘额度“当前已使用”改为直接显示并可手动编辑**：设置页里的 Claude Code 与 Codex `5 小时 / 日 / 周 / 月` 四类额度不再展示抽象的“当前已用调整”，现在会直接拉取当前额度窗口的已使用金额并显示在输入框中，录入后会自动折算成校准值参与托盘弹窗展示。

## 修复
- **过期额度快照不再冒充“当前已使用”**：移除设置页对 quota 已使用运行时数据的本地持久化，避免跨会话继续显示旧窗口、旧配置下的金额，并在刷新失败时把错值再次保存成新的额度校准值。
- **未启用额度不再误导用户“可编辑且会生效”**：只有真正设置了预算上限、且成功拉到当前窗口使用值的额度才允许编辑；未启用、加载中和拉取失败三种状态都会明确给出提示，不再出现“看着能改，托盘其实没反应”的拧巴交互。

## 技术改进
- **额度已使用请求策略按启用项收口并支持单项容错**：设置页现在只会为已启用的 quota 拉取当前窗口使用值，并改为逐项处理请求结果；单个额度拉取失败不会把整组数据一起拖下水，后续再扩展示状态和重试逻辑也更稳。

---

# Code Switch v2.7.34

## 修复
- **托盘弹窗不再把无限额度全摆出来凑热闹**：修复 Claude Code 与 Codex 托盘卡片在预算未设置时仍继续显示 `5 小时 / 日 / 周 / 月` 四条 `∞` 明细的问题；现在只有设置了预算上限的额度才会显示，避免无限额度占位把真正有用的信息往下挤。
- **全无限预算场景退化为总消耗摘要**：当某个平台所有额度都未设置时，托盘弹窗现在只显示一条“总消耗”摘要，不再出现四条全是 `∞` 的空洞卡片，信息密度总算正常了。
- **托盘首次加载不再误闪 `$0.0000` 总消耗**：修复展示模式还没根据真实配置解析完成前，就先按“全无限”分支渲染 summary 的问题；现在托盘会等预算展示模式确定后再决定渲染额度列表还是总消耗摘要。

## 技术改进
- **托盘预算展示模式与可见额度筛选独立收口**：新增纯函数统一处理 `summary / quotas` 模式判断和可见额度筛选，`∞` 额度会直接跳过 `fetchCostSince()` 统计请求，同时补齐 `all infinite` 与 `mixed finite/infinite` 的前端测试，后续再改托盘展示规则不至于一脚踹翻。

---

# Code Switch v2.7.33

## 修复
- **托盘弹窗不再把下半部分额度卡片直接裁掉**：修复 macOS 托盘弹窗在接入多额度预算明细后窗口最大高度过小，导致 `Codex` 卡片和底部内容显示不完整的问题；现在托盘窗口会按内容高度自动扩展，双平台预算卡片可以完整展示。
- **托盘内容过长时改为内部滚动兜底**：修复托盘弹窗高度测量只看可见区域，内容溢出后会把自己错误量矮的问题；现在改为基于 `scrollHeight` 计算窗口高度，并在超出上限时启用弹窗内部纵向滚动，后续额度信息再增加也不会继续截断。

## 技术改进
- **托盘窗口尺寸策略收口**：统一调整前端托盘根容器的滚动约束与 Go 端 tray window 最大高度配置，避免 WebView 内容高度、窗口高度和系统托盘容器三边各算各的，最后把界面卡成半截。

---

# Code Switch v2.7.32

## 新功能
- **每个预算额度都能单独手动校准已用金额**：Claude Code 与 Codex 的 `5 小时额度`、`日额度`、`周额度`、`月额度` 现在都可分别设置“当前已用调整”，设置页录入后会立即参与托盘弹窗的已用值展示，不再共用一份偏移量。

## 修复
- **历史单额度校准值不再错误铺满全部预算窗口**：修复旧版 `budget_used_adjustment` 升级到多额度配置时被同步复制到四个额度的问题；现在只会按旧刷新周期定向迁移到对应的 `daily / weekly / monthly` 额度，`5 小时额度` 不再被误伤。
- **多额度保存后旧字段兼容链路恢复可用**：修复保存设置时把 `budget_used_adjustment(_codex)` 等 legacy 字段直接清零，导致旧消费方或回退版本读到错误预算信息的问题；现在前后端都会把多额度配置按可表达范围投影回旧单额度字段，兼容行为稳定一致。

## 技术改进
- **预算配置归一化逻辑统一收口**：前端 `budgetUsage` 与 Go 端 `normalizeBudgetSettings()` 现在共用同一套“legacy 迁移 + multi-quota 投影”规则，设置页缓存、接口序列化、托盘展示和本地持久化终于说同一种人话了。
- **兼容回归测试补齐**：新增前端 quota adjustment 迁移 / legacy 投影测试，以及 Go 端单额度迁移、多额度回投影、`5 小时额度` 清空旧字段等用例，后面谁再乱改预算协议，测试先抽他一耳光。

---

# Code Switch v2.7.31

## 新功能
- **托盘预算升级为多额度独立配置**：Claude Code 与 Codex 的“刷新周期”设置不再是单选模式，现支持同时配置 `5 小时额度`、`日额度`、`周额度`、`月额度` 四类预算窗口；每个额度可独立设置总额与刷新点，未设置时按 `∞` 展示。
- **托盘弹窗新增多额度明细展示**：托盘面板现按平台分别展示四条额度状态，包含已用金额、总额度、进度条、重置倒计时与预算耗尽预测，预算窗口切换和设置页配置保持一致。

## 修复
- **多额度配置前后端字段不再静默丢失**：修复 `budget_quota_settings` 嵌套字段在前端 `camelCase` 与后端 `snake_case` 之间口径不一致，导致 `refreshTime`、`refreshWeekday`、`refreshMonthDay` 这类刷新配置可能保存后失效的问题；现在读取、保存与持久化链路都做了双向兼容和归一化。
- **无限额度不再显示伪倒计时 / 伪预测**：修复额度未设置、按 `∞` 展示时托盘仍继续显示重置倒计时和预算耗尽预测的问题，避免“明明无限还倒计时”的语义冲突。
- **已用金额校准升级为每额度独立调整**：设置页现支持为 `5 小时额度`、`日额度`、`周额度`、`月额度` 分别维护“当前已用调整”，历史单额度校准值会按旧刷新周期定向迁移到对应额度，避免升级后多额度显示被错误联动。

## 技术改进
- **预算配置协议兼容层补齐**：前端 `AppSettings` 读写链路统一增加预算额度配置序列化 / 反序列化；Go 端 `BudgetQuotaSetting` 新增宽容 JSON 反序列化逻辑，支持新旧字段名并存。
- **预算窗口与配置兼容测试补齐**：新增前端 `snake_case` 额度配置归一化测试和后端 JSON 兼容测试，后续再有人改预算配置协议时，不至于一脚把设置链路踹回解放前。

---

# Code Switch v2.7.30

## 修复
- **首页热力墙首次进入不再空白直到切页返回**：修复热力墙首屏初始化时容器宽度尚未稳定、查询窗口被错误压缩的问题；现在首次进入会先按默认窗口拉取历史数据，再结合 `ResizeObserver` 与多帧宽度复测补齐真实列数，不用再切到别的页面回来“唤醒”热力图。
- **热力墙手动刷新不再被进行中的旧请求吞掉**：修复首页刷新和热力墙重载链路在存在同范围 pending 请求时直接被去重逻辑短路的问题；现在显式 `reload` 会强制发起新请求，并让旧结果自动失效，避免用户明明点了刷新却啥也没变。

## 技术改进
- **热力墙布局同步支持页面隐藏暂停 / 恢复**：热力墙在 `keep-alive` 页面 `deactivated` 时会主动停止宽度探测、节流任务与 `ResizeObserver`，回到首页后再恢复同步，减少隐藏页无意义的布局测量和额外请求。
- **热力墙首屏与激活态回归测试补齐**：新增单测覆盖“宽度为 `0` 的首屏初始化”“多帧后宽度稳定补拉数据”“强制刷新覆盖 pending 请求”“隐藏页暂停布局同步”等场景，后续再有人把这块改炸就没那么容易蒙混过关了。

---

# Code Switch v2.7.29

## 修复
- **首页热力墙首屏首次打开立即展示历史数据**：修复首页首次进入时热力墙在容器宽度尚未稳定就按过小列数加载，导致历史数据需要切到其他页面再返回才会显示的问题；现在会先挂载尺寸监听，再结合 `getBoundingClientRect()` 与下一帧复测补齐真实宽度，首屏即可稳定展示历史数据。
- **首页热力墙当前日期 / 当前小时支持边框高亮定位**：热力墙现在会为 `daily` 模式的“今天”和 `hourly` 模式的“当前小时”格子加描边高亮，即使当前格子没有数据也能一眼定位，不用再顺着整面墙慢慢找。

## 技术改进
- **热力墙当前格子匹配逻辑抽离为纯函数并补齐测试**：新增 `frontend/src/utils/heatmapCurrentCell.ts`，统一处理本地时区下的当前格子 key 计算与下一次刷新边界，减少组件内部重复构造 `Date` 对象；同时补充单测覆盖时区换算、整点 / 零点边界与非法日期兜底。
- **热力墙当前格子刷新节奏按粒度边界对齐**：页面常驻时，高亮不再按分钟机械刷新，而是 `hourly` 等整点、`daily` 等次日零点；切换粒度时也会立即重排定时器，避免高亮停在旧的小时或日期上。

---

# Code Switch v2.7.28

## 修复
- **首页 Main 迁移收口后 `others` 代理状态不再串台**：修复自定义 CLI 工具在 `others` 平台切换、新增、删除后，顶部 relay toggle 仍沿用旧工具代理状态的问题；现在代理开关会跟随当前选中的工具即时同步，避免界面状态和真实代理状态打架。
- **首页热力图刷新与事件驱动切平台恢复等价行为**：修复拆分后热力图 `reload` 暴露链路缺失、`provider:switched` / `provider:blacklisted` 事件切平台时未补齐 tab 激活副作用的问题，避免出现热力图不刷新或切页后状态还停留在旧平台的回归。
- **`ProviderModelListModal` 目录结构与迁移文档彻底对齐**：将模型列表弹窗正式收口到 `frontend/src/components/Main/modals/`，修复迁移文档已标完成但目录仍残留在根路径的收尾遗漏。

## 技术改进
- **首页 Main 第二轮解耦改造正式收口**：`frontend/src/components/Main/Index.vue` 已从 `10287fa`（`release: prepare v2.7.27`，`1677` 行）继续收敛到本次 `v2.7.28` 发布提交（`514` 行），热力图展示、页面壳编排、Provider 表单编排、Provider 统计展示与模型列表弹窗目录全部完成收口。
- **自定义 CLI 代理状态拉取改为并发**：`useCustomCliTools.ts` 现在并发查询各工具代理状态并清理失效缓存，减少首页初始化时串行探测的等待时间。
- **迁移进度文档与索引同步完结**：`qianyi/main-index-migration.md` 与 `qianyi/README.md` 已回写最终完成状态、完成定义与关键修复点，后续追溯 `Main/Index.vue` 改造范围时不再需要对着 diff 盲猜。

---

# Code Switch v2.7.27

## 修复
- **首页 Main 拆分后的私有样式不再串到别处**：将 Provider 卡片、Provider 编辑弹窗、CLI 配置弹窗和自定义 CLI 工具条的样式收回组件边界，移除 `Main/Index.css` 全局兜底，修复 `level-badge`、`blacklist-banner`、`current-use-badge` 等类名泄漏后误伤其他页面的风险。
- **Provider 编辑保存链路不再各走各的**：新增统一的 Provider 表单 mapper，创建、编辑、保存与“保存并应用”现在共用同一套默认值、旧字段兼容和落库归一化逻辑，避免拆分后表单口径分叉把配置写歪。

## 技术改进
- **首页 Main 第一轮解耦改造完成收口**：首页根文件继续保留页面 orchestration，卡片列表、Provider 编辑、CLI 工具配置、更新轮询、黑名单与可用性逻辑已按目录拆分，`Main` 页结构终于不再是一坨 5000 行大乱炖。
- **迁移进度文档同步补齐**：`qianyi/main-index-migration.md` 已回写本轮拆分进度、样式边界收口结论和剩余待办，后续继续下沉 `useProviderForm` 时不至于再摸黑瞎改。

---

# Code Switch v2.7.26

## 新功能
- **托盘预算刷新周期新增每月**：Claude Code 与 Codex 的托盘预算设置现支持 `每月` 周期，可分别配置每月刷新日期与刷新时间，预算统计、重置倒计时和托盘展示会按月窗口同步切换。

## 修复
- **月末日期不再把预算周期算歪**：当每月刷新日设置为 `29` / `30` / `31` 且当前月份没有对应日期时，系统现在会自动回落到当月最后一天，避免 2 月和小月出现无效日期导致的周期边界错乱。
- **周模式与月模式配置不再互相串值**：预算周期配置现拆分“刷新星期”和“每月刷新日期”两套字段，切换周期模式时不会再出现拿周几硬顶每月几号的脏数据问题。

## 技术改进
- **预算周期前后端模型统一升级**：补齐 Claude / Codex 双平台的前端设置、托盘读取、Go 服务层计算与本地持久化链路，`daily` / `weekly` / `monthly` 三种周期现在共用同一套规范化与边界处理逻辑。
- **发版元数据版本号统一对齐**：同步更新应用版本常量、Wails 构建配置、Windows 版本信息与 Linux `nfpm` 版本，避免 GitHub Release 自动打包时再被旧版本元数据拖后腿。

---

# Code Switch v2.7.25

## 修复
- **接口调试详情暗色主题可读性恢复**：修复首页供应商模型列表的“接口调试详情”弹窗在深色主题下仍使用浅色请求记录卡片背景的问题；现在请求记录卡片、状态标签、请求/响应面板与代码块都切换到暗色层级，避免出现“白底糊字、信息看不清”的情况。
- **GitHub Release 正文不再只剩自动汇总**：修复发布 Workflow 按版本提取 `RELEASE_NOTES.md` 时，开始/结束条件同时命中当前版本标题导致正文被截空的问题；现在 GitHub Releases 会稳定带上当前版本对应的更新说明。
- **BOM 与本地发版依赖检查补强**：修复 `RELEASE_NOTES.md` 使用 `UTF-8 with BOM` 时版本段落提取失败的问题，并补齐本地 `publish_release.sh` 对 `python3` 的前置检查，避免打包完成后才在发版阶段翻车。

## 技术改进
- **发布说明提取链路统一收口**：新增 `scripts/extract_release_notes.py`，统一 GitHub Workflow 与本地发版脚本的版本日志提取逻辑，减少线上线下两套实现继续分叉的维护成本。
- **更新元数据字段兼容性增强**：`latest.json` 现同时写入 `release_notes`、`body` 与 `changelog`，应用内更新弹窗和旧兼容链路都能稳定拿到对应版本的更新内容。

---

# Code Switch v2.7.22

## 新功能
- **日志页金额列金色凸显**：日志列表中的金额文字现在使用琥珀色/金色等宽字体显示，非零金额一目了然；`$0` 金额自动降级为灰色，减少视觉干扰。
- **日志页缓存写入类型标签化**：缓存写入行中的 `5m` 和 `1h` 时间类型现在以药丸标签形式展示——5m 为紫色、1h 为蓝色，标签内类型文字加粗、数值字体柔和，视觉层次清晰。

## 修复
- **导入弹窗延迟聚焦时序不安全**：修复手动聚焦 textarea 的 setTimeout 延迟（350ms）小于 InlineModal 内部 fallback 定时器（380ms），可能在动画收尾阶段抢先聚焦触发 WebKit 渲染 bug 的问题；延迟已调整为 400ms。
- **快速开关导入弹窗时残留聚焦定时器**：修复连续打开/关闭导入弹窗时，前一次的延迟聚焦 setTimeout 未被清理的问题；现在关闭弹窗时统一清除定时器。

## 改进
- **金额列键盘无障碍增强**：为金额值添加 `focus-visible` 样式（outline + 背景），键盘 Tab 导航时有明确视觉反馈，与模型名、验证标签等可交互元素保持一致。
- **缓存标签换行间距优化**：缓存写入行添加 `flex-wrap` + `row-gap`，标签过多时自动换行且行间保持间距。
- 亮色/暗色主题全部适配。

---

# Code Switch v2.7.21

## 修复
- **首页"粘贴JSON导入"弹窗不显示**：修复首页供应商模型列表中点击"粘贴JSON导入"后，界面出现模糊遮罩但弹窗面板不显示的问题（每次必现）；根因是进场动画期间对 textarea 聚焦触发了 WebKit 渲染 bug，导致面板卡在 `opacity: 0` 状态。
- **transitionend 多属性事件重复触发**：修复 `transition: all` 导致 `opacity` 和 `transform` 各触发一次 `transitionend`，聚焦逻辑可能被执行两次的问题；现在只监听 `transform` 属性结束事件。
- **弹窗关闭时 transitionend 监听器未清理**：修复快速开关弹窗时，旧的 `transitionend` 事件监听器未被移除的资源泄漏问题；现在关闭弹窗和组件卸载时统一清理。

## 技术改进
- **`InlineModal` 初始聚焦改为事件驱动**：用 `transitionend` 事件替代不可靠的 `setTimeout(220ms)`，确保在面板 CSS 进场动画真正结束后再聚焦目标元素，彻底消除时序竞争；保留 380ms fallback timeout 兜底 WebKit 极端情况下 `transitionend` 未触发的场景。
- **聚焦防重入保护**：新增 `didFocus` 标志位，防止 `transitionend` 回调与 fallback timeout 在极端时序下双重执行聚焦逻辑。

---

# Code Switch v2.7.20

## 新功能
- **acw_sc__v2 WAF 挑战自动绕过**：当供应商 `/api/pricing` 接口返回 acw_sc__v2 WAF 挑战页时，程序会使用内嵌 JS 引擎（goja）自动求解挑战脚本、计算 cookie 并重试请求，大多数情况下无需用户手动在浏览器中完成验证即可拉取模型价格。

## 修复
- **WAF 绕过安全边界加固**：为挑战脚本执行添加 5 秒超时保护，防止恶意或异常脚本导致 goroutine 阻塞；`setTimeout` 同步模拟添加递归深度限制（最大 10 层），防止混淆脚本递归调用导致栈溢出。
- **调试信息完整性修复**：修复 WAF 绕过成功时原始请求的 debug attempt 缺少错误描述的问题，现在会完整记录"JSON 解析失败 → 检测到挑战 → 自动绕过成功"的完整链路。
- **DOM 模拟兼容性增强**：修复 `getElementsByTagName` 返回 `nil` 可能导致 JS 运行时 TypeError 的问题，改为返回空数组；重试请求补充 `Referer` 头，提升 WAF 变种场景的通过率。

## 技术改进
- 新增 `services/waf_acw_bypass.go`，封装 acw_sc__v2 挑战求解与带 cookie 重试的完整流程，与原有错误处理链路解耦，绕过失败时无缝降级到手动导入提示。
- 引入 `github.com/dop251/goja` 纯 Go JS 引擎，无 CGO 依赖，跨平台构建无额外配置。

---

# Code Switch v2.7.19

## 修复
- **首页模型列表 `粘贴 JSON 导入` 仍可能只剩模糊遮罩**：修复部分 WebKit / Wails WebView 环境下，导入弹窗在进场动画尚未结束时立即聚焦 `textarea`，可能触发面板本体不绘制、界面只剩全局模糊遮罩的问题；现在会等弹窗进场完成后再执行初始聚焦，导入面板会稳定显示。
- **弹窗关闭过程残留异步聚焦风险**：修复导入弹窗快速打开后立刻关闭、或弹窗切换过程中，延后聚焦任务仍可能落到已关闭弹窗上的时序问题；现在初始聚焦定时器会在关闭与卸载时统一清理。

## 技术改进
- **`InlineModal` 初始聚焦改为延后调度**：通用弹窗的 `initialFocusSelector` 现在会在进场动画结束后再尝试聚焦指定控件，并在失败时安全回退到关闭按钮或面板本身，降低 WebView 组合场景下的渲染合成异常概率。

---

# Code Switch v2.7.18

## 修复
- **首页模型列表 `粘贴 JSON 导入` 弹窗链路重新收口**：修复上一版继续拆分导入弹窗实现后，点击 `粘贴 JSON 导入` 可能再次出现弹窗不显示、模型列表主弹窗也被卡住无法正常关闭的问题；现在导入弹窗重新回到统一 `InlineModal` 链路，打开、关闭、遮罩与 `Escape` 行为恢复一致。
- **设置页 Teleport 弹窗按钮样式回归**：修复 `InlineModal` Teleport 到 `body` 后，设置页 `在线更新`、WebDAV 等弹窗底部 `action-btn` 样式丢失，导致按钮高度、内边距、对齐方式异常的问题；现在所有通用操作按钮都会稳定命中统一样式。
- **长标题与长按钮文案不再挤爆布局**：修复弹窗标题过长时顶压关闭按钮、底部按钮文案较长时超出按钮边界或把 footer 挤乱的问题；现在标题支持安全换行，按钮保持单行并会按文案自动增宽，放不下时整行按钮自动换行。
- **导入弹窗初始焦点不稳定**：修复 JSON 导入弹窗打开时，`textarea` 与关闭按钮互相争抢焦点，导致光标偶发不落在输入框中的问题；现在打开导入弹窗会稳定聚焦到 JSON 输入框。

## 技术改进
- **`InlineModal` 新增可配置初始焦点能力**：新增 `initialFocusSelector`，通用弹窗可按业务场景把焦点优先落到输入框、首要操作区或其它指定控件，避免继续在各业务组件里手搓 `nextTick + focus`。
- **通用按钮宽度策略统一为按内容扩展**：`action-btn`、`primary-btn` 与 `BaseButton` 共用“单行文字 + 按内容自动变宽 + footer 容器负责换行”的策略，减少 Teleport、局部自定义样式和全局按钮兜底之间继续打架的概率。
- **弹窗头部与操作区容错增强**：通用 modal header 新增长文本换行保护，模型价格编辑、MCP 确认、设置页弹窗等 footer 统一补齐 `flex-wrap`，降低后续继续增加文案或按钮数量时的回归风险。

---

# Code Switch v2.7.17

## 修复
- **首页模型列表粘贴 JSON 导入弹窗仍然只显示模糊遮罩**：修复在首页供应商模型列表中点击 `粘贴 JSON 导入` 后，页面整体变模糊但导入面板本身不出现的问题；现在会稳定弹出可见的 JSON 粘贴输入框与操作区。
- **导入弹窗共享链路不稳定**：修复导入弹窗继续复用共享 `InlineModal` 时，在首页供应商模型列表场景下仍可能受共享层级、过渡与关闭链路影响而表现异常的问题。

## 技术改进
- **导入弹窗改为独立 Teleport 实现**：`ProviderModelListModal` 中的 JSON 导入弹窗不再走共享 `InlineModal`，改为独立挂载到 `body` 的专用弹窗结构，并单独管理遮罩、标题区、底部操作区与动画。
- **导入弹窗单独接管层级与焦点**：新增导入弹窗自己的 `modalStack`、`Escape` 关闭、遮罩点击关闭与焦点恢复逻辑，避免被外层模型列表弹窗或其它内层弹窗误伤。

---

# Code Switch v2.7.16

## 修复
- **首页模型列表导入弹窗仍然不可见**：修复部分 WebView / 页面容器环境下，点击 `粘贴 JSON 导入` 后只出现遮罩模糊、输入框面板仍然不显示的问题；现在导入弹窗会稳定出现在最上层并可直接粘贴 JSON。
- **通用弹窗受祖先容器层叠上下文影响**：修复 `InlineModal` 仍挂在原组件树中时，可能被祖先容器的层叠上下文、滚动裁切或 `position: fixed` 兼容性问题影响，导致“遮罩可见、面板失踪”的异常显示。

## 技术改进
- **`InlineModal` 统一 Teleport 到 `body`**：所有内联弹窗统一脱离原组件树挂载到 `body`，降低嵌套弹窗、滚动容器与 WebView 组合场景下的层级不确定性。
- **弹窗视口尺寸与滚动兜底补强**：通用弹窗补齐 `max-width`、容器滚动和 `min-height` 约束，避免小视口或宿主容器特殊样式下弹窗被挤出可视区域。

---

# Code Switch v2.7.15

## 修复
- **首页模型列表导入弹窗显示异常**：修复在首页供应商模型列表中点击 `粘贴 JSON 导入` 后，只出现全局模糊遮罩、看不到输入框与操作区的问题；现在导入弹窗会稳定显示在模型列表弹窗之上。
- **内层弹窗层级冲突**：修复导入弹窗、调试详情弹窗与模型详情弹窗嵌套挂在外层模型列表弹窗内部时，可能被父容器层级与布局影响导致显示异常的问题。

## 技术改进
- **内层弹窗改为同级挂载**：`ProviderModelListModal` 中的导入、调试、详情三个 `InlineModal` 统一改为与外层模型列表弹窗同级挂载，减少嵌套弹窗在滚动容器、层叠上下文和遮罩管理上的不确定性。

---

# Code Switch v2.7.14

## 新功能
- **首页模型列表支持手动粘贴 `/api/pricing` JSON 导入**：当供应商接口被浏览器挑战页拦住、程序端无法直接拿到 JSON 时，可在模型列表弹窗中点击 `粘贴 JSON 导入`，直接导入浏览器里拿到的完整返回结果并展示模型与价格。
- **浏览器挑战页支持明确识别与引导**：当 `/api/pricing`、`/api/available_model`、`/api/user_group_map` 或 `/v1/models` 返回 challenge 脚本 / 挑战页时，界面会明确提示当前接口需要先在浏览器完成验证，不再只剩一条 `invalid character '<'` 的 parse error 折磨人。
- **导入失败支持单独查看调试详情**：手动导入 JSON 失败时，导入弹窗内可直接打开对应的调试详情，查看本次粘贴内容、解析失败原因与保留的调试上下文，排查 challenge / 非完整 JSON / `success=false` 更直接。

## 修复
- **手动导入异步竞态**：修复导入过程中关闭弹窗或重复触发导入时，旧请求结果仍可能回写当前模型列表的问题；现在导入流程增加请求序号保护，过期结果会被安全丢弃。
- **挑战页提示路径误导**：修复所有 challenge 提示一律指向 `/api/pricing` 的问题；现在会按真实命中的接口返回更准确的引导文案，避免 one-hub 组合接口场景下把排错方向带歪。
- **挑战提示重复显示**：修复错误区将同一条 challenge 文案重复渲染两遍的问题，保持提示清晰但不过度吵闹。
- **导入成功后旧搜索条件误伤结果**：修复用户导入成功后仍保留旧模型搜索关键字，导致页面看起来像“导入成功但没有任何模型”的假空列表问题；现在导入成功会自动清空当前搜索条件。

## 技术改进
- **手动导入失败统一走结构化响应**：`ImportProviderModelPricingJSON` 失败时现在也会返回 `fetchError + debug` 结构，和普通接口请求共用同一套调试展示链路，降低前后端错误分支分叉。
- **`InlineModal` 新增关闭禁用能力**：通用弹窗组件新增 `closeDisabled` 控制，支持在关键异步流程中禁用关闭按钮、遮罩关闭与 `Escape` 关闭，后续其他高风险提交类弹窗也能复用。
- **模型列表来源操作区布局收口**：顶部“当前命中来源”和操作按钮改为稳定分区布局，桌面端与移动端的换行逻辑都更可控，减少后续继续加入口时的样式补丁。

---

# Code Switch v2.7.13

## 新功能
- **首页模型列表新增接口调试详情弹窗**：在供应商模型列表弹窗中新增 `调试详情` 入口，可查看本次请求的来源选择、实际命中来源、请求地址、请求头、响应头、响应状态、耗时与原始返回体，排查供应商接口兼容性时不再只能盯着一句 parse error 瞎猜。
- **错误态支持直达调试信息**：当 `/api/pricing`、`one-hub` 或 `/v1/models` 请求失败时，错误提示区会直接提供“查看完整调试信息”入口，方便第一时间对照本次调用上下文。

## 修复
- **`/api/pricing` 认证方式默认值不合理**：修复首页模型列表在未显式配置认证方式时一律先用 `bearer` 请求，导致部分 Claude / Anthropic 风格站点明明手动请求可返回 JSON、程序里却拿到 HTML 登录页或面板页的问题；现在会按平台优先选择更合理的默认认证方式。
- **HTML 假成功响应不会再直接卡死**：修复上游在认证不匹配时返回 `200 + HTML 页面`，前端只看到 `invalid character '<'`、却拿不到实际返回内容的问题；现在会识别疑似 HTML 响应并继续尝试下一种认证方式，同时保留完整调试上下文。
- **接口选择区信息层级更清晰**：优化首页模型列表顶部工具栏布局，保持“接口选择 + 下拉框”同一行，说明与命中来源单独展示，模型搜索独占下一行，避免控件错位挤成一团。

## 技术改进
- **供应商模型价格响应新增结构化调试字段**：`ProviderModelPricingResponse` 现支持返回 `fetchError` 与 `debug` 信息，统一承载多次认证尝试、请求头脱敏快照、响应头与原始响应体，方便前端弹窗直接复用。
- **调试信息默认脱敏与长度保护**：认证相关请求头会自动脱敏，超长响应体会受控截断，既能保留排错价值，也避免把敏感信息和超大文本直接怼到界面上。

---

# Code Switch v2.7.12

## 新功能
- **首页模型列表支持切换数据来源接口**：在首页供应商模型列表弹窗中新增数据来源下拉，可在 `自动判断`、`/api/pricing`、`one-hub 组合接口` 与 `/v1/models` 之间切换，排查上游站点兼容性时不用再靠猜。

## 修复
- **手动切换来源污染共享计价缓存**：修复在模型列表弹窗里手动切换数据来源后，结果可能误写入共享 `provider pricing cache`，进而影响日志页 provider API 计价的问题；现在只有 `auto` 探测路径会回填共享缓存。
- **来源标签展示口径不一致**：修复来源下拉、本次实际命中来源与顶部数据来源标签各说各话的问题；现在统一走同一套来源标签映射，避免界面半中半英、语义打架。
- **来源切换说明不清晰**：修复固定来源和自动探测共用一套提示文案、用户看不出当前是否还会继续自动探测其他接口的问题；现在会按 `auto / fixed` 两种模式分别提示。

## 技术改进
- **来源类型与请求链路收口**：前端 `ProviderModelPricingResponse.pricingSource` 与来源选择参数统一收口为 `ProviderModelPricingSource`，降低前后端来源字符串漂移的风险。
- **模型列表请求竞态保护补强**：模型列表弹窗新增请求序号保护，快速切换来源或关闭重开时旧请求不会再覆盖新结果，状态重置也更干净。

---

# Code Switch v2.7.11

## 修复
- **日志分页器右侧留白**：修复日志主列表与「日志存储」按天详情列表分页器默认整行铺开、右侧留白明显的问题；分页控件现在统一贴齐右侧，表格底部布局更稳。
- **分页按钮尺寸偏大**：修复日志分页器在表格底部显得过于笨重的问题；页码按钮、上下页按钮、每页条数选择与跳页输入统一切换为更紧凑的尺寸，阅读重心不会再被分页器抢戏。
- **页码间距过松**：收紧日志分页器页码之间的横向间距，避免统一分页组件落到日志页后看起来“松松垮垮”，提升信息密度与整体观感。

## 技术改进
- **分页器新增场景化布局能力**：`BasePagination` 新增 `align` 与 `compact` 两个可复用配置，支持按业务场景切换右对齐和紧凑模式，避免后续再为单个页面额外堆样式补丁。

---

# Code Switch v2.7.10

## 新功能
- **日志页分页器统一升级**：日志主列表与「日志存储」弹窗按天详情列表统一切换为新的分页组件，支持页码切换、每页条数选择与跳页输入，交互和视觉风格保持一致。

## 修复
- **日志主列表假分页**：修复日志主列表只拉取前 `200` 条记录、再由前端本地切页导致后续页数据不完整的问题；现在会按当前页和每页条数走后端真实分页请求。
- **快速切页请求串页**：修复日志主列表在快速切页或切换每页条数时旧请求可能覆盖新页结果的问题，新增分页请求竞态保护，避免页码和数据对不上。
- **分页控件状态割裂**：修复主列表与详情列表分页交互和样式不一致的问题，避免同一页面出现两套分页体验。

## 技术改进
- **日志分页状态流收口**：`useLogsPageData` 改为统一管理 `page / pageSize / total / setPage / setPageSize`，主列表分页逻辑与详情列表保持同一套心智模型。
- **分页回归测试补强**：新增 `useLogsPageData` 单测，覆盖初始化分页、切页重拉、切换 `page size` 以及总数缩水后的越界回退，降低后续回归风险。

---

# Code Switch v2.7.9

## 新功能
- **请求明细热力墙新增年份选择**：在「日志 → 日志存储」弹窗的请求热力墙标题右侧新增年份下拉，默认选中当前年份，可按年份切换查看整年的请求分布。

## 修复
- **热力墙右侧大片空白**：修复请求热力墙按最近一年裁剪时内容靠左、右侧留出大块空白的问题；现在会按所选年份渲染全年日期格子，并在弹窗中居中展示。
- **切换年份后的重复请求**：修复刷新、清理日志或切换年份后，当天请求明细被重复拉取的问题，避免无意义的重复查询和状态抖动。
- **年份下拉箭头样式被覆盖**：修复年份选择器局部样式误伤全局 `mac-select` 箭头的问题，避免下拉框视觉回退。

## 技术改进
- **按年份热力墙查询链路补齐**：新增按年份返回请求热力墙统计与可选年份列表的接口，前后端围绕年度视图统一数据边界。
- **全年日期矩阵与闰年覆盖补强**：热力墙矩阵支持按指定年份生成全年日期格子，并补充闰年场景测试，降低年度切换时的日期边界回归风险。

---

# Code Switch v2.7.8

## 技术改进
- **日志页完成模块化解耦重构**：完成 `Logs/Index.vue` 拆分收口，页面头部、筛选区、汇总卡片、图表区、表格、分页与 `存储 / 金额 / Token / Payload` 四类弹窗均已独立，后续继续扩展日志页时不再需要在单个超大文件里来回翻锅。
- **页面根组件瘦身为顶层 orchestrator**：`frontend/src/components/Logs/Index.vue` 已由 `1843` 行收敛到 `508` 行，状态装配与副作用边界更清晰，自动刷新、筛选联动、tooltip、Payload 详情、定价明细与存储弹窗控制器均已沉到独立 `composables`。
- **样式与结构稳定性补强**：日志页原有样式整体迁移到 `frontend/src/components/Logs/Index.css`，模板结构与 class 命名保持一致，拆分过程中尽量保证界面视觉与交互行为不漂移。
- **迁移文档与校验同步收口**：`qianyi/logs-index-migration.md` 与 `qianyi/README.md` 已同步标记完成，并补充最终落地结果；前端类型检查 `vue-tsc --noEmit` 已通过，降低后续继续演进时的回归风险。

---

# Code Switch v2.7.6

## 修复
- **热力墙 Tooltip 指标语义纠正**：修复「日志 → 日志存储」弹窗中把日志正文占用误写成“内存占用”的问题；现在 Tooltip 会明确展示为 `Payload 占用`，避免把日志明细大小和进程内存概念混到一起。
- **Payload 未采集状态更清晰**：修复有请求明细但未开启 Payload 采集时 Tooltip 数值语义模糊的问题；现在会明确显示“未采集 / Not captured”，不再拿空值或占位符糊弄过去。

## 技术改进
- **Payload 指标改为写入时持久化**：新增 `request_log.payload_bytes` 与 `request_log.payload_captured` 字段，日志写入时即完成 Payload 字节数与采集状态计算，避免热力墙 Tooltip 每次悬浮都重新扫描大文本列，降低日志量较大时的聚合开销。
- **老库兼容与历史数据回填**：启动时会为旧版 `request_log` 表自动补齐 Payload 指标列，并对已有明细做一次回填；热力墙查询在新列不存在时也会自动回退到旧算法，避免升级后直接查崩。
- **Payload 聚合链路补强**：热力墙日统计新增 `payload_captured_requests`，前端 Tooltip 会区分“真实 0 B”与“当天没有采集到 Payload”，展示语义更稳当。

---

# Code Switch v2.7.5

## 修复
- **请求明细热力墙长条格子**：修复「日志 → 日志存储」弹窗中有数据日期单元在部分 WebView 环境下仍被拉成长条、与无数据灰色格子尺寸不一致的问题；现在所有日期格子会以统一方格尺寸稳定渲染。
- **交互节点原生样式残留**：修复热力墙可点击日期继续沿用原生 `button` 节点时，浏览器默认盒模型与最小尺寸参与布局计算，导致同列高度异常的问题。

## 技术改进
- **热力墙单元统一为非 button 节点**：日志存储热力墙的日期单元统一改为 `div` 渲染，并通过 `role="button"`、`tabindex` 与键盘事件保留交互语义，避免再次被原生按钮样式和宿主环境默认行为污染。
- **交互类名边界收紧**：热力墙单元样式从旧的 `logs-storage-heatmap-cell` 收口到新的 `logs-storage-heatmap-day` 体系，交互态、选中态与 focus 态的样式边界更清晰，后续维护不容易再跟全局按钮补丁打架。

---

# Code Switch v2.7.4

## 修复
- **请求明细热力墙数据格子堆积**：修复「日志 → 日志存储」弹窗中有请求数据的日期格子挤压重叠、视觉上糊成一片绿色的问题；现在所有日期单元都会按统一网格尺寸稳定排布。
- **交互格子尺寸异常**：修复热力墙中可点击日期因渲染为 `button` 后被全局按钮样式污染，导致宽高和内边距异常放大的问题。

## 技术改进
- **热力墙按钮样式隔离**：将日志存储热力墙单元从全局 `button` 通用样式白名单中剥离，避免语义化交互节点再被误套成常规操作按钮。
- **格子尺寸约束补强**：为 `.logs-storage-heatmap-cell` 增加显式块级显示与尺寸重置，统一 `button / div` 两种节点形态的盒模型表现，降低同类 UI 回归风险。

---

# Code Switch v2.7.3

## 修复
- **日志存储热力墙样式重新对齐首页**：修复「日志 → 日志存储」弹窗中按天热力墙与首页样式不一致的问题；现在空白日期格子会按统一尺寸稳定渲染，不再出现左侧大面积“像没画出来”的塌缩现象。
- **弹窗热力墙布局稳定性增强**：修复无请求日期单元因使用 `inline` 节点导致宽高撑不开的问题，热力墙在弹窗宽布局下也能保持完整网格结构与一致间距。

## 技术改进
- **热力墙单元结构统一**：日志存储弹窗中的热力墙单元统一收敛为 `button / div` 结构，交互语义与非交互语义边界更清晰，后续样式维护不再依赖局部补丁兜底。
- **共享格子基础样式下沉**：将 `contrib-cell` 的块级显示与盒模型约束收敛到共享样式，首页与日志弹窗复用同一套尺寸基础规则，降低后续热力墙 UI 再次分叉的风险。
- **无障碍噪音收敛**：无请求日期格子不再进入辅助技术朗读树，仅保留可操作日期的无障碍描述，减少屏幕阅读器面对整面热力墙时的无效播报。

---

# Code Switch v2.7.2

## 新功能
- **日志存储弹窗升级为按天清理工作台**：在「日志 → 日志存储」弹窗中完善请求明细热力墙与按天日志列表联动，点击热力墙中的日期后可直接查看当天日志并按日期删除对应明细，释放存储空间更直观。
- **请求日志支持分页拉取**：前后端新增请求日志分页查询能力，按天明细列表可按页浏览，避免单日日志量较大时一次性加载过多数据导致弹窗阅读和交互发闷。

## 修复
- **热力墙 Tooltip 被弹窗遮挡**：修复日志存储弹窗内热力墙 Tooltip 层级过低、悬停后看起来像“没反应”的问题；现在会稳定显示对应日期、日志条数与热度等级。
- **按天日志分页假动作**：修复按天日志列表点击上一页 / 下一页时只更新页码、不重新请求数据的问题；现在翻页会真实切换到对应页的日志数据。
- **删除确认状态残留**：修复关闭日志存储弹窗后按日期删除的确认状态未清理，导致再次打开弹窗时可能残留上一次目标日期的问题。
- **多语言文案缺失**：补齐按天列表区间展示文案等中英文 i18n key，避免界面直接渲染原始翻译 key，影响可读性。

## 技术改进
- **日志查询链路收敛为分页结果模型**：前后端统一新增 `RequestLogPageResult`，并抽离 request_log 条件过滤、分页参数归一化与 provider 匹配逻辑，统一支持 `provider_id` 优先、`provider` 名称回退。
- **分页筛选测试补强**：新增请求日志分页定点测试，覆盖 `provider_id` 优先命中和缺失 `provider_id` 时回退到 `provider` 名称查询两条关键路径，降低日志列表分页与过滤回归风险。

---

# Code Switch v2.7.1

## 新功能
- **日志存储弹窗新增请求明细热力墙**：在「日志 → 日志存储」弹窗内新增按天展示的请求明细热力墙，样式与首页热力墙保持一致，可直观看到最近一年每天仍然保留的请求明细数量。
- **支持按日期删除日志明细**：支持从热力墙中选中某一天并直接删除该日期下的请求明细日志，同时联动移除对应日期的统计桶，避免“明细删了、热力墙还亮着”的假象。

## 修复
- **热力墙数据语义修正**：修复日志存储弹窗错误复用首页聚合统计数据源、导致“请求明细热力墙”在清空明细后仍显示历史活跃度的问题；现在热力墙只基于现存 `request_log` 数据生成。
- **重复请求修复**：修复日志存储弹窗从第二次打开开始会重复初始化并额外触发一次热力墙 reload 的问题，降低无效请求和状态抖动。
- **删除反馈不准确**：修复按日期删除日志后前端一律提示“删除成功”的问题；现在后端返回真实删除计数，前端会区分“实际删除了多少日志/统计桶”和“当天本来就没有可删数据”。
- **无障碍文案兼容性**：修复热力墙单元格 `aria-label` 混入硬编码中文标点的问题，改为中英文分别走 i18n 模板，提升可访问性和多语言一致性。

## 技术改进
- **自适应热力墙支持注入数据源**：`useAdaptiveHeatmap` 新增可注入 fetcher，首页继续沿用 usage stats，日志页则可独立接入“现存请求明细日统计”接口，样式复用与数据语义彻底解耦。
- **日粒度数据兼容增强**：热力墙矩阵构造逻辑新增对 `YYYY-MM-DD` day-only 数据键的支持，便于直接复用每日统计结果构建热力墙。
- **测试覆盖补强**：补充自定义 fetcher、day-only key、按日期删除计数与“仅基于现存 request_log 生成热力墙”的定点测试，降低后续回归风险。

---

# Code Switch v2.7.0

## 修复
- **Windows 安装包构建恢复**：修复 GitHub Actions 在 Windows 打包阶段依赖 `Chocolatey + nsis.portable` 单一路径时，遇到源超时会直接导致 `makensis.exe` 缺失、安装包构建失败的问题；现在会优先复用现有 `makensis`，并在安装阶段自动切换 `nsis / nsis.install / winget` 多级兜底来源。
- **NSIS 安装网络抖动容错增强**：为 Windows 发布流程中的 `NSIS` 安装补充重试机制，降低 `Chocolatey` 偶发 `504 Gateway Time-out` 导致整条 release 流水线失败的概率。

## 技术改进
- **makensis 解析链路增强**：发布流程会在多个常见安装路径与 `Chocolatey` 目录中递归查找 `makensis.exe / .cmd / .bat`，并将解析到的可执行路径显式导出到后续步骤，减少“装上了但 PATH 没吃到”的隐性失败。
- **Windows 打包兜底顺序优化**：在真正执行 `NSIS` 打包前，再次执行跨源 fallback 校验，优先尝试 `winget`，再补 `nsis.install / nsis.portable / nsis`，将失败定位从“神秘丢失 makensis”收敛为更明确的依赖安装问题。

---

# Code Switch v2.6.99

## 新功能
- **热力墙 Tooltip 分组展示升级**：首页热力墙 Tooltip 现按“请求概览 / Token 统计 / 金额信息”分区展示，并在顶部补充“亮度参考 / 当前亮度值 / 当前亮度等级 / 占峰值比例”摘要卡片，切换亮度指标后更容易看明白颜色为什么会变。

## 修复
- **低亮度等级语义误导**：修复热力墙 Tooltip 将 `level 1` 低亮度错误渲染成接近无数据的中性色问题；现在非零亮度会按真实强度给出明确提示色，避免“格子有颜色、说明却像没数据”的错觉。
- **长数值布局挤压**：修复 `Total Tokens`、金额等长数值在 Tooltip 中可能撑破右侧列宽、导致标签和值错位的问题；现在摘要卡片与分组明细都支持受控换行与最大宽度约束。

## 体验优化
- **当前亮度参考项高亮**：Tooltip 中当前正在驱动热力墙亮度的那一项会在对应分组中同步高亮，分组标题色也会跟随当前指标变化，用户切换 `请求数 / 金额 / Tokens` 后能立刻定位颜色依据。
- **数字排版更整齐**：Tooltip 右侧值列统一做了 tabular 数字对齐、重点值 pill 化和分组卡片化处理，在深浅主题下都更利于扫读。

## 技术改进
- **Tooltip 指标配置收拢**：将热力墙 Tooltip 的分组、指标、tone 映射与 active 状态生成逻辑统一抽象为配置驱动，减少重复硬编码，后续继续扩展亮度指标时不容易漏改。
- **样式兼容性增强**：为分组背景补充 fallback，降低 `color-mix` 在旧环境中的退化风险；并补充前端类型检查与热力墙单测验证，确保亮度解释层调整不影响原有热力图计算链路。

---

# Code Switch v2.6.98

## 新功能
- **热力墙亮度指标可切换**：在「设置 > 应用设置」新增热力墙亮度参考指标，支持按 `请求次数 / 金额 / 总 Token / 输入 Token / 输出 Token / 推理 Token` 驱动首页热力墙亮度变化。
- **热力墙 Tooltip 可解释性增强**：首页热力墙 Tooltip 新增“亮度参考 / 当前亮度值 / 当前亮度等级 / 占峰值比例”，并补充 `Total Tokens` 展示，切换指标后能直接看清颜色依据。

## 修复
- **热力墙亮度计算口径统一**：修复热力墙切换到 `cost / total_tokens / input_tokens / output_tokens / reasoning_tokens` 后仍可能沿用请求数思路理解颜色深浅的问题；按小时与按天模式现统一基于当前亮度指标做归一化。
- **总 Token 聚合口径修复**：热力统计补充 `cache_read_tokens` 聚合并输出 `total_tokens`，前端优先使用后端透出的 `total_tokens`，缺失时再回退 `input + output + cache_read`，不再把 `reasoning_tokens` 混进总量。
- **Tooltip 首次定位边界修复**：修复热力墙 Tooltip 在内容增多后首次悬浮可能贴边或上下位置不准的问题，改为渲染后按真实尺寸二次定位。

## 体验优化
- **热力墙显示配置摘要更清晰**：设置页热力墙显示配置摘要新增当前亮度参考指标展示，双语文案与本地缓存同步补齐。
- **亮度数值展示更直观**：Tooltip 中的金额指标支持更细粒度的小额 USD 精度展示，Token 指标在较大数值时同时展示 compact 值与精确值，阅读更顺手。

## 技术改进
- 前后端 `AppSettings` 新增 `heatmap_intensity_metric` 配置项，并完成归一化、持久化与首页初始化联动，避免“设置存了但首页没吃到”的状态分叉。
- 热力墙矩阵单元格新增 `totalTokens / intensityValue / intensityPeakValue` 透传字段，前端 Tooltip 与真实着色逻辑复用同一套数据来源。
- 新增前后端回归测试，覆盖亮度指标归一化、`total_tokens` 聚合、`cache_read_tokens` 回退与按天峰值基线等关键路径。

---

# Code Switch v2.6.97

## 修复
- **缓存 TTL 展示误判**：修复首页供应商模型列表与设置页模型价格列表会把 `family fallback` 推导出的 `1h` 缓存价格误当成显式 `1h` 档位，导致部分模型被错误标记为同时存在 `5m / 1h` 两档缓存的问题；现仅在存在明确 `1h` 价格元数据时才展示独立 `1h` 档位。
- **首页缓存倍率语义含糊**：修复首页模型详情弹窗中“缓存创建倍率”在存在 `1h` 缓存分档时仍沿用泛化命名、容易让人误以为会同时影响所有缓存创建价格的问题；现明确为“缓存创建（5m）倍率”，避免调价语义混淆。

## 体验优化
- **首页 / 设置缓存展示口径统一**：首页供应商模型列表、首页模型详情弹窗与设置页模型价格列表现在统一按“通用缓存 / 5m / 1h”三种口径动态展示，有两个就两个都显示，只有一个就只显示一个，信息表达更直观。
- **模型价格编辑文案更清晰**：设置页模型价格编辑中的基础缓存字段统一收敛为“基础 / 5m”语义，和单独的 `1h` 字段区分更明确，减少编辑时的理解负担。

## 技术改进
- 模型价格服务新增“显式 `1h` 价格”读取能力，并与 provider 列表展示、请求日志成本计算复用同一套解析逻辑，避免“展示一套、计费一套”的维护分叉。
- 新增回归测试，覆盖“显式 `1h` 价格可展示”与“family fallback 不应被当成显式 `1h`”两条关键路径，降低后续继续调整缓存 TTL 逻辑时的回归风险。

---

# Code Switch v2.6.96

## 修复
- **首页模型详情弹窗层级冲突**：修复首页供应商模型列表使用 `BaseModal` 外层弹窗、详情编辑使用 `InlineModal` 内层弹窗时，可能出现详情弹窗被父弹窗遮挡或交互失活的问题；现统一改为同一套 `InlineModal` 结构，确保详情弹窗稳定显示在最上层。
- **首页模型列表信息残留与布局松散**：修复首页供应商模型列表仍残留缓存价格摘要、右侧价格区域空白偏大且信息分布松散的问题；现列表仅保留关键信息摘要，完整价格与缓存信息统一收敛到详情弹窗中展示。

## 体验优化
- **模型详情查看更完整**：首页供应商模型列表改为点击模型直接打开完整详情弹窗，统一展示模型名称、计费方式、所属方、输入 / 输出 / 缓存价格、倍率与缓存倍率来源，并保留缓存倍率调整能力。
- **列表交互与可达性增强**：模型列表项补充键盘可达能力与更明确的无障碍描述，价格区域新增拖拽 / 点击区分逻辑，减少横向查看价格时误触打开详情弹窗的问题。

## 技术改进
- 统一首页供应商模型列表的弹窗层级实现，补充价格区交互状态管理，并清理不再使用的首页模型列表文案键与冗余样式，降低后续继续维护这块 UI 时的分叉成本。

---

# Code Switch v2.6.95

## 修复
- **首页缓存倍率调整交互回归**：修复首页供应商模型列表直接在列表项内展开缓存倍率输入框，导致列表阅读与编辑操作混在一起、视觉拥挤的问题，现改为点击对应模型后通过独立弹窗调整缓存倍率。
- **长模型名显示不全**：修复首页供应商模型列表、设置页模型价格列表与 Claude 官方价格预览中，长模型名被过早截断或可视区域过窄的问题，现统一加宽弹窗与名称区，并放宽模型名称的换行展示策略。

## 体验优化
- **模型列表弹窗宽度统一优化**：首页供应商模型列表、设置页模型价格列表、模型价格编辑弹窗与 Claude 官方价格预览弹窗统一加宽，减少长模型名、长价格字段和多列信息并排时的压缩感。
- **缓存倍率编辑更聚焦**：首页缓存倍率编辑弹窗新增独立说明文案，列表页仅保留“调整缓存”入口，浏览模型价格与编辑倍率的操作边界更加清晰。

## 技术改进
- 统一首页、设置页与 Claude 价格预览的模型名称布局策略，收敛弹窗宽度与列表网格比例，降低后续同类模型列表样式再次分叉的风险。

---

# Code Switch v2.6.94

## 修复
- **模型价格列表横向布局回归**：修复首页供应商模型列表与设置页模型价格列表的价格字段仍会纵向堆叠、边框过重且在窄宽度下频繁换行的问题，现统一改为单行横向滚动展示，并移除右侧价格块的卡片边框效果。
- **设置页价格区误触编辑**：修复「设置 > 模型价格」列表中，横向滑动价格区域时容易误触发行点击并直接打开编辑弹窗的问题，现为价格区域增加独立事件隔离与滚动交互兜底。

## 体验优化
- **价格信息可读性增强**：模型价格列表新增横向滚动提示、右侧渐隐提示与细滚动条样式，模型长名称统一改为单行省略显示，减少列表拥挤和断行带来的阅读噪音。
- **编辑表单双语字段统一**：设置页「模型价格编辑」弹窗中的 `Model / Input / Output / Reasoning / Cache` 等字段统一调整为“中文（English）”表达，降低填写歧义。

## 技术改进
- 抽取模型价格区共享样式，统一首页供应商模型列表与设置页模型价格列表的横向滚动、间距、渐隐提示与无边框展示逻辑，降低后续样式继续分叉的风险。

---

# Code Switch v2.6.93

## 新功能
- **供应商缓存倍率可手动接管**：首页供应商模型列表新增缓存创建、缓存读取的价格与倍率展示，并支持对单个模型分别手动调整 `Cache Create / Cache Read Multiplier`，可按字段单独覆盖，也可一键恢复自动值。
- **模型价格编辑能力增强**：设置页模型价格列表新增缓存创建、缓存读取价格与倍率展示；新增模型时可从已有模型快速套用价格模板（保留自定义模型名），编辑时也支持直接修改模型名称。

## 修复
- **缓存倍率 `0` 值覆盖失效**：修复供应商缓存倍率手动覆盖把 `0` 误判为“未设置”的问题，显式设置 `0` 后不再被 provider / builtin / fallback 结果反向覆盖。
- **单字段覆盖互相污染**：修复只调整缓存创建或缓存读取其中一个倍率时，另一个字段被错误清空或被兜底结果覆盖的问题，现按字段独立保存与解析。
- **模型改名覆盖风险**：修复设置页编辑模型价格时可将当前配置直接重命名覆盖到已有模型的风险，前后端现统一拦截同名冲突。
- **缓存倍率文案误导**：修复缓存倍率输入校验与提示文案将 `0` 错误描述为非法值的问题，现统一为“不能小于 `0`”，并补齐“留空跟随自动倍率”的提示。

## 体验优化
- **价格信息布局更清爽**：首页供应商模型列表与设置页模型价格列表统一为“名称 / 价格 / 倍率”分组排布，减少上下堆叠造成的拥挤感。
- **模型价格表单可读性增强**：设置页新增/编辑模型时，`Input / Output / Reasoning / Cache` 等字段统一采用更清晰的中英混合命名，并补充改名行为说明与缓存倍率联动提示。

## 技术改进
- 新增供应商缓存倍率 override 持久化结构，支持“字段是否显式设置”的独立状态，彻底区分 `0` 与“未配置”。
- 补充缓存倍率与模型改名相关回归测试，覆盖手动 `0` 值、单字段覆盖、重命名冲突等关键边界场景。

---

# Code Switch v2.6.92

## 修复
- **供应商缓存倍率口径回归**：修复首页供应商模型列表为展示缓存倍率而提前覆写供应商缓存数据的问题，恢复日志结算使用原始倍率 + 实时推导的动态口径，避免后续内置价格更新后仍沿用旧缓存结果。
- **缓存能力展示误导**：修复供应商模型列表将内置推导/兜底倍率直接展示成“上游明确支持缓存价格”的问题，现按 `供应商提供 / 内置推导 / 兜底估算` 标明来源，降低误判风险。
- **设置页倍率备注噪音**：修复模型价格列表在无有效倍率时仍展示 `—` 备注的问题，改为仅在存在有效倍率时显示说明。

## 体验优化
- **供应商模型列表信息补全**：首页供应商模型列表新增缓存创建、缓存读取的价格与倍率展示，并对推导值使用更明确的视觉提示。
- **模型价格表单文案统一**：设置页新增/编辑模型时，`Input / Output / Cache` 等字段统一调整为“中文（English）”表达，减少填写歧义。

## 技术改进
- 新增 `resolved cache multiplier` 与来源字段，前端展示值与后端结算值彻底分层，降低后续缓存/定价联动回归风险。
- 补充缓存倍率相关回归测试，覆盖供应商原始倍率保留、`GPT` 兜底、`Claude` 兜底等关键路径。

---

# Code Switch v2.6.91

## 修复
- **日志金额 Tooltip 缓存 TTL 展示修复**：修复缓存创建价格与公式文案固定显示 `5m` 的问题，改为按实际缓存分档动态展示 `5m / 1h`。
- **缓存创建成本口径严格归零**：修复内置价格路径在日志已提供分项成本时仍可能回退估算、导致缓存创建成本被抬高的问题；当日志明确为 `0` 时严格保持 `0`。
- **分项成本识别补全**：`hasBreakdownCostPayload` 新增对 `ephemeral_5m_cost / ephemeral_1h_cost` 的识别，避免历史数据场景下错误回退到估算路径。

## 性能优化
- **缓存分档计算去重**：日志表格 `cache write` 展示新增 `WeakMap` 缓存，减少同一行记录在渲染期间重复执行缓存分档计算的开销。

## 技术改进
- 前端缓存分档逻辑与后端归一化策略保持一致，统一 `cache_create_tokens / ephemeral_5m_tokens / ephemeral_1h_tokens` 的拆分口径，降低金额明细与分档展示不一致风险。

---

# Code Switch v2.6.90

## 新功能
- **请求/响应详情增强**：日志详情弹窗新增 `JSON` 自动格式化与语法高亮，支持一键复制 `原文 / 格式化` 内容，并在超大 payload 场景自动降级为纯文本显示以保证流畅度。
- **脱敏策略可配置**：在「设置 > 应用设置」新增“记录时脱敏”开关，支持在开启请求/响应内容记录后按需选择是否落库前脱敏。

## 修复
- **热力墙配置提示换行**：修复「热力墙显示配置」按钮下方说明被迫换行的问题，改为单行完整展示。
- **日志详情内容空白**：修复部分请求在详情弹窗中请求体/返回体为空的异常链路，补齐 payload 展示与复制相关状态处理。

## 性能优化
- **payload 脱敏快路径**：新增敏感关键词快速短路判断，避免无敏感字段时触发整段正则替换，降低常见请求路径开销。
- **设置读取收敛**：合并单次请求中的 payload 记录/脱敏开关读取，减少重复读取配置文件的 I/O 开销。

## 技术改进
- 新增 `payloadPreview` 前端工具与单测，覆盖 HTML 转义、JSON 高亮、超大内容降级等关键分支。
- 新增后端 payload 脱敏行为测试，覆盖敏感字段脱敏与脱敏开关生效路径。

---

# Code Switch v2.6.89

## 新功能
- **日志请求/响应内容采集开关**：在「设置 > 应用设置」新增“记录请求/响应内容”开关，默认关闭；启用后可在日志页查看请求体与返回体详情。

## 修复
- **日志列表查询性能回退**：`request_log` 列表查询改为字段白名单，避免误读 `request_body/response_body` 大字段导致的查询变慢与内存膨胀。
- **速率异常偏大**：首 token 打点改为仅在检测到真实文本增量时记录，修复部分流式请求在尾包打点导致 `tokens/s` 偶发飙高的问题。
- **详情弹窗并发状态**：修复关闭 payload 弹窗后在途请求仍可能回写状态/提示错误的问题。

## 安全与稳定性优化
- **payload 自动脱敏**：日志 payload 入库前自动脱敏 `api_key / authorization / token / password / secret` 等敏感字段，降低明文落库风险。
- **流式 payload 累积优化**：响应体采集改为字节缓冲累积并在入库前一次性物化，减少高频字符串拼接的内存分配开销。

## 技术改进
- 新增日志字段选择回归测试，确保列表查询不会回退到读取 payload 大字段。

---

# Code Switch v2.6.88

## 修复
- **日志传输标签对比度不足**：修复日志表格「传输」列在深色主题下标签文字与背景过于接近的问题，增强 `流式 / 非流式` 标签的文字、背景与边框区分度，提升可读性与状态辨识效率。

## 体验优化
- **标签主题适配增强**：为传输标签补充分主题配色策略（light / dark 分离），降低不同主题与透明背景叠加时的视觉发灰问题。

## 技术改进
- 统一 `stream-tag` 状态色定义，补齐 `html.dark` 下 `on/off` 专用样式，减少后续样式调整时的回归风险。

---

# Code Switch v2.6.87

## 修复
- **首页供应商首/速统计异常偏大**：修复极短生成窗口样本导致输出速率被放大的问题；`速` 改为基于“总输出 Tokens ÷ 总生成时长”的真实口径，并过滤 `< 50 ms` 的异常窗口。
- **今日统计日边界在 DST 场景偏移**：修复默认时间范围在夏令时切换日可能多算/少算 1 小时的问题，按日查询统一收敛到“本地次日 00:00”边界。
- **日志性能列可读性**：修复性能列上下项过于拥挤的问题，补充首/速之间的显示间距，避免视觉粘连。

## 体验优化
- **首页性能指标可解释性增强**：首/速 tooltip 新增样本数展示（TTFT 样本数、TPS 样本数），并明确过滤口径，便于判断数据波动来源。

## 技术改进
- **性能缓存稳定性**：为 provider 性能缓存增加上限与淘汰整理逻辑，避免长时间运行导致缓存 map 无界增长。
- **回归覆盖补强**：新增默认日边界与极短窗口过滤场景测试，确保统计口径在边界条件下保持一致。

---

# Code Switch v2.6.85

## 体验优化
- **性能列视觉简化**：日志表格的性能列改为轻量标签展示，首 token 使用蓝色「首」标签，输出速率使用绿色「速」标签，信息密度更高且更直观。
- **速率文案精简**：移除“输出速率”前缀文字，直接展示 `xxx tokens/s`，减少视觉噪音并提升扫描效率。

## 技术改进
- 前端性能列渲染与样式统一抽象为 `performance-badge` 体系，便于后续扩展与主题微调。

---

# Code Switch v2.6.84

## 新功能
- **日志性能列（TTFT / TPS）**：日志表格新增性能列，展示首个 token 返回时间（TTFT, ms）与输出速率（tok/s）。
- **后端真实打点落库**：`request_log` 新增 `first_token_sec` 字段并完成兼容迁移，日志查询接口同步透出该字段。

## 修复
- **非流式 TTFT 误计**：修复非流式请求误记录 TTFT 的问题，非流式场景统一显示 `—`，避免伪精度指标误导。
- **Codex 首 token 识别滞后**：补齐 `response.output_text.delta` 事件识别，修复部分流式请求 TTFT 记录偏晚的问题。

## 技术改进
- 新增 TTFT 回归测试，覆盖非流式跳过记录、Codex root delta、Claude/Gemini 文本事件检测等关键路径。
- 前端性能列计算边界增强：仅在流式且生成窗口有效时展示 TPS，异常与无效值自动降级为 `—`。

---

# Code Switch v2.6.82

## 修复
- **按天热力墙周列异常**：修复按天模式在部分场景下列结构异常的问题，现统一为“每列 7 格（Mon→Sun）”，并保持未来日期空格按 0 展示。
- **DST 周边缺列问题**：修复跨夏令时（DST）周的周列计算误差，避免在 DST 切换周出现少一列的情况。
- **一年窗口稳定性**：按天模式固定拉取最近一年数据，避免随窗口宽度变化导致请求天数抖动。

## 性能优化
- **热力统计查询提速**：`LogService.HeatmapStats` 优先读取 `request_log_stats_hourly` 聚合表，显著降低一年窗口下的查询开销；旧库/缺表场景自动回退到明细聚合。
- **前端聚合开销收敛**：热力矩阵按粒度分支聚合，减少按天模式下不必要的 hourly map 占用与遍历成本。

## 技术改进
- 新增前端回归测试，覆盖 DST 周列、按天周列结构与一年窗口请求边界。
- 新增后端回归测试，覆盖 `HeatmapStats` 聚合表路径与缺表回退路径。

---

# Code Switch v2.6.81

## 新功能
- **首页热力墙粒度可配置**：在「设置 > 应用设置」新增热力墙统计粒度选项，支持在 `按小时` 与 `按天` 之间切换；首页热力墙与 tooltip 展示会自动联动更新。

## 修复
- **热力墙粒度切换竞态**：修复切换粒度时旧请求结果可能覆盖新状态的问题，避免出现“设置已切换但热力图仍显示旧粒度数据”。
- **跨年统计错位**：修复热力图时间键缺少年份导致的跨年数据映射错误，确保年末/年初区间统计准确。
- **首页初始化顺序**：调整首页初始化流程为“先加载应用设置，再初始化热力墙”，减少启动阶段的重复请求与闪动。

## 技术改进
- `LogService.HeatmapStats` 输出小时桶时间键升级为 `YYYY-MM-DD HH`，前端同时兼容新旧键格式。
- `useAdaptiveHeatmap` 增加请求 token 保护与初始化态分支，强化异步加载稳定性。
- 新增热力图单元测试，覆盖跨年键解析、按天强度缩放与粒度切换竞态场景。

---

# Code Switch v2.6.80

## 修复
- **首页供应商编辑弹窗宽度自适应**：供应商编辑弹窗支持按视口自适应放宽，修复内容区域过窄导致的横向滚动条问题。
- **弹窗内容防横向溢出**：为供应商编辑表单滚动容器补充 `min-width: 0` 与 `overflow-x: hidden` 约束，避免长字段或复杂控件撑破布局。

## 技术改进
- `BaseModal` 新增 `panelWidth` 可配置能力，支持按业务场景单独定制弹窗宽度，避免全局样式联动风险。

---

# Code Switch v2.6.79

## 新功能
- **日志存储改为弹窗入口**：日志页将“日志存储”从常驻面板改为头部按钮触发弹窗，支持在弹窗中查看数据库占用、明细/统计行数与空间，并保留清理能力。

## 修复
- **首页供应商 Token 口径统一**：首页供应商卡片 `Token` 统一按 `input + output + cache_read` 计算，和日志页口径保持一致；同时补充数值容错，避免异常数据导致显示 `NaN`。
- **供应商编辑弹窗底栏可见性**：修复首页“编辑供应商”弹窗在长内容场景下必须滚动到底部才能看到 `取消/保存` 的问题，底部操作栏改为固定可见且不遮挡最后一行内容。
- **日志筛选操作布局调整**：日志页查询按钮调整到“查询日期类型”后方（供应商下方），并与筛选表单提交行为保持一致。

## 技术改进
- `BaseModal` 新增 `bodyScrollable` 可配置能力（默认保持原行为），支持“内容区滚动 + 固定底部操作栏”布局模式。
- 清理日志页无引用样式，减少样式维护噪音并降低后续误改风险。

---

# Code Switch v2.6.78

## 新功能
- **日志模型分布面板**：在日志页请求卡片与折线图之间新增「模型占比」模块，包含模型占比饼图和模型列表（模型、请求、Token、金额），支持按当前筛选条件联动展示。

## 修复
- **模型统计 Token 口径统一**：后端模型统计与前端展示统一按 `输入 + 输出 + 缓存读取` 计算 `total_tokens`，不再混入 `reasoning`，并保持请求数、金额统计一致。
- **Token 明细缺项修复**：修复日志页 Token 明细弹窗仅重复展示输入的问题，补齐缓存读取项，确保明细与卡片总量可对齐。
- **费用公式可核对性增强**：计费公式中的 token 数值改为「单位缩写 + 原始值」双展示，避免仅显示 `k/M` 导致核算歧义。

## 体验优化
- **Token 展示单位自适应**：折线图坐标轴、tooltip、饼图 tooltip 及模型列表中的 token 统一支持 `k/M/B` 自动换算显示。
- **折线图可读性优化**：图例置顶显示名称、移除折线圆点、为各折线增加向 `x` 轴渐变填充，并将 tooltip 图例色块改为实心；暗色主题下图例与刻度文本颜色同步优化。

## 技术改进
- 新增 `ModelStatsRangeV2` 接口（含 `provider_id` 优先、`provider` 回退策略），前端新增 `fetchModelStatsV2` 与类型定义对接模型统计数据。
- 新增模型统计回归测试，覆盖 `provider_id` 优先匹配、`provider` 回退、token 口径与金额符号保持等关键场景。

---

# Code Switch v2.6.77

## 修复
- **日志 Token 流量口径调整**：日志页顶部「Token 流量」卡片改为 `输入 + 输出 + 缓存读取` 三项汇总，不再将 `reasoning` 计入该卡片，与当前统计口径保持一致。

## 技术改进
- 前端 `statsCards` 汇总公式调整为 `input_tokens + output_tokens + cache_read_tokens`，其余卡片与明细展示逻辑保持不变。

---

# Code Switch v2.6.76

## 修复
- **老库升级启动失败**：修复历史数据库残留 `request_log_stats_hourly_ai` / `request_log_stats_daily_ai` 触发器、但统计表缺失时，启动阶段执行 `request_log` 补列迁移触发 `no such table` 并导致应用退出的问题。升级流程现改为先清理旧统计插入触发器，再执行补列与统计存储重建，保证旧库可平滑升级。

## 技术改进
- 新增 `dropRequestLogStatsInsertTriggersWithDB` 兼容函数，统一处理历史版本残留统计触发器。
- 新增回归测试 `TestEnsureRequestLogTableWithDB_RepairsDanglingStatsTrigger`，覆盖“悬挂触发器 + 缺失统计表”的历史脏库场景，防止后续回归。

---

# Code Switch v2.6.75

## 新功能
- **供应商名称可编辑**：解除首页供应商编辑时对名称的限制，支持在不改变实体身份的前提下直接修改供应商名称。
- **导入支持同名供应商**：导入流程改为按 `API URL + API Key` 识别供应商身份，不再因为同名而阻止导入。

## 修复
- **身份键不一致导致的联动偏差**：统一健康检查、连通性检测、轮询与黑名单联动中的供应商引用规则，避免同一供应商在不同模块中被拆分成不同标识。
- **日志筛选供应商分裂**：修复历史日志中 `provider_id` 为空时，筛选项可能拆成 `name` 和 `id` 两个入口的问题；在“同名唯一映射到单一 id”场景下自动归并。

## 技术改进
- 新增统一身份引用工具，标准化数值 ID / 字符串 ID 的回退策略，降低后续模块接入的身份漂移风险。
- 重构日志供应商引用聚合逻辑，按最新请求时间选择展示名，并补齐同名多实体场景的安全分流。
- 新增导入去重与日志引用归并的回归测试，覆盖“同名可导入”“同名唯一映射归并”“同名多 id 不误合并”等关键边界。

---

# Code Switch v2.6.74

## 修复
- **日志页宽屏适配修复**：修复日志页面在超宽窗口下中间内容被固定宽度限制、无法随窗口拓宽的问题，页面主体现可随容器宽度自适应扩展。

## 体验优化
- **大屏留白更自然**：日志页头部与内容区横向内边距改为 `clamp` 响应式策略，在大屏和小屏下都保持更均衡的阅读间距。

---

# Code Switch v2.6.73

## 修复
- **预览弹窗底栏定位修正**：修复「Claude 官方价格预览」底部操作区未稳定贴底的问题，改为“内容区滚动 + 底栏固定区”结构，确保按钮始终在弹窗最底部可见。
- **同步按钮文本溢出修正**：修复“同步到内置价格表”按钮在窄宽度下文案溢出按钮边界的问题，优化按钮最小宽度与布局策略。

## 技术改进
- **弹窗滚动能力增强**：`InlineModal` 新增可配置 `bodyScrollable` 能力，支持在特定弹窗中由业务组件接管滚动布局。

---

# Code Switch v2.6.72

## 修复
- **预览弹窗操作区可见性**：修复「Claude 官方价格预览」在长列表滚动时底部按钮区被滚出可视区的问题，底部操作区改为常驻显示。
- **同步按钮文案溢出**：修复「同步到内置价格表」按钮在窄宽度下文字超出的问题，优化按钮布局与自适应换行策略。
- **同步来源时间可追溯**：模型价格列表中“网络同步”标签新增悬停提示，显示该模型最近网络同步时间（`yyyy-MM-DD HH:mm:ss`）。

## 技术改进
- **价格来源时间透传**：`ListModelPricing` 返回新增 `source_updated_at` 字段，前端类型与文案同步补齐。
- **交互细节优化**：预览弹窗底部操作区样式增强（sticky + 分隔线 + 按钮最小高度），提升可操作性与一致性。

---

# Code Switch v2.6.71

## 新功能
- **Claude 官方价格预览弹窗**：在「设置 > 模型价格」中点击 `同步 > Claude` 后，先展示官方价格预览弹窗，再由用户确认是否写入内置价格表。
- **预览信息增强**：预览列表完整展示官方模型名、Input、Output、Cache Hit、Cache 5m、Cache 1h，并显示与内置模型 key 的映射关系（含已映射/未映射状态）。

## 修复
- **官方价格页表头兼容性**：修复仅匹配旧表头导致的“未找到 Claude Model pricing 表格”问题，新增对 `Base Input Tokens`、`Output Tokens` 等表头变体兼容。
- **预览异步竞态**：修复弹窗关闭重开时旧请求回写 UI 的竞态问题，避免出现过期预览内容覆盖当前状态。
- **预览与同步数据漂移**：同步逻辑改为优先消费预览阶段缓存（一次性），避免确认同步时二次抓取导致结果不一致。

## 技术改进
- **共享样式抽取**：抽取模型列表共享样式到 `provider-model-list-shared.css`，统一首页模型列表与 Claude 价格预览弹窗视觉结构。
- **回归测试补齐**：新增表头别名解析、预览行构建、预览缓存 TTL 与一次性消费等测试用例，降低后续回归风险。

---

# Code Switch v2.6.70

## 修复
- **CI 打包 TypeScript 阻断**：修复「设置 > 模型价格」同步流程中 `task` 变量在赋值前被闭包引用导致的 `TS2454` 报错，恢复 `vue-tsc` 与自动打包流程。

---

# Code Switch v2.6.69

## 新功能
- **模型价格网络同步入口**：在「设置 > 模型价格」弹窗新增 `同步` 按钮与供应商选择小弹窗，支持一键同步 Claude 官方价格页。
- **价格来源标签增强**：模型价格列表新增来源标签，明确区分 `内置价格`、`网络同步`、`手动修改`。

## 修复
- **官方价格表列顺序变动风险**：修复抓取 Claude 官方价格表时依赖固定列下标的隐患，改为按表头索引解析，避免列重排导致价格错位写入。
- **同步状态并发误判**：修复同步任务在弹窗关闭后被误置空闲的问题，改为基于真实异步任务状态管理，避免重复触发同步请求。

## 技术改进
- **映射规则可维护性**：将 Claude 官方展示名到内置模型 key 的映射改为常量映射表，替代分支式硬编码，便于后续模型增量维护。
- **同步结果可观测性**：同步接口新增 `unchanged_models` 与 `unrecognized_models` 返回字段，前端增加同步摘要与未识别模型提示。
- **回归测试补齐**：新增表头重排解析、官方模型映射完整性、未识别模型回传等测试用例，降低价格同步回退风险。

---

# Code Switch v2.6.64

## 修复
- **流式缓存拆分计费偏差**：修复 Claude 流式日志在 `cache_create` 明细分片到达顺序不一致时可能出现 `5m + 1h > total` 的问题，避免缓存写入 token 被放大导致金额偏高。
- **缓存明细归一化口径**：统一 `cache_create_tokens / ephemeral_5m_tokens / ephemeral_1h_tokens` 的归一化策略，确保拆分明细不会反向抬高总量。

## 体验优化
- **日志缓存标签降噪**：日志页 `Cache write` 的 `5m/1h` 徽章改为仅在数值大于 `0` 时显示，减少无效 `0` 标签干扰阅读。

## 技术改进
- **Provider API 计费路径优化**：仅在存在 `1h` 缓存 token 时才计算 `ephemeral_1h` 单价，减少不必要的定价计算开销。
- **回归测试补齐**：新增流式分片顺序一致性测试与缓存拆分归一化/补齐测试，覆盖 `total/5m/1h` 的关键边界场景，降低计费回退风险。

---

# Code Switch v2.6.63

## 修复
- **接口计费口径对齐**：修复 `provider_api` 场景下 `cache_create` / `cache_read` 误按 input 单价计算的问题，现已按缓存倍率计费，日志金额与供应商后台口径一致。
- **接口价格日志可追溯**：补全「日志 > 使用请求明细」中接口价格的分步计算展示，支持按本次请求的 token 与分项成本还原计算过程。

## 技术改进
- 供应商价格解析增加缓存倍率字段兼容（`cache_create_*` / `cache_read_*`）。
- 新增计费回归测试，覆盖缓存倍率回退与接口缓存倍率映射。

## 下版本代办
- 按 Provider 实际分组应用 `group_ratio`（当前仅使用 `default` 组倍率）。

---

# Code Switch v2.6.57

## 体验优化
- **WebDAV 入口收敛**：将 WebDAV 同步能力收敛到「应用设置」中的单行入口，设置页更清爽；点击“配置与同步”后在弹窗内集中完成配置、测试与同步操作。
- **管理弹窗更稳**：WebDAV 管理弹窗宽度改为响应式（窄窗口不再横向溢出），加载配置期间入口按钮自动禁用，避免误触后看到不可操作状态。

## 修复
- **暗色模式按钮对比度**：修复“新版本已准备就绪”通知在 dark 模式下按钮背景与文字可读性异常的问题，统一了主/次按钮在不同状态下的视觉表现。
- **通知按钮样式串改**：修复全局 `button` 兜底样式对更新通知按钮与 spinner 的覆盖问题，避免尺寸、间距被全局规则意外改写。

## 技术改进
- 新增 `components.general.webdav.manage` 多语言文案键（中文/英文），避免复用无关模块文案导致后续维护耦合。
- 更新通知 dark 卡片背景增加 fallback，兼容不支持 `color-mix` 的环境。

---

# Code Switch v2.6.54

## 修复
- **托盘已用额度不再固定**：恢复「已用金额 = 实际消耗 + 调整值」口径，修复 Claude 在校准后已用值不再增长的问题。
- **设置保存稳定性**：`persistAppSettings` 增加防抖 + 排队重放 + 页面卸载前 flush，修复快速修改或切页时最后一次改动可能丢失的问题。

## 体验优化
- **托盘轮询降噪**：托盘窗口不可见时暂停分钟级刷新，恢复可见后立即补刷，降低无效数据拉取。
- **窗口高度同步更稳**：托盘高度缓存增加生命周期重置，减少重复 IPC 调整并避免重挂载后的高度不同步。

## 技术改进
- **防抖间隔可配置**：新增 `VITE_SETTINGS_PERSIST_DEBOUNCE_MS`（默认 `150`，范围 `0-2000`），并补充空值兜底逻辑。
- **开发文档对齐构建链路**：README 中 Node.js 要求更新为 `20.19+`（或 `22.12+`）。

---

# Code Switch v2.6.53

## 新功能
- **日志明细显示计价来源**：在「日志 > 使用请求明细」模型列新增“计价来源”标记，明确区分“接口模型价格 / 内置价格表 / 无可用价格”。
- **供应商模型价格自动刷新**：应用启动时自动刷新已配置供应商的模型价格缓存；点击供应商卡片时会再次刷新当前供应商的模型价格。

## 修复
- **0 价模型误回退**：修复接口价格为 `0` 时被误判为“无价格”并回退内置计费的问题，接口可用时始终优先使用接口价格。
- **金额 tooltip 口径不一致**：修复 `provider_api` 来源仍按内置价格公式拆解的误导展示，改为显示接口计价说明与日志记录金额。

## 技术改进
- 供应商价格刷新链路增加“点击节流 + in-flight 去重 + 启动限并发”，减少重复请求与启动瞬时压力。
- 请求日志计费链路完善为“接口缓存命中优先，未命中/异常回退内置价格表”，并将来源写入 `request_log.price_source` 便于审计与排查。

---

# Code Switch v2.6.52

## 体验优化
- **定价弹窗统一风格**：设置页「模型价格」弹窗对齐首页「模型列表」弹窗的展示风格（搜索 + pill 筛选 + 卡片列表），更一致也更好读。

---

# Code Switch v2.6.51

## 修复
- **CI 打包 TypeScript 报错**：主页 `GeminiProvider` 类型改为从 Wails bindings 推导，避免 `envConfig` 类型漂移导致 `vue-tsc` 失败。

--- 

# Code Switch v2.6.50

## 新功能
- **供应商模型列表**：首页每个供应商卡片新增「模型列表」按钮，一键查看该供应商的模型列表与定价信息。
- **厂商筛选 + 搜索**：按模型名/owner 识别厂商并分组展示，支持搜索过滤，定位模型更快。

## 体验优化
- **鉴权自动兜底**：拉取模型与定价时支持 Bearer / x-api-key 自动 fallback，减少“Key 没错但就是拉不下来”的玄学问题。
- **无定价提示**：当站点不提供定价接口时，弹窗明确提示仅展示模型名称，避免一屏 `—/M` 把人看懵。

---

# Code Switch v2.6.44

## 体验优化
- **日志页顶部栏吸顶**：顶部「返回首页 / 刷新」操作栏固定，滚动仅滚动下方内容区，避免操作按钮被滚走。
- **查询区布局收敛**：收紧「查询」与表单之间的间距，并将「查询」按钮移动到「日志存储」刷新按钮后方，按钮间距更统一。

---

# Code Switch v2.6.38

## 修复
- **测试污染用户配置**：修复 `providerrelay_models_test` 在运行时写入真实 `~/.code-switch` 目录的问题，避免测试把首页 Claude/Codex 供应商配置覆盖掉。

## 技术改进
- 新增测试隔离工具：统一将 `HOME` / `USERPROFILE` 指向 `t.TempDir()`，并清理 `HOMEDRIVE` / `HOMEPATH`，确保跨平台路径解析稳定。
- 新增测试文件落盘断言：明确校验配置仅写入临时目录，防止后续改动回退到真实用户目录。

---

# Code Switch v2.6.36

## 修复
- **日志金额气泡竞态**：修复金额列悬浮时的异步竞态问题，避免鼠标移出后气泡仍延迟出现
- **模型价格匹配口径**：补齐 `anthropic.` 前缀剥离与 `gpt-5-codex -> gpt-5` 别名匹配，减少“有价却显示无价”的误判
- **费用拆分一致性**：日志读取时补齐 `input/output/reasoning/cache` 的费用拆分字段，前端公式说明与后端计费保持一致

## 体验优化
- **金额气泡可读性**：气泡支持交互停留与滚动查看，长公式不再被截断，边界避让更稳定
- **样式细节**：优化金额 hover/focus 态与暗色主题对比度，提升整体视觉一致性

## 技术改进
- 金额气泡定位逻辑增加 `requestId + anchor` 防抖防串号机制
- 前端日志类型新增 `reasoning_cost` 字段，避免类型漏字段导致说明计算偏差

---

# Code Switch v2.6.35

## 新功能
- **模型价格模糊匹配**：当日志中的模型名未命中精确价格表时，自动匹配最相近模型并计算价格
- **日志模型列增强**：在日志列表中展示“原模型 + 计价匹配模型”，便于追踪价格来源

## 修复
- **Claude 流式 token 统计稳定性**：修复按行 SSE 回调和非标准响应场景下 token 解析遗漏问题，避免日志持续显示 `0`

## 技术改进
- 价格匹配升级为“精确命中 + 别名 + 相似度评分”的多阶段策略，降低新模型版本号变更导致的漏计费
- 日志读取阶段支持在 `total_cost=0` 但可命中价格时动态补算，兼容历史数据
- 新增 `model-pricing` 模糊匹配单测和日志解析回归测试，减少回退风险

---

# Code Switch v2.6.34

## 修复
- **Claude 日志 token 归零**：修复模型价格配置改造后，部分 Claude 请求在日志中 `input/output` 显示为 `0` 的问题
- **SSE 分片兼容**：修复 `data:{...}`（无空格）和跨 chunk 分片场景下的 usage 解析丢失
- **非流式 usage 兼容**：补齐非流式 JSON 响应的 usage 解析，避免只依赖 SSE 行格式导致漏记

## 技术改进
- Claude usage 解析新增多字段兼容：`input_tokens/output_tokens`、`prompt_tokens/completion_tokens` 及缓存字段别名
- 新增回归测试，覆盖 SSE 分片、非流式 JSON 分片与字段优先级，防止 token 统计回退

---

# Code Switch v2.6.33

## 体验优化
- **模型价格弹窗二段式**：弹窗第一页仅展示模型列表；点击列表中的模型才会弹出编辑弹窗，保存后自动关闭编辑弹窗并回到列表
- **弹窗宽度更合理**：列表弹窗更宽方便看表，编辑弹窗更窄专注改参数

## 技术改进
- **嵌套弹窗滚动锁定修复**：滚动锁改为引用计数，避免二层弹窗关闭时页面滚动被提前解锁

---

# Code Switch v2.6.31

## 修复
- **WebDAV 同步卡进度/大文件卡顿**：同步改为逐文件上传（不再本地打包 zip），进度更平滑，也更容易定位卡住的具体文件
- **同步内容过滤**：排除 `.DS_Store` 等无关文件，避免把 macOS 垃圾同步上去
- **错误可见性**：同步弹窗新增实时日志区，上传失败会在进度下方持续输出错误信息

## 技术改进
- WebDAV 备份采用 `manifest.json` + 目录结构（兼容旧 zip 备份的恢复）

---

# Code Switch v2.6.30

## 新功能
- **同步进度弹窗**：同步到 WebDAV 时展示同步内容清单与实时进度（含上传字节与远程 URL）

## 修复
- **WebDAV 上传兼容性**：PUT 请求补齐 `Content-Length`，兼容部分不支持 chunked 上传的 WebDAV / 网盘网关
- **设置项遮挡**：修复 WebDAV「同步操作 / 同步内容」等长文案在设置页被裁切的问题

## 技术改进
- WebDAV 同步流程通过 Wails Event 推送阶段与进度，前端可实时订阅展示
- 新增本地同步内容预览接口（用于弹窗展示）

---

# Code Switch v2.6.29

## 新功能
- **WebDAV 同步备份/恢复**：在「通用设置」新增 WebDAV 配置，支持一键上传本地配置到 WebDAV、以及从 WebDAV 下载并覆盖本地（下载前自动创建本地备份）
- **可配置超时**：支持设置 WebDAV 请求超时（秒），避免网盘/自建盘网络抖动导致同步失败

## 修复
- **大文件下载内存占用**：下载改为流式落盘，避免压缩包较大时内存飙升
- **界面语言一致性**：成功提示统一走 i18n，避免英文界面弹中文提示

## 技术改进
- SQLite 备份优先使用 `VACUUM INTO` 做一致性快照；失败时自动执行 WAL checkpoint 并重试，尽量避免导出“缺 WAL 的半残包”
- WebDAV redirect 兼容 `303 See Other` 等常见返回

---

# Code Switch v2.6.26

## 新功能
- **日志统计汇总独立存储**：请求量 / Token / 缓存读取 / 金额总数按本地小时/日聚合入库，查询更快
- **日志存储占用与清理**：新增存储占用面板，支持清除“请求明细”和“一键清除统计汇总”

## 修复
- **日志日期类型联动**：修复切换年/月/日/时间段后，卡片与折线图仍查询当天的问题
- **拉黑配置保存提示**：保存成功/失败 toast 提示更明确

## 技术改进
- 新增 `request_log_stats_hourly` / `request_log_stats_daily` 聚合表及 trigger，降低实时扫表压力
- 增加日志存储统计：展示 DB 使用/可回收空间、各表记录数与占用估算

---

# Code Switch v2.6.25

## 新功能
- **日志查询条件置顶**：平台、供应商、查询按钮移到页面最上方，查询更顺手
- **日志查询日期类型**：新增日期类型（全部 / 年 / 月 / 日 / 时间段），按类型展示不同日期选择器
- **拉黑时长更细粒度**：设置中新增 30 秒、1 分钟、3 分钟拉黑时长（原最小 5 分钟）

## 修复
- **按真实本地日历日筛选日志**：修复时区/日界线导致的“差一天/差几个小时”问题

## 技术改进
- 为 `request_log` 增加按时间/平台/供应商的索引，提升日志查询性能
- 拉黑时长固定模式改用秒级存储（兼容旧分钟配置）

---

# Code Switch v2.6.14

## 新功能
- **自适应热力图**：新增供应商使用热力图功能，直观展示各供应商的请求分布情况，支持按时间范围筛选
- **图标搜索**：新增供应商图标搜索功能，方便快速查找和选择合适的图标

## 修复
- 修复控制台日志递归爆炸问题

---

# Code Switch v2.0.0

## 新功能
- **自定义 CLI 工具支持（Others Tab）**：新增"托管自定义 CLI"功能，支持为任意 AI CLI 工具（如 Droid、RooCode 等）配置代理托管。用户可以自定义配置文件路径、格式（JSON/TOML/ENV）和代理注入字段，实现统一的供应商管理。
- **多配置文件编辑器**：支持为每个自定义 CLI 工具管理多个配置文件，提供实时编辑、格式校验和一键保存功能。

## 修复
- **自定义 CLI 代理路由**：修复自定义 CLI 工具的代理路由 `/custom/:toolId/v1/messages`，确保请求正确转发到供应商。
- **代理注入 URL 格式**：修复代理注入时 URL 路径拼接问题，确保生成正确的 `http://127.0.0.1:18100/custom/{toolId}` 格式。
- **Windows 路径扩展**：修复 Windows 系统下 `~\` 路径前缀的展开问题，正确识别用户主目录。
- **前端 toast 提示**：修复创建/更新 CLI 工具后的成功提示显示。
- **Claude 代理开关状态检测**：修复刷新后 Claude 代理开关显示为关闭的问题。根本原因是 Claude CLI 可能覆盖 `ANTHROPIC_AUTH_TOKEN`，现改为仅检查 `ANTHROPIC_BASE_URL` 是否指向本地代理。

## 技术改进
- 新增 `CustomCliService` 服务，提供完整的 CRUD 和代理状态管理
- 前端新增 `CustomCliConfigEditor` 组件，支持多文件编辑和格式校验
- 中英文国际化支持完善

---

# Code Switch v1.5.4

## 更新亮点
- 🔧 **彻底修复 Claude 代理开关状态问题**：根本原因是后端 `ProxyStatus` 使用 `map[string]string` 解析 `env`，当配置文件中存在非字符串值时解析失败，导致状态始终返回 `false`。现改用 `map[string]any` 宽容解析。
- 🔄 **CI 自动同步版本号**：构建时自动从 Git Tag 提取版本号，更新到所有平台配置文件。
- 📝 **Release 自动提取更新说明**：从 `RELEASE_NOTES.md` 自动提取当前版本的更新内容。

# Code Switch v1.5.3

## 更新亮点
- 🔧 **代理开关状态持久化修复**：修复了刷新页面后代理开关显示为关闭状态的问题，实际上代理仍在运行。问题根源是 Wails RPC 返回的字段名与前端读取的字段名不一致。
- ✏️ **CLI 配置预览可编辑**：配置文件预览区域新增解锁编辑功能，用户可以直接修改配置内容，支持 JSON/TOML/ENV 格式自动解析和校验。
- 🎨 **Tab 按钮左对齐**：Claude Code / Codex / Gemini 三个选择按钮现在与供应商卡片左边缘对齐，视觉更统一。
- 📖 **README 重写**：完全重写 README 文档，面向小白用户，3 步快速开始，包含常见问题解答。
