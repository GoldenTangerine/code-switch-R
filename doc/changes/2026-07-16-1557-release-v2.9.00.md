/**
 * @name: 发布 v2.9.00
 * @Descripttion: 记录供应商模型映射位置调整与临时测试程序清理版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-16 15:57:12
 * @LastEditTime: 2026-07-16 15:57:12
 * @FilePath: doc/changes/2026-07-16-1557-release-v2.9.00.md
 */

# 发布 v2.9.00

- 变更时间：2026-07-16 15:57:12 CST（Asia/Shanghai）
- 涉及范围：应用版本、供应商编辑界面、根目录测试产物、Wails 平台构建元数据、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.8.99` 推进到 `v2.9.00`。
- 将首页供应商编辑中的模型映射移动到预算额度正上方。
- 删除根目录 9 个历史 `test_*.exe` 构建产物，并补充对应忽略规则。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.9.00` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.9.00` Tag 触发全平台自动打包。

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过。
- `pnpm --dir frontend test:unit`：49 个测试文件、343 个测试通过。
- `pnpm --dir frontend exec vue-tsc --noEmit`：通过。
- `scripts/extract_release_notes.py --version v2.9.00`：成功提取本次版本说明。
- `wails3 task common:update:build-assets`：成功同步全平台 `2.9.00` 构建元数据。
- `git diff --check`：通过。
- `.github/workflows/release.yml` 监听 `v*` Tag 推送并执行全平台自动打包。
