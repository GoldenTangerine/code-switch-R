# 发布 v2.8.96

- 变更时间：2026-07-15 15:22:41 CST（Asia/Shanghai）
- 涉及范围：应用版本、Wails 平台构建元数据、Git 更新日志、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.8.95` 推进到 `v2.8.96`。
- 汇总连接详情模型路由、Claude Responses 子代理续链隔离、额度预设确认弹窗和 Codex 压缩流诊断四项变更。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.8.96` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.8.96` Tag 触发全平台自动打包。

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过。
- Claude 子代理隔离、模型路由落库、流诊断和零字节故障转移聚焦 `go test -race`：通过。
- `pnpm --dir frontend test:unit`：45 个测试文件、321 个测试通过。
- `pnpm --dir frontend exec vue-tsc --noEmit`：通过。
- `scripts/extract_release_notes.py --version v2.8.96`：成功提取本次版本说明。
- `git diff --check`：通过。
- `.github/workflows/release.yml` 已确认监听 `v*` Tag 推送并执行全平台自动打包。
