/**
 * @name: 发布 v2.9.05
 * @Descripttion: 记录 Claude 模型映射声明支持 1M 版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-20 14:43:24
 * @LastEditTime: 2026-07-20 14:43:24
 * @FilePath: doc/changes/2026-07-20-1443-release-v2.9.05.md
 */

# 发布 v2.9.05

- 变更时间：2026-07-20 14:43:24 CST（Asia/Shanghai）
- 涉及范围：Claude 供应商模型映射、1M 请求声明、应用版本、Wails 平台构建元数据、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.9.04` 推进到 `v2.9.05`。
- Claude 原生 Anthropic 格式的模型映射新增“声明支持 1M”勾选和 `1M` 状态徽标。
- 精确或通配符映射命中后合并 `context-1m-2025-08-07`，保留并去重已有 `anthropic-beta`。
- OpenAI Chat/Responses 格式隐藏声明选项且不注入 1M beta，切换格式时保留已保存配置。
- 修复隐藏的未保存 1M 草稿可能写入新映射的问题；编辑已有映射时保留原声明。
- 完善保存回显、重命名、删除、复制、归一化、深拷贝及跨层回归测试。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.9.05` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.9.05` Tag 触发全平台自动打包。

## Git 日志

- `🦄 feat(release): 发布 v2.9.05 并支持模型映射声明 1M`

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过；存在已有 macOS target-version linker warning，不影响测试结果。
- `pnpm test:unit`：51 个测试文件、370 个测试全部通过。
- `pnpm exec vue-tsc --noEmit`：通过。
- `python3 scripts/extract_release_notes.py --version v2.9.05 --notes-file RELEASE_NOTES.md`：提取通过。
- `wails3 task common:update:build-assets`：执行成功，各平台构建元数据已同步为 `2.9.05`。
- `git diff --check`：通过，GitHub Release 工作流监听 `v*` Tag。
