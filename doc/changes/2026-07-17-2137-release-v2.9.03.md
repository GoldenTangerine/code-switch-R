/**
 * @name: 发布 v2.9.03
 * @Descripttion: 记录日志多来源会话用量统计版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-17 21:37:57
 * @LastEditTime: 2026-07-17 21:37:57
 * @FilePath: doc/changes/2026-07-17-2137-release-v2.9.03.md
 */

# 发布 v2.9.03

- 变更时间：2026-07-17 21:37:57 CST（Asia/Shanghai）
- 涉及范围：本地会话用量同步、跨来源去重、日志来源切换、存储热力图、应用版本、Wails 平台构建元数据、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.9.02` 推进到 `v2.9.03`。
- 日志页新增“代理 / 会话 / 全部”来源切换，统一作用于明细、汇总、趋势、Provider、模型统计和存储热力图。
- 增量同步 Claude、Codex、Gemini CLI 本地会话用量，Windows 支持手动扫描 WSL 会话日志。
- “全部”模式持久化匹配代理与会话记录，避免重复计算；日志清理支持按需重新导入会话历史。
- 修复 Claude 跨周期最终快照、Gemini 零 Token 补录、思考 Token 去重口径及无效日期来源切换问题。
- 未变化文件直接跳过，Gemini 仅重新计价变化消息，Codex 归档文件继承原游标。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.9.03` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.9.03` Tag 触发全平台自动打包。

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过；存在已有 macOS target-version linker warning，不影响测试结果。
- `pnpm test:unit`：51 个测试文件、364 个测试全部通过。
- `pnpm exec vue-tsc --noEmit`：通过。
- `python3 scripts/extract_release_notes.py --version v2.9.03 --notes-file RELEASE_NOTES.md`：提取通过。
- `wails3 task common:update:build-assets`：执行成功，各平台构建元数据已同步为 `2.9.03`。
- `git diff --check`：通过，GitHub Release 工作流监听 `v*` Tag。
