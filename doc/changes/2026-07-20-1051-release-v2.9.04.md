/**
 * @name: 发布 v2.9.04
 * @Descripttion: 记录首页供应商切换排序优化版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-20 10:51:33
 * @LastEditTime: 2026-07-20 10:51:33
 * @FilePath: doc/changes/2026-07-20-1051-release-v2.9.04.md
 */

# 发布 v2.9.04

- 变更时间：2026-07-20 10:51:33 CST（Asia/Shanghai）
- 涉及范围：首页供应商排序、编辑失败回滚、应用版本、Wails 平台构建元数据、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.9.03` 推进到 `v2.9.04`。
- 已有供应商关闭后移动到未启用组首位，重新启用后移动到启用组首位，连续关闭时最近关闭的供应商优先显示。
- 新增供应商、手动拖拽及组内排序持久化规则保持不变。
- 编辑弹窗切换启用状态后若保存失败，完整恢复供应商列表、排序字段和弹窗卡片引用。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.9.04` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.9.04` Tag 触发全平台自动打包。

## Git 日志

- `🦄 fix(release): 发布 v2.9.04 并优化供应商切换排序`

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过；存在已有 macOS target-version linker warning，不影响测试结果。
- `pnpm test:unit`：51 个测试文件、366 个测试全部通过。
- `pnpm exec vue-tsc --noEmit`：通过。
- `python3 scripts/extract_release_notes.py --version v2.9.04 --notes-file RELEASE_NOTES.md`：提取通过。
- `wails3 task common:update:build-assets`：执行成功，各平台构建元数据已同步为 `2.9.04`。
- `git diff --check`：通过，GitHub Release 工作流监听 `v*` Tag。
