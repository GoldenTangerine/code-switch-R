/**
 * @name: 发布 v2.8.99
 * @Descripttion: 记录模型映射独立开关与供应商模型搜索下拉版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-16 10:20:51
 * @LastEditTime: 2026-07-16 10:20:51
 * @FilePath: doc/changes/2026-07-16-1020-release-v2.8.99.md
 */

# 发布 v2.8.99

- 变更时间：2026-07-16 10:20:51 CST（Asia/Shanghai）
- 涉及范围：应用版本、Wails 平台构建元数据、Git 更新日志、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.8.98` 推进到 `v2.8.99`。
- 汇总连接详情悬浮窗主题修复、供应商模型搜索下拉和模型映射独立开关。
- 修复关闭映射后的 Claude 透传路由、即时保存竞态和过期请求回滚问题。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.8.99` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.8.99` Tag 触发全平台自动打包。

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过。
- `pnpm --dir frontend test:unit`：49 个测试文件、343 个测试通过。
- `pnpm --dir frontend exec vue-tsc --noEmit`：通过。
- `scripts/extract_release_notes.py --version v2.8.99`：成功提取本次版本说明。
- `wails3 task common:update:build-assets`：成功同步全平台 `2.8.99` 构建元数据。
- `git diff --check`：通过。
- `.github/workflows/release.yml` 监听 `v*` Tag 推送并执行全平台自动打包。
