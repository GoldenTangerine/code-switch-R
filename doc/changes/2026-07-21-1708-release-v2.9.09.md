/**
 * @name: 发布 v2.9.09
 * @Descripttion: 记录模型下拉显示、输入保护和性能优化的版本发布
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-07-21 17:08:37
 * @LastEditTime: 2026-07-21 17:08:37
 * @FilePath: doc/changes/2026-07-21-1708-release-v2.9.09.md
 */

# 发布 v2.9.09

- 变更时间：2026-07-21 17:08:37 CST（Asia/Shanghai）
- 涉及范围：应用版本、Wails 构建元数据、发布说明、Git Tag、GitHub 自动打包

## 变更内容

- 应用版本从 `v2.9.08` 更新为 `v2.9.09`。
- 发布说明新增模型下拉遮挡、输入自动替换和大列表性能修复。
- 同步 macOS、Windows、Linux 平台构建元数据。
- 创建并推送 `v2.9.09` Tag，触发 GitHub Actions 全平台自动打包。

## 验证结果

- `go test ./services/...`：通过；存在已有 macOS target-version linker warning，不影响测试结果。
- `cd frontend && pnpm test:unit`：通过，53 个测试文件、399 个测试。
- `cd frontend && pnpm exec vue-tsc --noEmit`：通过。
- `wails3 task common:update:build-assets`：通过。
- `git diff --check`：通过。
