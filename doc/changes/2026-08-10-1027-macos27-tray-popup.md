/**
 * @name: macOS 27 托盘弹窗兼容修复
 * @Descripttion: 升级 Wails 并恢复 macOS 27 状态栏托盘弹窗的窗口层级行为
 * @version: 1.0.0
 * @Author: sm
 * @Date: 2026-08-10 10:27:32
 * @LastEditTime: 2026-08-10 10:27:32
 * @FilePath: doc/changes/2026-08-10-1027-macos27-tray-popup.md
 */

# macOS 27 托盘弹窗兼容修复

- 变更时间：2026-08-10 10:27:32 CST（Asia/Shanghai）
- 涉及范围：Wails 运行时、Go 工具链、前端绑定、发布工作流、开发文档

## 变更内容

- 将 Wails 从 `v3.0.0-alpha.38` 升级到 `v3.0.0-beta.6`，使用上游已合并的 macOS 托盘窗口置顶修复。
- 将 `@wailsio/runtime` 锁定为 `3.0.0-beta.5`，并重新生成前端绑定。
- 将 Go 最低版本升级到 `1.25`，同步发布工作流和开发文档。
- 适配 beta runtime 的 `WailsEventCallback` 事件类型和可选 map 绑定类型，保持现有业务数据结构不变。
- 按 beta.6 官方生成模板将 macOS 最低版本统一为 `12.0`，避免 plist 与编译参数不一致。
- Linux 继续使用 `gtk3` 兼容标签，并使用独立 nFPM 配置保留 GTK3/WebKit2GTK 4.1 运行时依赖。
- GitHub Release 工作流在 Linux 打包前校验 GTK3 构建标签、独立 nFPM 配置和运行时依赖，并明确生成 GTK3 AppImage、DEB 与 RPM。
- 不增加 Cocoa 私有补丁，不修改现有托盘交互和主窗口生命周期逻辑。

## 验证结果

- `wails3 version`：确认临时 CLI 为 `v3.0.0-beta.6`。
- `wails3 task common:update:build-assets`：通过。
- `wails3 task common:generate:bindings`：通过。
- `pnpm test:unit`：通过，60 个测试文件、456 项测试。
- `pnpm exec vue-tsc --noEmit`：通过。
- `go test -c -o /private/tmp/codeswitch-root.test .`：通过。
- `go test -c -o /private/tmp/codeswitch-services.test ./services`：通过。
- `go test ./resources/model-pricing/...`：通过。
- Linux 开发态与生产态 dry-run：通过，分别使用 `-tags gtk3` 和 `-tags production,gtk3`。
- GitHub Release 工作流静态检查：通过，Linux 打包链路固定使用 GTK3 配置。
- DEB/RPM/AUR 打包 dry-run：通过，版本注入和 GTK3 nFPM 配置路径正确。
- `git diff --check`：通过。
- 完整 Go 测试未在当前沙箱重跑：现有测试会直接删除并重写 `~/.code-switch/app.db`，且部分用例需要监听本地端口；为避免影响真实用户数据，未为此放开沙箱。
- 按项目限制未启动开发服务、未执行构建或打包；macOS 27 托盘弹窗仍需打包态手工验收。
