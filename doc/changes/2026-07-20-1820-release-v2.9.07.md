# 发布 v2.9.07

- 变更时间：2026-07-20 18:20:22 CST（Asia/Shanghai）
- 涉及范围：应用版本、Wails 构建元数据、发布说明、Git Tag、GitHub 自动打包

## 变更内容

- 应用版本从 `v2.9.06` 更新为 `v2.9.07`。
- 发布说明新增 Claude 供应商级认证字段、认证链路修复、导入兼容和模型候选点击选择修复。
- 同步 macOS、Windows、Linux 平台构建元数据。
- 创建并推送 `v2.9.07` Tag，触发 GitHub Actions 全平台自动打包。

## 验证结果

- `go test ./services/...`：通过。
- `cd frontend && pnpm test:unit`：通过，51 个测试文件、382 个测试。
- `cd frontend && pnpm exec vue-tsc --noEmit`：通过。
- `wails3 task common:update:build-assets`：通过。
