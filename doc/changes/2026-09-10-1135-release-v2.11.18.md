<!--
@name: v2.11.18 发布
@Descripttion: 记录托盘图标修复版本的发布准备与验证。
@version: 1.0.0
@Author: sm
@Date: 2026-09-10 11:35:00
@LastEditTime: 2026-09-10 11:35:00
@FilePath: doc/changes/2026-09-10-1135-release-v2.11.18.md
-->

# v2.11.18 发布

- 变更时间：2026-09-10 11:35:00（Asia/Shanghai，UTC+08:00）。
- 涉及范围：托盘图标加载、组件测试、更新日志、版本常量、全平台构建元数据与 Git Tag。

## 变更内容

- 发布 Claude Code 等平台图标预加载修复、供应商图标响应式更新、加载失败重试及备用文字溢出优化。
- 更新 `RELEASE_NOTES.md`，编写对应 Git 提交说明。
- 应用版本与构建配置升级至 `v2.11.18`，执行 `wails3 task common:update:build-assets` 同步平台资源，并保留原有文件头与 LF 换行。
- 发布操作为提交代码并推送 `main` 与 `v2.11.18` Tag，由现有 `Release` 工作流执行全平台自动打包。

## 验证结果

- 托盘与图标相关 9 个测试文件、60 项测试通过；TypeScript 类型检查通过。
- 应用常量、构建配置、macOS/iOS plist、Windows JSON/manifest/NSIS 与 Linux nFPM 版本一致性校验通过。GTK3 打包配置继续从 `PACKAGE_VERSION` 读取发布版本。
- `scripts/extract_release_notes.py` 可准确提取 `v2.11.18` 的版本说明。
- `git diff --check` 通过。
- 发布准备时本地 `main` 与远端一致，远端最新 Tag 为 `v2.11.17`。
- 本地未执行完整平台打包及桌面视觉验收；构建产物以 GitHub Actions 运行结果为准。
