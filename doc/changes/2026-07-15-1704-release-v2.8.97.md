# 发布 v2.8.97

- 变更时间：2026-07-15 17:04:15 CST（Asia/Shanghai）
- 涉及范围：应用版本、Wails 平台构建元数据、Git 更新日志、GitHub Release 更新说明

## 变更内容

- 版本号由 `v2.8.96` 推进到 `v2.8.97`。
- 汇总模型映射强制思考强度、三种 API 格式转换、请求日志来源、能力记忆和兼容重试修复。
- 更新 `RELEASE_NOTES.md`，供 GitHub Actions 按 `v2.8.97` 提取本次版本说明。
- 同步 macOS、Windows、Linux 构建元数据，并通过推送 `v2.8.97` Tag 触发全平台自动打包。

## 验证结果

- `go test ./services/... ./resources/model-pricing/... -count=1`：通过。
- `pnpm --dir frontend test:unit`：46 个测试文件、326 个测试通过。
- `pnpm --dir frontend exec vue-tsc --noEmit`：通过。
- `scripts/extract_release_notes.py --version v2.8.97`：成功提取本次版本说明。
- `git diff --check`：通过。
- `.github/workflows/release.yml` 已确认监听 `v*` Tag 推送并执行全平台自动打包。
